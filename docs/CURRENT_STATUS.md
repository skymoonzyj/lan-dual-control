# 当前开发状态

最后更新：2026-06-17

用途：这是 Windows Codex 和 Mac Codex 每次开工前的第一入口。这里只写当前事实，不写长期规划。

## 总体状态

- 项目中心仓库：`lan-dual-control`
- 当前主目标：继续把 Windows 控制 Mac 做成真实可日常试用的版本，同时保留 Mac 反控 Windows 的骨架。
- 当前优先级：真实 Mac 被控端验收、输入注入安全验证、真实 Mac 音频长时间验证、H.264 端到端解码体验和低延迟稳定性。
- 协议入口：`docs/03-architecture-and-protocol.md`
- 任务入口：`docs/04-task-board.md`
- 下一步入口：`docs/NEXT_ACTIONS.md`

## Windows 端状态
- Windows 常用检查/回归脚本已继续补齐 `--help/-h` 纯帮助入口：`check-windows-audio-devices`、`check-windows-firewall`、`test-auth-retry-policy`、`test-coordinate-mapping` 和 `test-windows-input-helper` 查参数时不会列设备、探测端口、启动临时服务、创建 input helper 或运行断言。
- Windows `.mjs` 工具脚本新增统一帮助入口覆盖自检 `scripts/windows/test-windows-script-help.mjs`，会逐个验证 `scripts/windows/*.mjs` 的 `--help` 和 `-h` 都快速 0 退出并打印 Usage/Options 帮助；当前 45 个脚本、90 条帮助命令全部通过，也修正了 `check-windows-host-readiness`、`start-windows-host` 和 `test-windows-host-start-helper` 的 `-h` 短写误忽略问题。
- Windows 音频观察脚本已可用 `--help/-h` 纯查看参数，不会启动临时 host；也可统计 `audio_frame.timestamp` 接收年龄，并用 `--maxFrameAgeMs` / `--requireMonotonicTimestamp` 做音频帧新鲜度强校验；本机 WASAPI 短观察通过，倒退 timestamp 的临时假 host 会按预期失败。
- Windows 侧 Mac 联络板提醒 watcher 已整理为可回归工具：`scripts/windows/start-mac-alert-watcher.ps1` 后台启动时优先使用 PowerShell 7 `pwsh`，读取 Agent Link Board 后在 Mac 端 `NEED_USER_AUTH`、`USER_ACTION_REQUIRED`、权限/授权、502/Bad Gateway、`blocked` 状态或长时间无更新时弹 Windows 本机提醒；现在也会识别 Mac Codex 发给 Windows Codex/Windows host 的 active `currentCall`，以及 Mac 端等待 Windows 临时反控授权时常见的 `LAN008`、`ReverseGrant`、`allow-windows-reverse-control`、`临时允许反控` / `重试反控` 文案，让 Mac 控制 Windows 正式 smoke 或反控演练的 `--sendCall` 能在 Windows 本机弹窗/日志提醒。启动器用 `.dev-lab/mac-alert-watcher.pid` 防重复启动，并支持 `-Status`、`-Stop`、`-Restart` 管理后台进程；需要给恢复总览、桌面壳或自动化消费时可加 `-Json`，输出单个不含 token 的机器可读对象，包含 `action`、`running`、`processIds`、日志路径和 `message`。`watch-codex-link-mac-alerts.ps1 -Once -NoPopup -AlertExistingEvents` 可前台无弹窗调试，`scripts/windows/test-mac-alert-watcher.mjs` 用本机假联络板覆盖授权、中文权限提示、502、反控授权等待、blocked、stale、Mac -> Windows active call、完成呼叫忽略、反向呼叫忽略、默认跳过历史事件，以及 `-Status/-Stop -Json` 纯 JSON 且不泄露 token。
- Windows 恢复开工总览现在优先消费 `start-mac-alert-watcher.ps1 -Status -Json` 判断本机 Mac 提醒 watcher 状态；JSON 报告保留 `windowsMacAlertWatcher.source`、原始 `payload` 和 `parseError`，只有机器可读输出不可用时才退回旧的人类文本解析。

- Windows 控制端已有中文界面、局域网连接、连接历史、刷新设备桌面版自动扫描同网段 `/discovery`、发现真实在线设备后自动选中最佳 WebSocket 目标、画质设置、缩放模式、适应窗口黑边输入防护、认证失败剩余次数提示、诊断状态条、Mac host 运行时 PID/启动时间/build 显示、Mac H.264 启动回退原因显示、真实 Mac H.264/WebCodecs 画布解码、重配置后 H.264 关键帧等待、真实视频帧延迟显示、真实 Mac 音频 PCM 播放、文本剪贴板、远端文件托盘、远端文件托盘清理提示、桌面版远端文件分块写入 Windows 系统文件剪贴板、桌面版本机被控启动/低风险/部署/深度体检入口、反控策略选择、本机一次性临时反控授权按钮、Agent Link Board currentCall 提示、Windows 本机 Mac 提醒 watcher 状态/启动/停止入口和桌面壳。
- Windows 控制端的远控 macOS 按键映射已抽到 `apps/windows-client/mapping-utils.js`，页面和测试共用同一套规则：默认 Win→Command、Alt→Option、Ctrl→Control；开启 Windows 快捷键兼容时，Ctrl+C/V/X/A/Z/F/S/P/O/N/W/T/R 会按 macOS Command 快捷键发送，Ctrl+Y 和 Ctrl+Shift+Z 统一映射为 Command+Shift+Z。`scripts/windows/test-coordinate-mapping.mjs` 已覆盖这些纯函数规则和自定义修饰键映射；`scripts/windows/test-windows-client-browser.mjs --diagnosticsOnly` 也新增页面级断言，确认 Ctrl+C -> Command+C、关闭兼容后 Ctrl+C -> Control+C，以及自定义 Win/Alt/Ctrl 映射生效。
- Windows 控制端顶部“输入事件”状态已补齐输入模式提示：Mac host 处于 `inputMode=log` 时会直接显示“安全日志，不会真正控制”，真实注入时显示“真实控制 / 已注入”，拒绝时显示“被拒绝”和错误码；`test-windows-client-browser.mjs --diagnosticsOnly` 已覆盖该 UI 文案，避免用户看到已连接但不能点击时误判为故障。
- Windows 控制端断线自动重连体验已补齐倒计时和手动入口：非手动断线后状态栏会按秒显示下一次自动重连，连接面板出现“立即重连”按钮，可跳过等待直接执行当前重连尝试；“断开”仍会停止自动重连。`test-windows-client-browser.mjs --diagnosticsOnly` 已覆盖倒计时、按钮显示和立即重连后清理等待计时器。
- Windows 控制端导出日志/状态快照现在顶部会先给出“快速摘要”，一屏内汇总远端连接、重连、本机协作和画质请求；下面再把远端连接和本机协作分段。远端连接记录重连状态、重连原因、下次重连倒计时和主机诊断；本机协作记录 Mac 提醒 watcher 的开启状态、详情、最近检查时间、自动轮询间隔和联络板地址，以及本机被控 Windows host 的状态、端口、画面/声音/输入/反控策略、体检档位、媒体基线开关和最近状态输出脱敏摘要。事件面板也有“复制诊断”按钮，复用同一份脱敏文本写入剪贴板，便于现场直接粘贴。页面级 diagnostics 回归在模拟断线等待态下确认快速摘要和这些字段会进入导出/复制文本，便于真实联调现场复盘断线恢复、窗口最小化提醒链路和 Mac 反控 Windows 准备状态。
- Windows 控制端顶部“帧延迟”卡片和诊断条现在会用 `video_frame.timestamp` 估算远端帧到达本机时的新鲜度；没有真实帧时间戳时保持等待，两端系统时钟明显不一致时显示“时钟偏差”，不再显示随机模拟延迟。`test-windows-client-browser.mjs --diagnosticsOnly` 已固化该显示、时钟偏差提示和诊断条 warning 回归。
- Windows 控制端可连接本机假 Mac 服务，也可用脚本探测真实 Mac 被控端；`scripts/windows/discover-lan-hosts.mjs` 可扫描本机 IPv4 局域网或指定网段的 `/discovery`，也可用 `--noLocalSubnets --host <Mac IP> --requireMacHost --boardSummary` 做只读定点发现并输出可直接发 Agent Link Board 的无密摘要。发现 Mac host 时，JSON 会保留通用 `found`，并额外输出 `macHosts`、`bestMacHost`、`bestMacHost.buildDiff`、`macFormalE2e.preflightCommand`、`userAuthRequestCommand`、`sendUserAuthRequestCommand` 和 `formalCommand`，方便下一步正式 E2E；其中 `sendUserAuthRequestCommand` 会在后续预检 ready 时由 `check-mac-formal-e2e --sendUserAuthRequest` 自动发送无密授权提醒，未 ready 会拒绝发送。`buildDiff` 只比较运行中 build 到当前 git 的 `apps/mac-host/Package.swift` 与 `apps/mac-host/Sources` 变化，能区分 `stale-metadata` 和 `restart-recommended`。2026-06-16 真实 `192.168.31.122:43770` 已在 runtime build `c5e5009` 完成 Windows formal Mac E2E：H.264/WebCodecs 解码、1920x1080 canvas、音频 PCM 播放、文本/文件剪贴板、input-log、客户端诊断和黑边防护均通过，node exit code 0；formal E2E 当时未执行 `inject`，随后已单独完成 safe inject 小验收，见下一条。`test-windows-client-browser.mjs` 也支持 `--discover`、`--discoverNoLocalSubnets` 和 `--discoverTimeoutMs`：可先自动选中最佳 Mac host，再用 `--diagnosticsOnly --expectDiscoveryRuntimeBuildId <build-id>` 不输入密码验证真实 `/discovery.runtime` 是否显示到设备列表和诊断条，并断言刷新设备会自动选中 WebSocket 目标；新增 `test-windows-client-browser-discover.mjs` 用本机假 `/discovery` + Edge diagnosticsOnly 覆盖该路径；假 Mac 服务已对齐 3 次认证失败断开行为。
- Windows 底层 Mac host 探针 `scripts/windows/probe-mac-host.mjs` 的 `--inputEvents` 已增加真实注入验收守卫：`--expectInputInjected true|false` 会强制检查每个 `input_ack.injected`，避免把 log-only ack 误当成真注入；`--inputEventSet safe|full` 默认只发低副作用安全集（鼠标移动 + F13），需要旧的点击/滚轮/快捷键覆盖时才显式用 `--inputEventSet full`。PowerShell 包装 `scripts/windows/test-mac-host.ps1` 同步支持 `-ExpectInputInjected` 和 `-InputEventSet`。2026-06-16 真实 Mac inject 小范围验收已通过：Mac host `192.168.31.122:43770` 由 Mac Codex 在用户确认看着 Mac 屏幕后切到 `inputMode=inject`，runtime build `d398d64`，Windows 本机隐藏输入密码后运行 `node scripts/windows/probe-mac-host.mjs --host 192.168.31.122 --port 43770 --promptPassword --requirePassword --inputEvents --inputEventSet safe --expectInputMode inject --expectInputInjected true`，probe exit code 0，2 个 safe 事件均收到 `input_ack injected=true`；未发送密码，未执行点击、Delete、Ctrl+A 或 full event set，transcript：`.dev-lab/mac-inject-safe-20260616-091526.transcript.txt`。后续复跑时，Mac 端启动 inject host 必须使用 `node scripts/mac/start-mac-host.mjs --promptPassword --requirePassword --inputMode inject --confirmUserWatching --background`。
- Windows 端新增 `scripts/windows/check-mac-formal-e2e.mjs` 正式 Mac 端到端验收聚合脚本，并新增 PowerShell 包装入口 `scripts/windows/check-mac-formal-e2e.ps1`：正式路径会先做无密码 `/discovery` 预检，再用 `LAN_DUAL_PASSWORD` 把用户本机输入的正式密码传给子探针，默认串联 H.264 长观察、PCM 音频、文本/文件剪贴板、`inputMode=log` 输入 ack 和 Windows 控制端页面 H.264 自检；不会把密码放进命令参数，不执行 `inject`。可加 `--discover` / PowerShell `-Discover` 先调用 `discover-lan-hosts --json --requireMacHost` 自动选中最佳 Mac host，避免正式验收前手工复制 IP/端口；`--discoverNoLocalSubnets` / `-DiscoverNoLocalSubnets` 可把发现限定为 127.0.0.1 和显式目标。底层 `scripts/windows/probe-mac-host.mjs` 也支持同一组 `--discover` 参数，可单独做 H.264、音频、剪贴板或 input-log 探针而不手工填写 IP/端口；PowerShell 包装 `scripts/windows/test-mac-host.ps1` 同步支持 `-Discover`、`-DiscoverNoLocalSubnets` 和 `-DiscoverTimeoutMs`，发现失败会先退出，不弹密码框；新增 `scripts/windows/test-probe-mac-host-discover.mjs`、`scripts/windows/test-mac-host-powershell-discover.mjs` 和 `scripts/windows/test-mac-formal-e2e-powershell.mjs` 用本机 mock Mac 覆盖自动发现、认证、会话、首帧、正式 runner PowerShell 预检和快速正式路径，并确认离线 discovery 会先失败、不弹密码。`--preflightOnly --json` / `-PreflightOnly -Json` 可输出机器可读预检结果，并带 `runPlan` 字段列出正式验收将跑的子步骤、预计耗时、视频/音频/剪贴板/input-log 范围和密码传递方式；普通 `--preflightOnly` 输出也会展示同一份安全计划。`--preflightOnly --boardSummary` / `-PreflightOnly -BoardSummary` 可输出一段适合直接发 Agent Link Board 的无密摘要；`--preflightOnly --checkClientDiagnostics` 会额外启动 Windows 控制端诊断，并在 Mac host 提供 runtime build 时强校验 UI 发现到同一个 build；`--preflightOnly --userAuthRequest` 可输出标准 `NEED_USER_AUTH` 提醒文本；`--preflightOnly --sendUserAuthRequest` 或 PowerShell `-SendUserAuthRequest` 只会在预检 ready 时把该无密提醒发到 Agent Link Board，未 ready 时拒绝发送，避免误催用户输入密码。当前真实 Mac host `192.168.31.122:43770` 已在 build `c5e5009` 跑完整正式 E2E 并通过：实收约 52.4 FPS、协商 30 Hz、请求 60 Hz，H.264/WebCodecs `H264Errors=0`，音频、剪贴板和 input-log 均 OK；正式密码未上通讯板、未写入命令参数，`inject` 未执行。回归脚本 `scripts/windows/test-mac-formal-e2e-preflight.mjs` 覆盖离线文本/JSON/boardSummary/userAuthRequest/sendUserAuthRequest、mock JSON/boardSummary/userAuthRequest/sendUserAuthRequest、mock UI diagnostics、discovery 自动选中、密码守卫、runPlan 安全形状和 mock 快速路径摘要，确保不泄露密码。
- Windows formal E2E 和底层 `probe-mac-host` 的长时间视频/音频观察已补充进度心跳：开始观察会打印目标时长，默认每 10 秒输出已收帧数、剩余时间、当前 FPS 和最大帧间隔；`--progressIntervalMs <ms>` 可调整频率，`0` 可关闭。这样现场跑 5 分钟 H.264 长测时不会再看起来像卡住。
- Windows 控制 Mac 页面自检 `scripts/windows/test-windows-client-browser.mjs` 也已补齐等待进度心跳：连接、视频 surface、H.264/WebCodecs 和 PCM 音频播放等待会默认每 10 秒输出状态、远端、诊断、FPS、音频和帧计数快照；`--progressIntervalMs <ms>` 可调，`0` 可关闭。页面自检还支持 `--boardSummary`：stdout 只输出一行无密 `Windows client diagnostics: ...` 摘要，详细 `[OK]` 进度转 stderr，便于直接发 Agent Link Board；diagnostics-only 路径已验证该摘要包含检查项、目标、发现结果和 no password/no input/inject 边界。页面自检还修复了重连控件 diagnostics 后连接按钮禁用状态恢复漏项，避免后续真实连接点击无效。
- Windows 端新增 `scripts/windows/check-windows-resume-status.mjs` 恢复开工总览，并新增 PowerShell 包装入口 `scripts/windows/check-windows-resume-status.ps1`：只读汇总当前 git 状态、可选 Agent Link Board 快照、Mac formal E2E 无密预检和下一步安全命令，并附带 Windows 控制端无密页面诊断 `test-windows-client-browser --discover --diagnosticsOnly --boardSummary` 命令、页面内“复制诊断 / 快速摘要”操作提示、本机 Windows host 媒体基线 `check-windows-host-readiness --probeMedia --boardSummary` 命令，以及本机一次性反控授权 `allow-windows-reverse-control --host 127.0.0.1 --port 43770 --durationMs 30000 --boardSummary` 命令；JSON 和普通输出还会给出 Windows 本机 Mac 提醒 watcher 的启动/状态命令 `start-mac-alert-watcher.ps1 -Server <Agent Link Board>` / `-Status`，并只读显示 watcher 当前 `running/not-running/unknown/unavailable` 状态，方便远控窗口最小化或等待授权时直接判断本机浮窗提醒是否已开。默认 `--discover` / PowerShell 默认发现会自动选中最佳 Mac host，不认证 WebSocket、不要求或打印密码、不发送输入、不执行 `inject`。`--checkBoard` / `-CheckBoard` 现在会优先直接读取 Agent Link Board `/api/state` 的 `currentCall`，失败时才退回旧命令输出解析；JSON、普通输出和 `--boardSummary` 会标出 active Mac -> Windows 测试呼叫，DONE call 不会当作待办摘要，方便 Windows 恢复开工时直接看到 Mac 端要自己做什么；`--boardSummary` / `-BoardSummary` 可输出一行适合发到通讯板的秘密安全摘要，且包含 `WinClientDiagnostics=`、`CopyDiagnostics=`、`WindowsHostMedia=` 和 `ReverseGrant=`；`--userAuthRequest` / `-UserAuthRequest` 可在预检 ready 后直接输出可发联络板的 `NEED_USER_AUTH` 文本，并优先给出 PowerShell 正式验收命令；显式 `--sendUserAuthRequest` / `-SendUserAuthRequest` 会在 preflight ready 时才发送这段无密文本到 Agent Link Board，preflight 不 ready 时拒绝发送并失败，避免误催用户输入密码；`--json` / `-Json` 可供自动化消费。Mac host 离线默认只作为提醒，只有加 `--requireMacReady` / `-RequireMacReady` 才让脚本失败，`--requireClean` / `-RequireClean` 可把脏工作区升级为失败。2026-06-16 已完成一次正式 Mac E2E 通过记录；后续恢复开工仍建议先跑总览确认 Mac host 是否在线、build 是否当前、通讯板是否有新 call。回归脚本 `scripts/windows/test-windows-resume-status.mjs` 和 `scripts/windows/test-windows-resume-status-powershell.mjs` 覆盖帮助、mock 在线 JSON、通讯板摘要、结构化 active/DONE currentCall、UserAuthRequest、SendUserAuthRequest、Windows 控制端诊断命令/复制诊断提示、Windows host 媒体命令、Windows 本机反控授权命令、Windows 本机 Mac 提醒 watcher 命令与只读运行状态、离线默认非失败和 `--requireMacReady` / `-RequireMacReady` 失败路径，并确认不泄露密码。
- Windows 被控端已有 WebSocket 骨架、认证、FFmpeg gdigrab MJPEG 视频帧、可选 FFmpeg H.264 Annex B 视频帧（默认 `libx264`，可选 `h264_nvenc` 等 encoder）、无 FFmpeg 时的 Windows 系统截图 JPEG 回退、默认模拟音频帧、显式 WASAPI loopback 系统声音采集入口、显式 DirectShow PCM 音频设备采集入口、文本和文件剪贴板接收、常驻 C# SendInput helper 输入注入；`/discovery` 和 `hello_ack` 会带可选 `runtime` 诊断，显示 `processId`、`startedAt`、`uptimeSeconds` 和 `buildId`。Windows host 的 `reverse_control_request` 已改为策略化安全回执：未认证仍按 `LAN002` 拒绝，认证后默认 `reverseControlMode=deny` 用 `LAN008` 说明需要用户确认且保持方向不变，并在 `/discovery.capabilities.reverseControlGrant.lastRequest` 短时暴露最近一次被拒绝请求的 requestId、来源、时间和状态；Windows 本机桌面壳可通过只允许回环访问的 `/reverse-control/grant` 打开约 30 秒一次性临时授权，下一次请求会返回 accepted 并立即消耗；命令行备用入口 `allow-windows-reverse-control.mjs` 也可查看状态、打开一次性授权、撤销授权，并输出无密 JSON 或 Agent Link Board 单行摘要；只有显式 `LAN_DUAL_WINDOWS_REVERSE_CONTROL_MODE=accept` / `start-windows-host --reverseControlMode accept` 的可信局域网实验模式才长期自动同意，`disabled` 会在发现能力里声明不可用，专项回归 `test-windows-host-reverse-control.mjs` 和 `test-windows-reverse-control-grant-helper.mjs` 已覆盖。启动助手 dry-run、`--status --json`、普通状态输出和 `--boardSummary` 现在都会显示当前反控策略、临时授权和最近请求状态，并在默认需确认策略下给出 `windowsReverseControlGrantCommand` / `ReverseGrant=` 本机临时授权命令；readiness runtime 摘要也会优先保留 `reverse=temporary-grant|pending-request|deny-confirm|accept-lab|disabled`，并在 boardSummary 被压缩时仍独立保留 `ReverseGrant=`；Windows 桌面“本机被控”面板也已接入同一策略选择和状态显示，启动前默认“需确认”，如果 Mac 先请求过会提示“反控：刚收到请求”，便于 Windows 本机临时允许后让 Mac 重试。
- Windows 被控端文件剪贴板接收已完成完整性审查整改：`clipboard_file_chunk` 必须先有已接受的 `clipboard_file_offer`，offer 阶段校验文件数量、总大小和清单一致性，chunk 阶段校验 `fileIndex`、`chunkIndex`、`offset`、声明 bytes、协商分块大小、不越界且不重叠；完成阶段按每个文件真实覆盖区间和 expected size 判断完整，不再用重复/重叠分片累计字节蒙混通过。新增 `scripts/windows/test-windows-clipboard-bridge.mjs` 覆盖模块级无 offer、超大 chunk、重复/重叠 chunk、不完整文件、总大小/文件数上限、空文件和乱序合法分片；新增 `scripts/windows/test-windows-host-clipboard-security.mjs` 通过真实 WebSocket host 覆盖服务级无 offer、超总量/文件数、超大 chunk、重复/重叠、未接收完整、bytes 不一致和错误 `fileIndex` 拒绝路径；`check-windows-host-readiness --probeClipboardSecurity` 和 `--profile deep` 现在也会把该服务级坏包回归纳入一键体检。Windows 审查机新增 `scripts/windows/test-clipboard-integrity-suite.mjs`，可一条命令串联 Windows 模块级、Windows WebSocket 服务级和 Mac host 源码完整性三项剪贴板回归，并支持无密 `--boardSummary` / `--json` 输出。
- Windows 被控端新增 `scripts/windows/check-windows-host-readiness.mjs` 一键体检入口，默认低风险串联 Node/FFmpeg、Windows Graphics Capture 只读预检、Windows host 语法、输入 helper 安全干跑、音频设备/WASAPI 格式、局域网/防火墙只读检查和运行中 host runtime 检查；runtime 步骤现在消费 `start-windows-host --status --json`，统一使用状态助手的 runtime、buildDiff 和 capabilities，默认不要求 host 在线，若在线但 build 落后当前 git 只给 warning。`--checkBoard` 会只读读取 Agent Link Board `/api/state.currentCall`，JSON、普通输出和 `--boardSummary` 会提示 active Mac -> Windows call，DONE call 不进入待办摘要；读取失败只作为 warning，不认证、不发送密码、不执行输入。可显式加 `--requireWgc` 强制要求 WGC 支持预检通过；本机 WGC 预检已通过，Windows 11 build `26200`、WGC WinRT 类型齐全、`GraphicsCaptureSession.IsSupported()` 为 `true`，检测到 2 个硬件 GPU 和 5 个虚拟显示适配器。可显式加 `--requireCurrentBuildId` 或 `--expectBuildId <id>` 强校验运行中 host build；`--profile deploy` 会在 Windows host 已启动后开启严格模式、要求端口可达、要求当前 build，并跑带 1000ms 帧新鲜度和 timestamp 单调性强校验的视频/音频短验收，`--profile deep` 会在 deploy 基础上再跑 Windows host PowerShell 本机自检；`scripts/windows/test-windows-host.ps1` 可临时启动本机服务并验证真实 JPEG 首帧、文本剪贴板、文件剪贴板接收，也可加 `-AudioMode wasapi -RequireAudio` 强校验系统声音 PCM；`scripts/windows/check-windows-firewall.mjs` 可只读检查 Windows host 端口监听、局域网 IP 探测、当前网络配置和 TCP 入站放行规则，并给出管理员放行建议命令但不会自动改系统；`scripts/windows/measure-windows-input-helper.mjs` 可安全干跑量化常驻 input helper 的冷启动和热路径延迟；`scripts/windows/observe-windows-host-video.mjs` 可观察持续视频帧并统计实际 FPS、真实新帧 FPS、唯一 WGC helper 源帧 FPS、重复帧比例、最大间隔、采集管线、请求码率、`jpegQuality`、`video_frame.timestamp` 接收年龄和本机 host 资源摘要，可用 `--minFreshFps`、`--minUniqueHelperFps`、`--maxRepeatedFrameRatio`、`--maxContentAgeMs`、`--maxFrameAgeMs` / `--requireMonotonicTimestamp` 强校验帧节奏和新鲜度，也可用 `--useDefaultMaxScreenFps --expectSessionFps 60` 验证普通启动默认上限；当前 FFmpeg gdigrab 60 Hz 基线为 4 秒 230 帧、57.1 FPS、最大间隔 41 ms、帧年龄最大 1 ms，带 `--resourceSampleTree true` 的 60Hz 资源对照为 4 秒 198 帧、49.49 FPS、最大间隔 43 ms、帧年龄最大 1 ms、进程树 CPU 平均/峰值 4.5/5.4%、工作集峰值约 309.3 MiB；Windows host FFmpeg gdigrab 普通启动默认最高 60Hz，可用 `LAN_DUAL_WINDOWS_MAX_SCREEN_FPS` 降回 30 省资源；Windows host 会按既有 `qualityPreset`/`maxBandwidthKbps` 调整 FFmpeg/System.Drawing JPEG 压缩质量，未设置 `LAN_DUAL_WINDOWS_JPEG_QUALITY` 时自动计算，设置后可强制覆盖；`scripts/windows/observe-windows-host-audio.mjs` 可观察持续音频帧并统计稳态 FPS、最大间隔、payload、电平和本机 host 资源摘要，支持显式 `--playTone --requireLevel` 播放短测试音并强校验 WASAPI 电平；当前 WASAPI 本机 30 秒稳态基线为 1482 帧、49.98 FPS、最大间隔 33 ms、首帧约 395 ms、帧年龄最大 1 ms，3.5 秒资源短对照为 135 帧、稳态 49.72 FPS、最大间隔 32 ms、主进程工作集峰值约 62.5 MiB，测试音最高电平旧基线为 0.222；`scripts/windows/observe-windows-host-media.mjs` 可顺序串联视频和 WASAPI 音频观察并输出统一媒体基线报告，默认避免并发启动多个临时 host，现支持 `--boardSummary` 输出一行可发 Agent Link Board 的无密摘要，包含请求分辨率/Hz/Mbps、视频 FPS/最大间隔/帧年龄、音频稳态 FPS/最大间隔/帧年龄和资源采样状态；单路成功单路失败时摘要标为 `Windows media: partial`，全部执行链路失败时标为 `Windows media: failed`，`--json` 失败报告仍可解析并带 `boardSummary` 与 `summary.status=ok|partial|failed`，且视频/音频其中一路失败时会继续尝试另一条链路并保留成功结果；本轮音频顺序路径 3.5 秒通过，视频路径在当前桌面会话遇到 FFmpeg `gdigrab` error 5 / mock fallback，需要稳定后复测或继续推进 WGC 替换；`scripts/windows/test-mac-client-browser.mjs` 可启动 Mac client 页面验证 Mac 反控 Windows 的真实 JPEG 画面、`input_ack`、Mac `Command+C` 到 Windows `Ctrl+C` 映射、最近连接保存/回填/清空且不保存密码、文本剪贴板、Mac 本机文本剪贴板读取/监听、文件剪贴板发送、可选音频帧/PCM payload/播放计数、认证失败剩余次数提示，以及 `--expectRepeatSignalVideo` 下的 WGC `repeatPreviousFrame` 轻量重复帧画面保持/诊断计数，也可加 `--requireAudio` 临时启用 WASAPI 并强校验 `pcm-f32le-base64`；脚本会打印首次视频可见、短窗口实收视频帧/FPS 和首条音频帧耗时，`--maxInitialVideoMs` / `--observeVideoMs` / `--minObservedVideoFrames` / `--minObservedVideoFps` / `--maxAudioFrameMs` 可设强校验阈值，搭配 `--expectReconnect` 时还可用 `--maxReconnectRestoreMs` 强制重连恢复耗时，真实 PCM 播放路径可用 `--maxAudioPlaybackMs` 强制播放计数耗时；`scripts/windows/test-auth-retry-policy.mjs` 可回归 Windows host 和假 Mac 的 3 次认证失败断开策略。
- Windows readiness 现在可显式加 `--probeMedia` 复用 `observe-windows-host-media --json` 做统一视频+音频媒体基线；`--boardSummary` 会显示 `media=ok`、`media=partial(passed=X,failed=Y)` 或 `media=failed(passed=X,failed=Y)`，JSON 结果保留 `Windows host media aggregate` 的 `summary.status`、video/audio/resource 细节，方便 Agent Link Board 和 Mac 端不用解析媒体摘要字符串。
- Windows host 视频/音频/媒体观察脚本已补齐进度心跳：`observe-windows-host-video`、`observe-windows-host-audio` 和 `observe-windows-host-media` 普通输出默认每 10 秒打印观察进度，包含已收帧、剩余时间、FPS、最大间隔、帧年龄、音频电平或聚合子任务等待状态；`--progressIntervalMs <ms>` 可调整频率，`0` 可关闭。视频/音频单脚本也修正了观察窗口尾段等待：到达目标时长且已有帧后会正常收口，不再因为最后一次等帧跨过窗口而多等完整 timeout。`--json` 和 `--boardSummary` 仍保持纯 JSON / 一行摘要，不混入进度行。
- Windows 被控端新增 `scripts/windows/start-windows-host.mjs` 和 PowerShell 包装 `scripts/windows/start-windows-host.ps1`，用于日常启动 Windows host：启动后会列出 Mac 端可填写的局域网地址，等待 `/discovery` 就绪，并自动运行只读局域网/防火墙检查；Node 启动助手和 Windows 桌面壳启动路径都会默认把 `LAN_DUAL_BUILD_ID` 设置为当前 git short hash，便于 readiness 和 Mac client 判断是否连到旧进程；Node 启动助手现在也支持 `--status` 只读查看当前 `/discovery`、runtime、视频/音频/输入/剪贴板能力和旧 build 源码差异，离线时给安全启动建议，不启动服务、不认证、不要求或打印密码；`--status --json` 会输出纯机器可读 JSON，适合联络板或桌面壳消费，并包含 `windowsHostMediaReadinessCommand` 和 `windowsReverseControlGrantCommand`；`--status --checkBoard --json`、普通输出和 `--status --checkBoard --boardSummary` 会只读读取 Agent Link Board `/api/state.currentCall`，提示 active Mac -> Windows call，DONE call 不当作待办；`--status --boardSummary` 会给出 Mac 端 readiness、formal checklist、ready 后 `check-mac-client-formal-status --sendCall` 协调命令，以及 `WindowsHostMedia=check-windows-host-readiness --checkBoard --probeMedia --boardSummary` 媒体基线命令和默认需确认策略下的 `ReverseGrant=allow-windows-reverse-control --boardSummary` 本机临时授权命令，方便 Mac 控制 Windows 真连前发起无密 call、刷新本机视频/音频基线或临时允许一次反控；PowerShell 包装也支持 `-Status`、`-Status -Json`、`-Status -CheckBoard` 和 `-Status -BoardSummary` 走同一条状态检查；可加 `--wasapi` / `-Wasapi` 显式开启系统声音采集；可加 `--promptPassword --requirePassword` 或 PowerShell `-PromptPassword -RequirePassword` 避免退回 demo 密码；可用 `--dryRunFirewallRule` / `-DryRunFirewallRule` 预览防火墙规则，只有显式 `--addFirewallRule` / `-AddFirewallRule` 才会尝试新增 Private TCP 入站规则；`scripts/windows/test-windows-host-start-helper.mjs` 覆盖缺密码拒绝、非交互密码提示拒绝、环境密码干跑、`--status`/`--status --json`/`--status --boardSummary` 在线/离线、fake board active/DONE currentCall、Windows host 媒体基线命令、反控授权命令、防火墙干跑、build id 输出和临时端口真实启动。
- Windows 桌面壳“本机被控”面板已接入 Tauri 原生命令：可在 UI 内选择低风险/部署/深度 Windows host readiness、预览防火墙放行命令、要求隐藏密码后启动/停止 `apps/windows-host/server.mjs`，并轮询 Node 状态助手 `start-windows-host --status --json --checkBoard`，显示真实 `/discovery` 在线/离线、runtime build、视频/音频/输入/剪贴板能力、Agent Link Board currentCall 和最近启动日志；若检测到端口上已有非桌面壳托管的 Windows host，会显示“已在线”，但停止按钮仍只针对桌面壳自己启动的进程；active Mac -> Windows call 会在状态区显示“Mac 正在请求 Windows 配合”，DONE call 只作为非待办记录；输入模式默认“安全日志”，需要真实反控时再手动切到“真实控制”。
- Windows 桌面壳“本机被控”面板现在新增“媒体基线”开关：默认不跑媒体，勾选后 `run_windows_host_readiness` 会传 `probeMedia=true`，Tauri 后端转成 `check-windows-host-readiness --probeMedia`；体检摘要会显示媒体基线正常/部分通过/失败，详情行压缩展示视频 FPS、音频 FPS、最大间隔和帧年龄。
- Windows 桌面壳“本机被控”面板现在也接入 Windows 本机 Mac 提醒 watcher：Tauri 后端调用 `start-mac-alert-watcher.ps1 -Status/-Stop/-Json` 或启动路径，页面“Mac 提醒”区会显示 `提醒中/未开启/不可用`，并提供“开启提醒 / 停止提醒”和“刷新提醒”按钮；浏览器预览版保持禁用，不会启动后台进程。自动状态轮询会把 watcher 查询节流到约 15 秒一次，避免本机状态 2.5 秒轮询频繁启动 PowerShell；手动刷新和开启/停止仍即时执行。导出日志会同步带出 watcher 状态和最近检查时间，方便现场排查 Windows 侧提醒是否真正开启。
- Windows 桌面壳远端文件写入系统文件剪贴板已升级为原生分块路径：前端每次传 1MB 到 Tauri，原生层校验偏移、最终字节数和 512MB 总量上限后写入 Windows 文件剪贴板；新建写入任务前会尽力清理 7 天以上旧临时目录，近期目录会保留以保证系统文件剪贴板可继续粘贴；控制端事件日志和远端文件托盘状态会显示最近一次临时目录，工具栏可一键打开，失败后复制按钮会提示可重试写入；清空托盘会清掉内存暂存和状态提示，但不会删除系统剪贴板仍可能需要的临时目录；旧的一次性写入命令保留作兼容回退，Rust 单测已覆盖偏移错误、写超、未写完、取消清理、超限拒绝和临时目录白名单。
- Windows 被控端已限制同一 WebSocket 连接内最多 3 次密码认证失败，失败耗尽后返回 `LAN002` 并关闭连接。
- Windows 被控端真实屏幕采集目前默认优先 FFmpeg gdigrab 持续 MJPEG，PowerShell/System.Drawing 系统截图作为兜底，全部失败时回退模拟帧；可显式设置 `LAN_DUAL_WINDOWS_SCREEN_MODE=ffmpeg-h264` 或观察脚本 `--screenMode ffmpeg-h264 --preferredVideoCodec h264` 走 FFmpeg H.264 Annex B 过渡模式，默认 `libx264`，也可通过 `LAN_DUAL_WINDOWS_H264_ENCODER` / `--h264Encoder h264_nvenc` 切到 NVENC，并在 `video_frame`、session、discovery 和观察摘要里显示实际 `h264Encoder`。当前真实桌面权限下 720p/30Hz libx264 短基线约 73 帧/2.5 秒、28.83 FPS、最大间隔 53ms；`2026-06-15` 本机 NVENC 短验收约 55 帧/2 秒、27.38 FPS，Mac client binary-h264 观察约 58.2 FPS。普通沙盒上下文仍可能因 `gdigrab error 5` 回退 mock，属于桌面抓屏权限/会话限制。该模式用于 Mac client H.264 接收联调，不替代 WGC。WGC 已有支持预检、显式 `LAN_DUAL_WINDOWS_SCREEN_MODE=wgc` 入口和 `LAN_DUAL_WINDOWS_WGC_HELPER` JSON 行 helper 接入点：未配置 helper时保持 FFmpeg/System.Drawing 降级诊断，配置可执行 helper 且预检通过后会走 `capturePipeline=windows-wgc-helper-jpeg` 并让 `/discovery.capabilities.screen.wgc.active=true`。新增 `apps/windows-wgc-helper` Rust 原生 helper 项目，`--probe` 已在本机成功初始化 D3D11 device、WinRT Direct3D device、主显示器 `GraphicsCaptureItem`、frame pool 和 capture session，识别 `显示 1` 为 `2560x1440`；默认 capture 模式已能从 `Direct3D11CaptureFrame.Surface` 读取真实帧，复制到 D3D11 staging texture，按请求宽高等比缩放且不放大，并用 WIC `ImageQuality` 编码 JPEG 输出 JSON 行 frame；本机 `cargo run -- --frames 1 --fps 10 --width 1280 --height 720 --jpegQuality 0.55` 已输出真实 `1280x720` JPEG，源尺寸 `2560x1440`，本轮首帧约 96 KB，体积会随桌面内容波动。`scripts/windows/test-windows-wgc-helper.mjs` 已验证直接缩放真帧 JPEG、`--mock` 合同帧、Node host mock 合同接入，以及真实 Windows host + 真实 helper 的 `windows-wgc-helper-jpeg` 管线；本轮真实 host 观察收到 14 帧 `1280x720`，平均约 84 KB。新增 `scripts/windows/benchmark-windows-wgc-settings.mjs` 后，本机 2.2 秒短基准显示 30/60/120Hz 会话刷新率均可协商，但当前静态桌面实收约 9-12 FPS。已新增可选 repeat-last-frame 诊断模式：`LAN_DUAL_WINDOWS_WGC_REPEAT_LAST_FRAME=1` 或脚本参数启用后，60Hz/20M 短基准从约 10.45 FPS 提升到约 31.16 FPS，默认三档 repeat 短基准为 30Hz 30.45 FPS、60Hz 33.92 FPS、120Hz 37.78 FPS，内容年龄最大约 80-96 ms。进一步新增轻量信令模式：`LAN_DUAL_WINDOWS_WGC_REPEAT_LAST_FRAME_MODE=signal` 或 `--repeatLastFrameMode signal` 时，重复帧只发送 `repeatPreviousFrame=true` 和诊断字段，不重发 JPEG/base64；60Hz/20M signal 两轮短基准约 31-32 FPS、重复信令帧 33-35、平均图片 payload 约 17-23 KB、内容年龄最大 79-82 ms。Windows host 现在也支持可选 `binary-jpeg` WebSocket 二进制视频帧：Mac client 在 `session_offer/display_settings` 声明 `preferredVideoTransport=binary-jpeg` 后，JPEG 帧会把 JSON 元数据放入二进制头、图片改为原始 JPEG 字节，不再重发 data URL/base64；页面级 `--expectBinaryVideo` 已验证 `jpeg/binary` 画面可见，1.2 秒收到 11 帧、约 9.1 FPS。repeat/signal/binary 可降低重复帧和图片文本传输成本，但真实源帧仍受 WGC `FrameArrived` 节奏限制；后续应把 WGC 采集源接到 H.264/NVENC，并做 Mac client 真连观感验收。音频默认仍为模拟帧，但可显式设置 `LAN_DUAL_WINDOWS_AUDIO_MODE=wasapi` 采集默认播放设备系统声音并发送 `pcm-f32le-base64`，也可配置 `LAN_DUAL_WINDOWS_AUDIO_DEVICE` 试用 FFmpeg DirectShow PCM；后续仍需用 Mac client 做真实听感和延迟验收。
- Windows host `ffmpeg-h264` 模式已修复 H.264 解码失败后的 JPEG 回退可靠性：如果 Mac client/WebCodecs 不支持当前 `codecString` 并发送 `preferredVideoCodec=mjpeg` / `preferredVideoEncoding=data-url`，同一个 `ffmpeg-h264` host 会把当前会话切到 `windows-ffmpeg-gdigrab-mjpeg`，继续配合 `binary-jpeg` 显示真实 JPEG 画面。`test-mac-client-browser --expectH264Fallback` 已验证先 H.264、后 MJPEG fallback 的路径，短窗口 52 帧 / 907ms / 57.3 FPS；`--disableWebCodecs` 的直接 MJPEG fallback 也通过，短窗口 55 帧 / 915ms / 60.1 FPS。
- Windows 端新增 `scripts/windows/check-webcodecs-h264-support.mjs` 本机浏览器 WebCodecs H.264 探针，可用 `--requireCodec avc1.42C02A` 或 `--json` 输出当前 Edge/Chrome 对 `avc1.*` 的 `annexb`/默认 AVC 支持情况。`test-mac-client-browser` 不再在 headless 默认加 `--disable-gpu`，避免把 H.264 能力误判为 unsupported；`--requireH264Video` 已验证 Windows `ffmpeg-h264` 到 Mac client H.264 canvas 解码通过，短窗口 55 帧 / 906ms / 60.7 FPS，页面显示 `h264 · 解码 #4 · 8 ms · 到达 2 ms`。fallback 回归改用显式 `--forceH264Unsupported` 或 `--disableWebCodecs`，不再依赖禁 GPU 的副作用。
- Windows host 和 Mac client 已把可选 WebSocket 二进制视频传输扩展到 H.264：当 Mac client 请求 H.264 且声明 `binary-h264` 时，Windows host 会把 JSON 头保留在 `LDCV1` binary frame 中，原始 Annex B 字节走二进制 payload，不再用 base64 文本；旧兼容路径仍可用 `?binaryVideo=0` / `--disableBinaryVideo` 回到 `annexb-base64` JSON。页面级 `--expectBinaryH264Video` 已验证 `h264/binary` canvas 画面可见，短窗口 55 帧 / 911ms / 60.4 FPS，收到 23 个二进制 H.264 帧；`--disableBinaryVideo` 兼容回归 54 帧 / 914ms / 59.1 FPS；WGC JPEG `binary-jpeg` 回归 11 帧 / 1213ms / 9.1 FPS。
- Windows 端新增 `scripts/windows/test-mac-client-video-transports.mjs` 视频传输矩阵回归脚本：顺序启动独立端口的临时 Windows host、Mac client 页面和浏览器，默认覆盖 `binary-h264`、H.264 JSON/base64、H.264 unsupported fallback 和 `binary-jpeg` 四条路径；单个 case 失败后默认自动重试 1 次并换用新的临时端口，可用 `--retries 0` 严格复现首次失败。真实 WGC helper + NV12 + NVENC H.264 页面级路径可用 `--case wgc-nv12-h264` 单独跑，或用 `--includeWgcNv12` 加入矩阵。当前默认矩阵 4/4 通过：`binary-h264` 首次瞬时失败后重试成功，55 帧 / 912ms / 60.3 FPS；H.264 JSON/base64 52 帧 / 916ms / 56.8 FPS；fallback 52 帧 / 908ms / 57.3 FPS；`binary-jpeg` 11 帧 / 1209ms / 9.1 FPS。可选 `wgc-nv12-h264` 单项也通过，1080P 49 帧 / 1503ms / 32.6 FPS，session pipeline 为 `windows-wgc-helper-nv12-ffmpeg-h264` / `h264_nvenc`。后续视频传输或 H.264 改动优先跑默认矩阵；涉及 WGC NV12/H.264 时再加 `--includeWgcNv12`。
- Windows 端新增 `scripts/windows/check-windows-video-encoder-support.mjs` 只读视频编码能力体检：汇总 FFmpeg H.264 软件/硬件编码器、Windows Graphics Capture 预检和本机浏览器 WebCodecs H.264 解码能力，支持 `--json` 机器可读输出和 `--requireAnyH264` / `--requireHardwareH264` / `--requireWgc` / `--requireWebCodecsH264` 强校验。本机 `2026-06-15 00:05` 强校验通过：FFmpeg `8.1.1` 检测到 `libx264`、`h264_nvenc`、`h264_qsv`、`h264_amf`、`h264_mf`、`h264_d3d12va` 等 H.264 编码入口，WGC 预检通过，Edge WebCodecs H.264 支持通过；WGC JPEG 桥接 `h264_nvenc` 正确性原型已短测通过。
- Windows WGC helper 已新增 `--outputFormat bgra` / `nv12`，Windows host 可用 `LAN_DUAL_WINDOWS_WGC_H264_SOURCE=raw-bgra` / `nv12` 或 `--wgcH264Source raw-bgra` / `nv12` 将 helper raw BGRA/NV12 帧喂给 FFmpeg/NVENC。raw-bgra 和 NV12 H.264 bridge 都默认使用 helper 内部 `binary-frame-v1`，不再用 JSON/base64 承载 raw 像素；普通 JPEG helper 路径仍保持 `json-lines-v1`。本轮新增 NV12 验证：helper mock binary NV12 合同 2 帧、每帧 72 bytes；mock NV12 H.264 bridge 67 帧通过；真实 helper + `h264_nvenc` 在 `320x180` 2.2 秒收到 67 帧、约 30.12 FPS、fresh helper frame 42，在 `1280x720` 2.2 秒收到 67 帧、约 30.06 FPS、fresh helper frame 12、repeat full 55，pipeline=`windows-wgc-helper-nv12-ffmpeg-h264`。帧节奏诊断新增后，静态桌面 60Hz/20M/NV12/`h264_nvenc`/repeat-full 短测显示会话协商 60Hz，但 1.5 秒实收 35 帧、约 22.94 FPS，真实新帧约 3.93 FPS、唯一 helper 源帧约 4.59 FPS、重复帧 82.9%、内容年龄最大 152ms；这说明后续 60Hz 观感要同时看实收 FPS 和真实源帧 FPS。`benchmark-windows-wgc-settings --motionStimulus` 现可短暂打开 WinForms 或 browser 动画窗口再测 WGC；本机 WinForms 动态窗口 60Hz/20M/NV12/`h264_nvenc`/repeat-full 短测仍只有约 23.52 FPS、真实新帧约 4.92 FPS、重复帧 79.1%，browser 动画约 25.40 FPS、唯一源帧约 4.97 FPS，说明当前瓶颈更像 helper 读回/事件节奏而不是单纯静态桌面。NV12 已把 helper 到 host 的 raw payload 从 BGRA 的 4 bytes/pixel 降到 1.5 bytes/pixel；下一步是 helper 原生硬编、helper 内部采集节奏优化、资源对照和 Mac client 真连观感验收。
- Windows WGC helper 内部阶段耗时诊断已接入：真实 helper frame header 新增 `helperTimingMs`，Windows host 会把它透传到 JPEG 和 H.264 `video_frame`，`observe-windows-host-video` / `benchmark-windows-wgc-settings` 会汇总每个阶段的样本数、平均值和最大值。新增 `scripts/windows/compare-windows-wgc-h264-sources.mjs` 可在同一组分辨率/刷新率/码率下自动对照 raw-bgra 与 NV12 两条 WGC H.264 源格式，并输出 `--json` 或无密 `--boardSummary`；脚本只启动本机临时 Windows host，不连接 Mac、不认证正式密码、不发输入、不执行 `inject`。`2026-06-15` 真实 720p/60Hz/20M/NV12/`h264_nvenc`/repeat-full 短测显示 helper 整帧平均约 127.5ms，其中 `convertEncodeMs` 平均约 126.3ms，`mapMs` 平均约 0.6ms，`copyResourceMs` 约 0.004ms；同日 `1280x720`、30Hz、10M、repeat-full、`h264_nvenc`、1.2 秒 raw-bgra vs NV12 短对照显示 NV12 胜出：raw-bgra 19 帧/15.58 FPS、helper frame avg 72.086ms、convert avg 70.696ms；NV12 28 帧/23.03 FPS、helper frame avg 68.805ms、convert avg 67.741ms。结论：当前 WGC/H.264 仍应优先推进 helper 原生 D3D11/NVENC 硬编或 GPU/SIMD 转换，避免继续在 CPU readback/转换桥上深调。
- Windows readiness 已新增显式 `--probeWgcH264Sources`：它会在低风险体检后额外启动本机临时 Windows host/WGC helper，跑一组 30Hz/10Mbps raw-bgra vs NV12 H.264 短对照，并把结果纳入 JSON、普通输出和无密 board summary；首次失败会换临时端口自动重试一次。该探针比普通 readiness 重，默认关闭，`--profile deep` 也不会自动触发；需要排查 WGC 帧率、重复帧或转换瓶颈时再显式启用。
- Windows `ffmpeg-h264` 过渡路径已支持显式选择 FFmpeg H.264 encoder：`LAN_DUAL_WINDOWS_H264_ENCODER`、`start-windows-host --h264Encoder`、`observe-windows-host-video --h264Encoder`、`test-windows-h264-mode --h264Encoder`、`test-mac-client-browser --h264Encoder` 和视频传输矩阵的 `--h264Encoder` 均已接通。`2026-06-15 00:50` 本机 `h264_nvenc` 强验证通过：Windows host discovery/session/frame 都回传 `h264Encoder=h264_nvenc`，`test-windows-h264-mode` 约 27.38 FPS，Mac client `binary-h264` 页面观察约 58.2 FPS，视频传输矩阵 4/4 通过；默认 `libx264` 回归也通过。该能力只是把 gdigrab H.264 过渡路径切到可选硬编；WGC JPEG 桥接硬编也已验证正确性，真正低延迟路线仍是 raw BGRA/NV12 或原生硬编。
- Windows host 已新增显式 WGC H.264 桥接原型：`LAN_DUAL_WINDOWS_WGC_H264_BRIDGE=1` 或 `start-windows-host --wgcH264Bridge` 开启后，`screenMode=wgc` 且会话请求 H.264 时，会把 WGC helper JPEG 帧喂给 FFmpeg stdin，输出 `capturePipeline=windows-wgc-helper-ffmpeg-h264`；启动助手和 PowerShell 包装已支持 `--wgcHelper` / `-WgcHelper`、`--wgcH264Bridge` / `-WgcH264Bridge`、`--wgcRepeatLastFrame` / `-WgcRepeatLastFrame`。`2026-06-15` 本机真实 WGC helper + `h264_nvenc` 短观察通过：1280x720、30Hz、50Mbps、repeat full，3 秒 91 帧、约 30.29 FPS、最大间隔 59ms、平均帧年龄 1ms；mock helper 的 libx264 桥接合同也通过。该桥仍有 JPEG 解码/重编码开销，只用于正确性原型，下一步应改 helper 输出 raw BGRA/NV12 或直接原生编码。
- Mac 控制 Windows 的 Web 控制端原型已新增到 `apps/mac-client`：可发现/连接 Windows host、显示 JPEG/data-url、`binary-jpeg` 或 `binary-h264` 二进制视频画面；浏览器支持 WebCodecs 时会优先请求 H.264/Annex B 并渲染到 canvas，不支持或连续解码失败时自动请求 MJPEG/JPEG 兜底；可选择画质/分辨率/刷新率/码率并发送 `display_settings`、在会话诊断显示 Windows host 可选 runtime/build、发送鼠标/键盘输入事件并显示 `input_ack`，页面会提示 Mac `Command` 按 Windows `Ctrl` 发送且自检已覆盖该映射，认证失败时显示剩余尝试次数并释放连接按钮，意外断线后最多自动重连 3 次，成功连接后可保存最近 host/port/时间、一键回填或清空，且不保存密码，也可手动发送文本 `clipboard_text` 并显示 `clipboard_ack`，可读取 Mac 本机文本剪贴板，用户显式开启后可监听文本变化并自动发送，以及选择文件后按 `clipboard_file_*` 分块发送；新增 PCM 音频播放入口，可播放 `pcm-f32le-base64` 过渡音频帧，mock 音频帧只显示状态；收到 `video_frame.timestamp` 时，顶部视频状态和会话诊断视频流行会显示帧到达年龄或时钟偏差；收到 `audio_frame.timestamp` 时，顶部音频状态、播放状态和会话诊断音频行会显示帧到达年龄或时钟偏差。Windows 本机页面级自检已验证普通启动真实 `windows-ffmpeg-gdigrab-mjpeg` 画面、Windows host runtime/build 显示、默认 1080P/60Hz/20Mbps 会话请求、2K/60Hz/40Mbps 设置更新和 Windows host 回执里的码率/JPEG 质量、log 模式输入确认、文本剪贴板 `system`、文件剪贴板 `saveMode=clipboard`，以及 `--requireAudio` 下真实 WASAPI PCM 播放计数递增；默认视频自检会在对端提供 timestamp 时断言视频 frame age 已显示，并已兼容 `<img>` 和 `<canvas>` 视频表面；`--expectBinaryVideo` 可验证 WGC JPEG helper 的 `binary-jpeg` 帧保持画面可见且诊断显示二进制帧数；`--expectBinaryH264Video` 可验证 H.264 二进制 payload 到 WebCodecs canvas；`--disableBinaryVideo` 可验证旧 JSON/base64 兼容路径；`--disableWebCodecs` 可验证 MJPEG/data-url 请求 fallback；`--enableAudio --expectAudioFrame` 会在对端提供 timestamp 时断言音频 frame age 已显示；`--expectReconnect` 可验证临时 host 被杀后页面自动重连恢复，并可用 `--maxInitialVideoMs` / `--maxReconnectRestoreMs` 把首帧和恢复耗时变成体验阈值，`--observeVideoMs` / `--minObservedVideoFrames` / `--minObservedVideoFps` 可把持续来帧能力变成体验阈值，`--maxAudioFrameMs` / `--maxAudioPlaybackMs` 可把音频首帧和真实 PCM 播放耗时变成体验阈值。
- Mac client 会话诊断的“反控策略”现在也会消费 Windows host 的 `reverseControlGrant`：一次性临时授权 active 时显示“Windows 已临时允许一次 / N 秒内重试”，最近被默认拒绝的请求 active 时显示“Windows 已收到请求 / 临时允许后重试”。页面还新增受保护的“请求反控/重试反控”入口：只在已连接、已认证且对端支持反控接收时启用；点击只发送 `reverse_control_request`，不会发送 `input_event`；收到默认 `LAN008` 会提示 Windows 临时允许后重试，收到 accepted 会显示“Windows 已同意 / 临时授权已使用”。页面级自测覆盖扁平策略、对象策略、禁用、临时授权、最近请求、默认拒绝、回环临时授权、重试成功、不泄露密码和不发送额外输入事件。
- Mac client “一键反控”区域现在会在 `LAN008` 默认拒绝、最近请求或临时授权窗口状态下直接显示并可一键复制 Windows 本机回环授权命令 `node scripts/windows/allow-windows-reverse-control.mjs --host 127.0.0.1 --port <Windows host port> --grant --durationMs 30000 --boardSummary`；accepted/临时授权已使用后隐藏命令，避免重复授权。日志导出同步带“反控授权提示”“Windows 本机授权命令”和复制状态，便于现场复盘；页面自测锁定命令端口、回环 host、复制到浏览器剪贴板、无密码、无额外 `input_event`。
- Mac client 事件日志面板现在也有“复制诊断”入口，复用同一份不含连接密码的导出日志文本写入 Mac 浏览器剪贴板，方便现场直接粘贴到 Agent Link Board 或发给另一端；页面自测覆盖复制内容、按钮状态、无密码字符串和无额外 `input_event`。
- Mac 端新增 `scripts/mac/start-mac-client.mjs` 本地控制页面启动/状态助手：默认启动 `apps/mac-client/server.mjs` 并等待页面可访问，`--status` 只读检查页面是否在线，`--json` 输出机器可读结果，`--boardSummary` 输出可发联络板的无密摘要，`--allowExisting` 可复用已在线页面。它只启动/检查本地 Web 页面，不连接 Windows host、不认证、不要求或打印密码、不发送输入；自测 `scripts/mac/test-mac-client-start-helper.mjs` 覆盖 help、离线 status、启动成功、重复启动拒绝和 allowExisting 复用。
- Mac 端新增 `scripts/mac/discover-windows-hosts.mjs`，用于 Mac 控制 Windows 真连前只读发现 Windows host：复用现有 LAN `/discovery` 扫描，但只保留 `platform=windows` 的目标；支持 `--json`、`--boardSummary`、`--requireFound` 和 `--noLocalSubnets`，发现后输出可直接运行的 `check-mac-client-formal-status --host <Windows IP> --port <port> --boardSummary` 命令，并在 JSON/board summary 里同步给出 ready 后可用的 `check-mac-client-formal-status --host <Windows IP> --port <port> --sendCall` 协调命令。该脚本不会认证 WebSocket、不会要求或打印密码、不会发送输入事件、不会执行 `inject`；自测 `scripts/mac/test-discover-windows-hosts.mjs` 用假扫描器覆盖发现 Windows、过滤 Mac/self、无 Windows 时的下一步提示、`sendCallCommand` 和摘要不泄密。
- Mac 端新增 `scripts/mac/check-mac-client-readiness.mjs`，用于 Mac 控制 Windows 真连前只读预检：检查 repo、Mac client 静态文件和 JS 语法、可选本地 Mac client HTTP 页面、可选 Windows host `/discovery` runtime/capabilities、可选 Agent Link Board，并输出 `--boardSummary` 或 `--json`。该脚本不会启动 Mac client、不会启动或认证 Windows host、不会要求或打印密码、不会发送输入事件；自测 `scripts/mac/test-mac-client-readiness.mjs` 覆盖离线/require 失败、临时 Mac client server 和 mock Windows `/discovery`。
- Mac 端新增 `scripts/mac/check-mac-client-formal-status.mjs`，作为 Mac 控制 Windows 正式真连前的只读清单：复用 readiness，要求 repo 干净、本地 Mac client 页面在线、Windows host `/discovery` 在线、Agent Link Board 可读，并汇总 H.264/视频传输、音频、input-log、剪贴板和 inject 安全跳过状态，输出 `readyToCall`、`callText`、`runPlan`、`--boardSummary` 或 `--json`。`runPlan` 会列出本地 Mac client 页面、Windows host 发现、formal checklist、真实浏览器 smoke 和质量/资源观察步骤，并明确 `passwordInCommandArguments=false`、`passwordOnAgentLinkBoard=false`、`inject=false`；当本地 Mac client 页面离线时，blocker/warning 的下一步会同时给出 `start-mac-client --allowExisting` 和 `run-mac-client-formal-smoke --ensureClient` 路径，JSON `commands` 也包含 `safePreflightWithEnsureClient`、`sendCallWithEnsureClient` 和带 `--ensureClient` 的正式 `browserSmoke` 命令。该脚本不认证 WebSocket、不要求或打印密码、不发送输入、不执行 `inject`；显式 `--sendCall` 只有 `readyToCall=true` 时才会向 Agent Link Board 发送 Mac Codex -> Windows Codex 的无密正式 Windows host 验收 call，发送前会直接读取 `/api/state` 的 `currentCall`：DONE/COMPLETED/CANCELLED/RESOLVED/CLOSED 等完成态只记录不阻塞，active call 仍默认拒绝覆盖，只有确认协调后加 `--forceCall` 才会替换；自测 `scripts/mac/test-mac-client-formal-status.mjs` 用临时 Mac client server、mock Windows discovery 和假 Agent Link Board 覆盖正式清单、runPlan、`--ensureClient` 提示、ready 发送、离线拒绝、已有 call 拒绝、DONE call 不阻塞和 force 覆盖。
- Mac 端新增 `scripts/mac/run-mac-client-formal-smoke.mjs`，作为 Mac 控制 Windows 真连浏览器冒烟执行器：可加 `--discover` 先只读扫描 Windows `/discovery` 并自动选中最佳 Windows host，发现失败会先退出且不会弹密码框；可加 `--ensureClient` 在预检前调用 `start-mac-client --allowExisting --json` 安全启动或复用本地 Mac client 页面，并在 JSON 输出 `ensuredClient` 摘要，这一步只碰本机 Web 页面，不连接 Windows host、不认证、不要求或打印密码、不发送输入；随后运行无密 formal checklist，ready 后才认证 Mac client 页面。正式密码来自 `LAN_DUAL_PASSWORD` 或 `--promptPassword`，其中 `--promptPassword` 会先响两声并输出不含密码的前台弹窗提示，再优先打开 macOS 系统隐藏密码弹窗；如果系统弹窗打不开，才尝试原生 AppKit 前台高层级隐藏密码框作为备用。脚本把密码通过环境变量传给 `scripts/windows/test-mac-client-browser.mjs --useEnvPassword --requirePassword`，不放命令参数、不打印、不发 Agent Link Board；默认拒绝空密码和 `demo-password`，不执行 `inject`，支持 `--discoverNoLocalSubnets`、`--preflightOnly`、`--dryRun`、`--boardSummary` 和 `--json`。当已有 Windows host 时，预检/干跑 JSON 会输出 `commands.sendCall`，ready 的 `--boardSummary` 也会提示可先用 `check-mac-client-formal-status --sendCall` 协调 Windows；也可直接运行 `--discover --ensureClient --preflightOnly --sendCall`，让包装器在发现、确保本地页面和只读预检 ready 后代发同一条无密 Agent Link Board call。`--sendCall` 只能与 `--preflightOnly` 同用，未 ready、无 host 或通讯板拒绝时不会认证或弹密码。显式 `--server <url>` 会传给内部 formal checklist、输出的 sendCall 命令和实际发送 call 的子流程。自测 `scripts/mac/test-mac-client-formal-smoke.mjs` 覆盖 help、缺 host、preflight/dryRun 秘密安全、`--ensureClient` 启动本地页面、备用假通讯板传递、ready sendCall 提示、`--sendCall` 安全发送、discover 自动选中、discover ready 后发送 call、discover 失败不弹密码、空密码和 demo 密码拒绝；`test-mac-client-browser.mjs` 新增的环境密码入口默认不改变原本 mock 测试行为。
- Mac client formal status/smoke 的 `runPlan.commands` 和 `--boardSummary` 已补齐反控请求安全演练：`windowsReverseGrantStatus` / `windowsOpenOneTimeReverseGrant` 是 Windows 本机回环命令，`reverseControlRehearsal` 明确 Mac 先点“请求反控”预期 `LAN008`，Windows 本机运行一次性授权或点桌面按钮，Mac 再点“重试反控”预期 accepted/临时授权已使用；自测锁定无密、无 `input_event`、无 `inject`。
- Windows 侧 `scripts/windows/test-mac-client-browser.mjs` 现在也有页面等待进度心跳：连接首帧、认证失败等待、H.264/二进制视频等待、音频首帧/播放等待、`--observeVideoMs` 持续视频观察和 `--expectReconnect` 恢复等待都会默认每 10 秒输出当前页面状态；长视频观察会额外输出已收帧数、剩余时间和当前 FPS，重连恢复会输出连接状态、session 数和画面状态。`--progressIntervalMs <ms>` 可调整频率，`0` 可关闭。

- Windows WGC benchmark/compare 长等待也已补进度心跳：`scripts/windows/benchmark-windows-wgc-settings.mjs` 普通输出会在 helper 构建和每个 profile 子观察期间默认每 10 秒打印 elapsed/expected/timeout left；`scripts/windows/compare-windows-wgc-h264-sources.mjs` 普通输出会在每个 raw-bgra/NV12 source 子 benchmark 期间打印同类进度。两者均支持 `--progressIntervalMs <ms>`，传 `0` 可关闭；`--json` 和 `--boardSummary` 保持干净，不混入进度行。新增 `scripts/windows/test-windows-wgc-progress-output.mjs` 用临时 fake 子脚本覆盖普通输出进度、JSON/boardSummary 干净和不泄密。
- Windows Mac client 视频传输矩阵也已补外层进度心跳和上板摘要：`scripts/windows/test-mac-client-video-transports.mjs` 普通输出会在每个 case/attempt 子页面自检期间默认每 10 秒打印 elapsed、临时 host/client/debug 端口和子进程超时剩余，同时把 `--progressIntervalMs` 透传给 `test-mac-client-browser.mjs`，让内层页面等待也继续输出快照；`--json` 保持纯 JSON 并带 `boardSummary` 字段，`--boardSummary` 输出一行无密摘要，包含通过数、失败 case、重试次数和 no input/inject 安全说明。新增 `scripts/windows/test-mac-client-video-transports-progress.mjs` 用 fake 子自检覆盖外层进度、参数透传、成功/失败 boardSummary 和 JSON 纯净。

## Mac 端状态

- macOS 被控端已有 Swift WebSocket 服务、`/discovery`、认证、会话协商、真实 JPEG 屏幕帧、模拟帧回退、CGEvent 输入注入、文本剪贴板双向同步和文件剪贴板接收。Mac host 文件剪贴板接收已对齐完成完整性加固：必须先有 `clipboard_file_offer`，offer 校验文件数、总大小和清单一致性，chunk 校验 `fileIndex`、`offset`、分块大小、连续 offset 和不越界；完成时要求逐文件 received/expected/disk size 完全一致，重复/重叠/不完整分块不能误完成，并保留空文件兼容。新增 `scripts/mac/test-mac-host-clipboard-file-integrity.mjs` 锁定这些守卫。
- JPEG 调试链路默认真实采集上限已改为 30 FPS，控制端会显示实收 FPS、协商帧率和请求帧率。
- 真 Mac 已用于验证真实 JPEG 首帧、文本剪贴板双向同步、文件剪贴板从 Mac 推送到控制端内存托盘。
- ScreenCaptureKit + VideoToolbox H.264 输出入口已在真 Mac 上编译、启动并通过本机 `--requireH264` 强校验，实际返回 `videoCodec=h264`、`videoEncoding=annexb-base64`、`capturePipeline=screencapturekit-h264`。
- Mac 端新增 `scripts/mac/observe-mac-video.mjs` 视频持续帧观察脚本；真机 H.264 30 秒收到 877 帧，约 29.2fps，最大接收间隔 45ms，全部为 `screencapturekit-h264`；空闲/低变化桌面下 5 分钟 H.264 收到 3168 帧、约 10.6fps，60 秒复测约 10.9fps、最大接收间隔 883ms，JPEG 60 秒对照约 16.4fps；脚本也可统计并断言 `video_frame.activeDisplayId` / `displayName`，并汇总帧 `timestamp` 接收年龄、H.264 `timestampUs` 单调性、媒体间隔和 `durationUs`。当前 43770 H.264/JPEG 短观察均确认帧级显示器为 `main`；时间线短测 H.264 3 秒 89 帧、约 29.2fps、最大接收间隔 39ms，`timestampUs` 单调，媒体间隔平均/最大 `34281/41668us`，`durationUs=33333`。Mac host 最新代码会输出带小数秒的 ISO `timestamp`，临时 43771 build 已验证 discovery/runtime 为毫秒格式且 mock `video_frame` 接收年龄 max 0ms；主 43770 已重启到 `c2db37f` fractional timestamp build，H.264 短测 `frameAge` 约 `0-2ms`，可用 `--maxFrameAgeMs 250` 做低延迟新鲜度强校验。后续高 FPS 强校验需要区分静态桌面和动态画面。
- Mac 系统声音采集第一版已接入 ScreenCaptureKit，真机验证可输出 `pcm-f32le-base64`、48kHz、双声道、20ms 的真实 `audio_frame`；Windows 控制端已可播放该 PCM 帧，并通过页面级自检验证播放计数。
- Mac 端新增 `scripts/mac/stress-mac-host.mjs` 连续连接稳定性脚本，复用 canonical 探针验证 H.264 + PCM；真机 50 次循环已通过，RSS `79376->80656 KB`，FD 保持 `30->30`。脚本现会统计完整 probe、首帧、H.264 确认和首个音频帧耗时的 min/avg/max，并支持 `--maxProbeMs`、`--maxFirstFrameMs`、`--maxH264ConfirmMs`、`--maxAudioFrameMs` 可选阈值；真实 43770 三轮轻量回归首帧约 `157-169ms`、音频约 `237-250ms`。
- Mac 端新增 `scripts/mac/observe-mac-audio.mjs` 音频持续帧观察脚本；真机 30 秒收到 1501 帧，约 50fps，最大接收间隔 24ms，payload 恒定 7680 bytes；5 分钟长稳收到 15001 帧，50.0fps，最大接收间隔 31ms，payload 仍恒定 7680 bytes。脚本现支持显式 `--playTone --requireLevel --minLevel` 做有声电平强校验，默认不播放声音；无人值守下已用 `--toneVolume 0` 验证测试音流程和清理逻辑。
- Mac 端新增 `scripts/mac/observe-mac-media.mjs` 媒体聚合入口：只连接已运行 Mac host，顺序汇总 H.264 视频和系统 PCM 音频观察，支持 `--json` 和一行无密 `--boardSummary`；单路成功单路失败会标为 `Mac media baseline partial`，全部执行链路失败保留 `failed <数量>`，JSON `summary.status=ok|partial|failed` 可供自动化直接判断；可选 `--resourceSample` 只读采样本机 Mac host 进程 CPU/RSS 并写入 JSON 与一行摘要；密码只通过 `LAN_DUAL_PASSWORD` 传给子探针，不放 argv，不启动 host、不发送输入、不执行 `inject`。Windows 审查机 fake-host 自测覆盖成功、partial failure、全失败、跳过音频和不泄密。
- Mac 端新增 `scripts/mac/check-input-keymap.mjs` 输入映射覆盖自检；当前 `KeyboardEvent.code` 115 项、`event.key` 113 项，常用键组、同义 code/key 和 `meta/command`、`alt/option`、`ctrl/control`、`shift` 修饰键 flag fallback 全覆盖。
- Mac 端新增 `scripts/mac/smoke-mac-input-log.mjs` 输入事件安全冒烟脚本；脚本只允许 `/discovery` 显示 `inputMode=log` 时发送事件，真机 16 个鼠标/滚轮/键盘/快捷键事件全部收到 `input_ack`，且 `mode=log`、`injected=false`。
- Mac host 的 H.264 视频流和系统音频流已增加异步启动 generation token，切换显示器或重发显示/音频设置后，旧流迟到的启动成功、帧回调或失败回退不会覆盖新会话；`video_frame` 也会带可选 `activeDisplayId` / `displayName` 诊断字段。
- Mac host 的 `/discovery` 和 `hello_ack` 会带可选 `runtime` 诊断：`processId`、`startedAt`、`uptimeSeconds` 和 `buildId`；`LAN_DUAL_BUILD_ID` 可在启动时指定，方便识别是否连到了旧进程。`lastSeenAt`、`runtime.startedAt`、WebSocket envelope `timestamp`、`video_frame.timestamp` 和回退诊断时间统一使用带小数秒的 ISO-8601 UTC 字符串，便于低延迟观察脚本计算帧接收年龄。`check-mac-displays` 支持 `--requireRuntime`、`--expectBuildId` 和 `--maxRuntimeUptimeSeconds`，可在部署/重启后强制确认 `/discovery` 与 `hello_ack` 来自同一目标进程；Windows 控制端连接前会轻量探测 `/discovery`，并把 runtime 显示在设备列表和诊断条。
- Mac host 直接启动时 `LAN_DUAL_INPUT_MODE` 未设置会默认 `log`，避免无人值守时真实注入输入；只有显式设置 `LAN_DUAL_INPUT_MODE=inject`、启动助手 `--inputMode inject` 或 `--injectInput` 时才启用 CGEvent 真实注入。日常真实注入复跑必须走启动助手并带 `--confirmUserWatching`，裸 `LAN_DUAL_INPUT_MODE=inject` 只保留给人工监督下的底层调试。`scripts/mac/test-mac-host-defaults.mjs` 可用临时端口验证默认 `log` 和显式 `inject` 覆盖。
- Mac 端新增 `scripts/mac/start-mac-host.mjs` 日常安全启动助手，默认 `LAN_DUAL_INPUT_MODE=log`，会打印 Windows 可连接的局域网地址、等待 `/discovery`、并默认运行只读 runtime/display round-trip 校验；`--status` 可只读查看当前 `/discovery`、runtime、权限、能力和 Windows 可连地址，不启动服务也不要求密码；若运行中 build 落后当前 git，会只比较 `apps/mac-host` 运行源码并提示旧 build 是否有 host 行为相关改动，辅助判断是否必须重启；`--status --json` 会输出只含 JSON 的机器可读对象，包含 `online`、`runtime`、`permissions`、`capabilities`、`lanAddresses` 和 `buildDiff`；`--status --checkBoard --json`、普通输出和 `--status --checkBoard --boardSummary` 会只读读取 Agent Link Board `/api/state.currentCall`，提示 active call，DONE call 标为 inactive，摘要不回显 call command；如果只写 `--boardSummary --checkBoard` 而忘了 `--status`，启动助手也会自动走只读 status，不会误启动 Mac host。`--stop` 可安全停止本机 `/discovery` 对应的 Mac host：它不读取密码、不认证 WebSocket，只在目标为 macOS 且提供 `runtime.processId` 时向该 PID 发送 `SIGTERM`，离线时视为已停止，非本机或非 Mac discovery 会拒绝。`--promptPassword --requirePassword` 会先播放两声提示音，并输出一行不含密码的前台弹窗提示，再优先打开 macOS 系统隐藏密码弹窗；如果系统弹窗打不开，才尝试原生 AppKit 前台高层级隐藏密码框作为备用。即使已有 `LAN_DUAL_PASSWORD`，显式 `--promptPassword` 也会要求用户在前台弹窗重新输入，避免用户等不到输入框或悄悄复用旧密码。`--ephemeralPassword --requirePassword` 可生成一次性随机密码且不打印，适合临时恢复 `/discovery`/runtime 诊断通道但不用于认证联调。显式 `--background` 会在 `/discovery` 和 runtime/display 校验通过后退出启动助手，让 Mac host 保持后台运行；后台日志默认写入 `.dev-lab/mac-host/lan-dual-mac-host-<port>.log`，也可用 `--logFile <path>` 指定。后台模式不会改变默认 `inputMode=log`，且 runtime/display 子校验通过环境变量继承密码，不把密码放进 `--password` 命令参数。真实 `--inputMode inject` / `--injectInput` 启动必须额外带 `--confirmUserWatching`，缺少确认会拒绝启动；dry-run、status、stop 和 log 模式不受影响。`scripts/mac/password-prompt.mjs` 是共享提示 helper，`scripts/mac/test-mac-password-prompt.mjs` 用假 `swift`/`osascript` 覆盖两声提示音、不含密码弹窗提示、默认系统隐藏密码弹窗、原生前台高层级备用弹窗、取消不回退和全部失败路径；fake `swift`/`osascript` 会同时生成 Unix shim、`.mjs` 执行体和 Windows `.cmd` 包装器，并处理 PATH/PATHEXT，Windows 审查机上会优先直连 fake `.mjs` 执行体以避开 `cmd.exe` 多行 AppleScript 参数转义，失败时会打印 status/signal/error 便于定位，不打开真实系统弹窗。`scripts/mac/test-mac-host-start-helper.mjs` 已覆盖缺密码拒绝、`demo-password` 拒绝、自动化禁用弹窗后的非交互密码提示拒绝、`--promptPassword` 不复用环境密码、环境密码干跑、一次性密码干跑、inject 缺少 `--confirmUserWatching` 拒绝、`--status` 在线/离线检查和旧 build 诊断、`--status --json` 在线/离线机器可读输出、`--status --checkBoard` active/DONE call 摘要和 `--boardSummary --checkBoard` 防误启动路径、`--stop` 离线/非本机/非 Mac 目标拒绝、临时端口实启关闭、后台启动后仍可查询 `/discovery`、后台 host 可由 `--stop --json` 安全停止、后台校验失败不脱离，以及 runtime/display 校验不经 argv 传密码；真实 Swift Mac host 启动段在非 macOS 上明确 `[SKIP]`。Windows 审查机已复跑 `test-mac-password-prompt` 和 `test-mac-host-start-helper` 通过；在当前 Codex 沙盒里需要允许子进程启动，否则 fake shim 会被 `EPERM` 拦截。
- Mac 端新增 `scripts/mac/check-mac-resume-status.mjs` 恢复开工轻量总览：只读汇总当前 git 分支/干净状态、可选联络板快照、Mac host `/discovery` 在线状态、runtime、权限、能力、LAN 地址、显示器和旧 build 的 Mac host runtime 源码差异，并输出下一步建议；它不会启动服务、不会认证 WebSocket、不会要求或打印密码、不会发送输入事件。`--checkBoard` 现在会直接读取 Agent Link Board `/api/state` 并把 `currentCall` 写入 JSON `board.currentCall` / `board.activeCall`；普通输出和 `--boardSummary` 会显示 `call=active/done/none`，DONE/完成呼叫不会当作待办，摘要不回显 call command。可加 `--json` 供联络板/自动化消费，`--boardSummary` 可输出一段适合直接发到 Agent Link Board 的秘密安全摘要，包含 repo/host/权限/H.264/音频/buildDiff/正式验收下一步但不含密码或 token；`--requireOnline`、`--requireClean` 或 `--requireNoRuntimeChanges` 可把对应问题升级为失败；自测脚本 `scripts/mac/test-mac-resume-status.mjs` 覆盖帮助、离线 JSON、离线/在线 board summary、requireOnline 失败、online JSON 字段形状、active currentCall 和 DONE currentCall。
- Mac resume status 的普通建议和 `--boardSummary` 已明确写出真实注入启动必须 `--confirmUserWatching`，避免联络板摘要只保留“用户确认”这种软提示；该脚本仍只读，不启动 host、不认证、不要求密码、不发送输入。
- Mac 端新增 `scripts/mac/check-mac-formal-e2e-status.mjs` 正式端到端验收清单：复用 resume status，只读检查 repo、联络板、Mac host、LAN 地址、`inputMode=log`、屏幕录制/辅助功能/输入监控、H.264、系统 PCM、剪贴板、显示器和 build 差异，并输出 `readyToCall`、checklist、`callText` 和可发联络板的 `--boardSummary`；显式加 `--sendCall` 时，只有 `readyToCall=true` 才会向 Agent Link Board 发送一条无密 formal E2E 呼叫，未 ready 会拒绝发送并失败，并在 JSON 错误里保留 blockers、下一步和变动 runtime 文件；发送和 `--clearStaleCall` 守卫会直接读取 `/api/state` 的 `currentCall`，DONE/COMPLETED/CANCELLED/RESOLVED/CLOSED 等完成态只记录不阻塞，active call 仍默认拒绝覆盖，只有人工确认协调后显式加 `--forceCall` 才允许替换；`--clearStaleCall` 只会在 checklist 不 ready 且当前 call 精确匹配 Mac Codex -> Windows Codex 的 formal Mac host E2E 时清理旧呼叫，不会碰其他测试 call。它不启动服务、不认证、不要求或打印密码、不发送输入事件；`inject` 会明确标为跳过，只有用户另行明确确认后才可验收。自测脚本 `scripts/mac/test-mac-formal-e2e-status.mjs` 覆盖帮助、离线 blocker、离线 `--sendCall` 拒绝、旧 runtime 源码变化时 `--sendCall` 拒绝且不触碰通讯板、过期 formal call 只清匹配项、非 Mac formal call 不清、ready formal call 不清、假 Mac host + 假通讯板 ready 发送、已有 currentCall 默认拒绝、DONE call 不阻塞、`--forceCall` 显式覆盖、在线 checklist shape、board summary 和 secret-like 文本不泄露。2026-06-16 Mac host `192.168.31.122:43770` 已在 runtime build `c5e5009` 完成 Windows formal Mac E2E，通过 H.264/系统 PCM/剪贴板/input-log/Windows 控制端页面验收；密码未上联络板，`inject` 未执行，后续如要验收真实输入注入仍需用户另行明确确认。
- Mac 端新增 `scripts/mac/check-mac-formal-local-smoke.mjs` 正式端到端前本机短验收聚合脚本：复用 `observe-mac-video`、`observe-mac-audio` 和 `smoke-mac-input-log`，串联 H.264、系统 PCM 和安全 `inputMode=log` 输入 ack。默认要求正式密码来自 `LAN_DUAL_PASSWORD` 或 `--promptPassword`，拒绝空密码和 `demo-password`；`--promptPassword` 会先响两声并输出不含密码的前台弹窗提示，再优先打开 macOS 系统隐藏密码弹窗，系统弹窗失败时才尝试原生 AppKit 前台高层级隐藏密码框，密码通过环境变量传给子探针，不放进命令参数；脚本不启动 Mac host、不切 `inject`、不打印密码，支持 `--json` 输出 `boardSummary`。自测 `scripts/mac/test-mac-formal-local-smoke.mjs` 用临时假 Mac host 覆盖密码安全、JSON 失败、demo 密码放行仅限显式假服务测试和三探针聚合成功。
- Mac 端新增 `scripts/mac/check-mac-host-readiness.mjs` 一键体检入口：默认低风险检查 Node/Swift、Mac host build、Mac host 直接启动输入默认值、启动助手语法/干跑、键盘映射覆盖和当前 `/discovery` 状态，并汇总 `/discovery.permissions` 的屏幕录制、辅助功能和输入监控状态；显式 `--checkBoard` 会只读读取 Agent Link Board `/api/state.currentCall`，JSON、普通输出和 `--boardSummary` 会提示 active call，DONE call 标为 inactive，摘要不回显 call command。Mac host 最新源码用 macOS `IOHIDCheckAccess` 只读读取真实 Input Monitoring 状态，不再硬编码为 `false`。readiness 会默认对比当前 git short hash 与运行中 `/discovery.runtime.buildId`，不一致时只给 warning，并在能解析旧 build commit 时列出之后变动的 Mac host runtime 源码文件；可加 `--requireCurrentBuildId` 在部署验收时强制失败。也可加 `--requireControlPermissions` 强制要求屏幕录制和辅助功能已开启，`--requireInputMonitoring` 单独强制要求输入监控已开启，或显式加 `--probeVideo --probeAudio --probeMedia --probeInputLog --probeStartHelper --probeClipboardSecurity` 做 H.264、PCM、媒体聚合、log 输入、启动助手临时端口和文件剪贴板接收完整性深度验收；`--probeMedia --boardSummary` 会消费媒体聚合 `summary.status=ok|partial|failed` 并显示 `media=ok|partial|failed`，partial/failed 会保留 passed/failed 计数；`--probeClipboardSecurity` 只串联本地 `test-mac-host-clipboard-file-integrity`，不会启动 host、写系统剪贴板、要求密码或执行输入。正式密码 host 可加 `--promptPassword`，它会先响两声并输出不含密码的前台弹窗提示，再优先打开 macOS 系统隐藏密码弹窗，系统弹窗失败时才尝试原生 AppKit 前台高层级隐藏密码框；readiness 会通过 `LAN_DUAL_PASSWORD` 环境变量传给子探针，避免把密码放进 argv。`--maxVideoFrameAgeMs <ms>` 可让视频探针强制要求 `video_frame.timestamp` 新鲜度，`--json` 可输出机器可读摘要且不包含密码。常用组合可直接用 `--profile deploy` 要求当前 build、控制权限、输入监控、H.264、PCM 和 input-log 冒烟；`--profile deep` 在此基础上再跑启动助手临时端口自测和文件剪贴板安全回归。
- Mac host H.264 流式启动新增 5 秒 watchdog；若启动阶段迟迟未建立 `videoStream`，会发送带 `streamFallbackReason` 的 `display_settings_ack` 并回退 `background-jpeg`。临时 `43771` 单屏显式 H.264 display 自检已复现：5 秒超时后拿到 JPEG 首帧，随后迟到启动的旧 H.264 流被 token 停止。
- Mac 端新增 `scripts/mac/check-mac-displays.mjs` 显示器枚举与 `displayId` 切换回执自检；脚本默认要求 `video_frame.activeDisplayId` 存在且匹配，避免旧 host 未重启到最新二进制时误通过；调试旧 host 可显式加 `--allowMissingFrameDisplayDiagnostic`。当前单屏真机 `43770` 最新 host 验证通过：`displays=main*:1920x1080`，`session_answer`、`display_settings_ack`、首帧和切换后帧均指向 `main`，默认 MJPEG 和显式 H.264 路径均通过。真实外接双屏切换仍待实物验收。

## 共享协议状态

- `hello/auth/session`、`video_frame`、`audio_frame`、`input_event`、`input_ack`、`display_settings`、`clipboard_text`、`clipboard_file_*` 已有基础定义和多端实现；`video_frame.activeDisplayId` / `displayName` 为向后兼容的可选诊断字段。
- 修改共享协议前，必须先更新 `docs/03-architecture-and-protocol.md`，再同步修改两端实现。
- 协议变更必须写入 `docs/HANDOFF_LOG.md`，并在 `docs/NEXT_ACTIONS.md` 留下对接项。

## 每次开工检查

1. 拉取最新代码。
2. 阅读本文件。
3. 阅读 `docs/NEXT_ACTIONS.md`。
4. 阅读 `docs/ACTIVE_LOCKS.md`，确认自己要改的文件没有被另一端占用。
5. 阅读 `docs/TEST_COORDINATION.md`，确认当前是否有人正在呼叫另一端测试。
6. 如需改共享协议，先更新协议文档，再动代码。
7. 完成后更新 `docs/HANDOFF_LOG.md` 和 `docs/04-task-board.md`。
