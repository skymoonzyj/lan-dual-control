# 下一步行动

最后更新：2026-06-21

用途：让两台机器上的 Codex 都知道现在最值得做什么。

- 当前最高优先级：真实复测 Windows 已修的 `W2-BACKGROUND-VISIBILITY-VIDEO-FREEZE`。Windows 控制端现在会在切出/后台后恢复可见时，针对 H.264 等关键帧、`queue-overflow-wait-keyframe` 或队列超阈值主动清本机旧队列，并保持 `preferredVideoCodec=h264` / `preferredVideoEncoding=annexb` 请求下一关键帧；诊断会显示 `原因 visibility-return-h264-recovery` 和 `可见恢复 <n> 次`。下一步由用户真实切出/切回控制端窗口确认画面是否继续流动；如果仍冻结，先复制诊断，看 `recv/key/sps/pps/idr` 是否齐、`reason` 是否仍回到 queue overflow、canvas 是否还在绘制，再决定是否需要 Mac 补证据。Mac 侧继续保持 host 在线；无密码/auth/input/inject。
- Windows 开工第一屏新增 W2 可见性复测提示：恢复总览会直接显示 `W2VisibilityRetest=status=pending-user-retest action=switch-away-and-back evidence=visibility-return-h264-recovery next=Run-WinClientRetest-And-Post.cmd safety=no-password-on-board,no-auth,no-input-inject`。看到这条时，下一步就是运行 `Run-WinClientRetest-And-Post.cmd`，在当前终端隐藏输入 Mac 临时密码，连接后真实切出/切回控制端窗口；不要把密码发通讯板，也不要自动发 input/inject。

- 今日协作优先级：W2 后台冻结最高；W3 音频低延迟/低丢包第二，验收看连续体验接收=播放、丢包接近 0、重同步/补缓冲下降；W1 只保持入口稳定，不再扩展 helper；M2 真实输入安全流程排在 W2/W3 稳定后；M1 远程独占声音只做方案/只读探测，未获用户明确同意不得改系统声音输出；C3 双方真实体验收口需要 Windows/Mac 分别勾选。当前主线仍只做 Windows 控 Mac，不做 Mac 控 Windows/反控/WindowsHost。

- 当前阶段背景：W2/W3 真实复测已 PASS，旧 `Run real WinClientRetest for W2/W3` 呼叫已清理，说明首屏 H.264 解码 gate 已过。不要再重复旧 H.264 首屏 blocker 诊断；在修完后台冻结后继续真实体验收口，按顺序确认画面稳定性/延迟、音频连续性、窗口/全屏/原画、剪贴板/文件、输入安全日志到真实控制流程。涉及用户授权、密码、真实输入或声音输出变更时，必须先播放提示音，再说明目标、用户动作、安全边界和预计耗时；密码只在本机隐藏提示输入，不上通讯板，不发真实 input/inject。

- W2/W3 后续复测口径：如果体验收口中再次发现黑屏、卡顿或声音断续，再重新运行对应真实复测/诊断；只有 `W2W3Retest h264=` 显示 `decoded=0`、`canvas=false image=false`、`needsKeyframe=yes` 或 `h264Errors>0` 时，才回到 H.264/WebCodecs 诊断。当前已有 PASS 证据为 `canvas=true 1920x1080`、`decoded=已绘制 18`、`h264Errors=0`、实收约 `89.8 FPS`、音频 `14/14 drop=0`。

- Windows 开工第一屏现在可直接看预检状态：运行 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --boardSummary` 或 PowerShell 包装入口；如果同一行出现 `WinClientRetestPreflight=ready target=<Mac LAN>:43770 build=<build> diagnostics=passed next=Run-WinClientRetest-And-Post.cmd`，说明无密预检已通过，下一步就是用户在 Windows 当前终端运行正式 `Run-WinClientRetest-And-Post.cmd` 并隐藏输入 Mac 临时密码。该状态只读消费通讯板，不请求密码、不认证、不发 input/inject。


- W2/W3 真实复测前先跑无密预检：如果不确定 Mac 目标、Windows client 本地诊断或“密码输到哪里”，先运行 `Run-WinClientRetest-And-Post.cmd -PreflightOnly`。看到 `WinClientRetestPreflight=ready` 后，再运行不带参数的 `Run-WinClientRetest-And-Post.cmd`；正式复测出现“当前终端输入 Mac 临时密码（输入不显示，回车继续）:”时，只在这个黑色终端输入 Mac 当前临时密码。预检不请求密码、不认证、不发板、不发 input/inject。

- W2-H264 复测摘要 surface 最新口径：下一次真实 `Run-WinClientRetest-And-Post.cmd` 上板后，先看 `W2W3Retest h264=` 里的 `decoded=<n> canvas=<true|false> image=<true|false> needsKeyframe=<yes>`，再看 `W2H264BoardDiagnosis surface=canvas:<...> image:<...>`。`recv/key/sps/pps/idr` 正常但 `canvas=false image=false` 时继续查 Windows WebCodecs decoded surface / canvas 绘制；`canvas=true` 则进入人工画面/FPS/声音观感确认。不要再让 Mac 端重复补关键帧证据，除非 `recv/key/sps/pps/idr` 缺失。
- W2-H264 当前下一步：Windows 端已修复“收到 SPS/PPS/IDR 关键帧后仍 `needsKeyframe=yes`”的本地解码门禁；两端拉最新后，用户在场时再运行 `Run-WinClientRetest-And-Post.cmd` 做一次真实 Windows 控 Mac 复测。预期新的 `W2W3Retest h264=` 不应在收到 `key/sps/pps/idr` 后继续长期卡 `needsKeyframe=yes`；若仍黑屏，优先看 `h264Errors`、`decoded`、canvas/image surface 和 WebCodecs 错误，而不是继续补 Mac 侧关键帧证据或扩展 helper。密码只在本机隐藏终端输入，不发通讯板，不发 input/inject。
- W2/W3 当前最短路径：优先运行 `Run-WinClientRetest-And-Post.cmd`。它会先执行真实 Windows 控 Mac 复测，在本机黑色终端隐藏输入 Mac 临时密码；复测成功且输出 `W2W3Retest=...` 后，会自动做脱敏检查、发 `W2W3Retest=`，并追加只读 `W2H264BoardDiagnosis=`。如果只想先看结果不发板，继续用 `Run-WinClientRetest.cmd`；如果已经有一行或整段复测输出，可用 `node scripts/windows/post-w2w3-retest-board.mjs --send --text "<粘贴 W2W3Retest=...>"`，或 `type retest.txt | node scripts/windows/post-w2w3-retest-board.mjs --stdin --send`。不要把密码、token、系统账号或原始输入事件放进通讯板。

- Mac readiness currentCall 最新口径：`check-mac-host-readiness --checkBoard --boardSummary` 仍会显示 active call，但只有 `need` / `owner` / `from` 表明需要 Mac 处理时才把它列为 `agent-link-board-currentcall` warning。当前 W2/W3 这种 `need/owner=Windows Codex` 的复测 call 对 Mac 是信息，不应阻断或提示重启/补证据；如果后续 call 改为 `need=Mac Codex`、Windows 呼叫 Mac，或归属不明，Mac readiness 仍会保守提示先协调。
- Mac readiness/formal 旧 build 最新口径：如果摘要显示 `runtimeBuild=<old> stale metadata only, hostRuntimeChanges=0`，表示运行中 Mac host 的 runtime 源码行为没有落后，只是 build id 元数据旧；不要因此要求重启。真正需要重启的情况是 `hostRuntimeChanges>0` / `restart recommended`，或显式部署强校验 `--requireCurrentBuildId`。当前 W2/W3 真复测优先等 Windows 跑 `Run-WinClientRetest-And-Post.cmd` 并上板 `W2W3Retest=`；Mac 端不要因为 metadata-only 自己重启 host。
- W2/W3 真实复测最短入口：Windows 用户在仓库根目录双击或运行 `Run-WinClientRetest.cmd`。看到中文隐藏密码提示后，在同一个黑色终端输入 Mac 当前临时密码并回车；输入不显示是正常的。它默认发现 Mac host、要求 H.264、输出脱敏 `W2W3Retest=video=... audio=... h264=...`，随后把这一行发通讯板，再跑 `node scripts/windows/diagnose-w2-h264-board.mjs --server http://192.168.31.68:17888 --boardSummary` 做对照诊断。需要指定目标时可追加 `-DiscoverNoLocalSubnets -HostName <Mac LAN IP> -Port 43770`。不要把密码发通讯板，不发 input/inject。

- W2-H264 Mac 证据合并口径：`diagnose-w2-h264-board` 会合并多条 Mac 侧摘要；如果最新 Mac Codex 状态只重复 `h264Key/sps/pps/idr/keyParam`，仍会从较早完整 `MacHostMedia=` 里保留 `h264Frames/h264Delta/keyGap/keyTail/firstKeyNal/lastKeyNal`。看到 `macStream=frames:<n>` 时可继续等真实 `W2W3Retest=`；只有 `waiting-for-mac-nal-evidence` 或 `macStream=na` 且板上没有完整媒体基线时，才让 Mac 端重新跑 `MacHostMedia`。

- W2-H264 诊断对照最新口径：`diagnose-w2-h264-board --boardSummary` 除了 Windows `recv/key/sps/pps/idr/lastNal/decoded` 和 Mac `h264Key/firstKeyNal/lastKeyNal/lastNal`，还会输出 `macStream=frames:<n> delta:<n> keyGapMax:<frames>/<ms> keyGapLast:<frames>/<ms> keyTail:<frames>/<ms> firstKeyParam:<yes/no> lastKeyParam:<yes/no>`。真连后优先把这一行和 `W2W3Retest h264=` 一起看：Mac `frames/delta/keyGap` 正常但 Windows `recv` 少，查传输/接收；Windows 已收到 SPS/PPS/IDR 但 `decoded=0`，查 WebCodecs configure/decode/队列；两边都正常再进入人工画面/FPS/声音观感确认。
- W2-H264 诊断证据口径：`diagnose-w2-h264-board` 只承认真实 `W2W3Retest=` 摘要里的 `h264=` 字段；通讯板里的“准备推送/已推送/说明文字”即使提到 `W2W3Retest h264=` 也不会算作复测。看到 `reason=waiting-for-w2w3-retest` 时，不要继续解读旧说明文字，直接让 Windows 跑 `WinClientRetest=` 并把新 `W2W3Retest=` 上板。
- W2-H264 复测后先跑通讯板对照诊断：用户或 Windows 端把 `W2W3Retest=` 上板后，运行 `node scripts/windows/diagnose-w2-h264-board.mjs --server http://192.168.31.68:17888 --boardSummary`。如果输出 `reason=windows-decode-path`，说明 Mac 侧有 key/NAL 证据且 Windows 已收到 SPS/PPS/IDR 但 `decoded=0`，下一步查 Windows WebCodecs configure/decode/flush/队列；如果是 `waiting-for-w2w3-retest`，先重新跑 `WinClientRetest=`；如果是 `windows-receive-missing-keyframe` 或 `windows-receive-missing-video`，再回查传输窗口、WebSocket video payload 或 Mac host 重启流。该脚本只读 `/api/state`，不请求密码、不认证、不发 input/inject，也不回显通讯板原文。
- W2 真连复测密码提示最新口径：formal runner、browser runner 和底层 `probe-mac-host --promptPassword` 都会先用中文说明“在当前终端窗口输入 Mac 端当前临时密码，输入不显示，按 Enter 继续，不是卡住，不要输到网页或通讯板”，等待标签统一为 `当前终端输入 Mac 临时密码（输入不显示，回车继续）:`。如果用户看到这个提示，就直接在同一个黑色终端里输入；如果是网页登录，则密码填页面左侧“连接密码”框。不要把密码发通讯板，不发 input/inject。

## 2026-06-20 现场校正
- W2-H264 接收 NAL 证据最新口径：`test-windows-client-browser --boardSummary` 的 `W2W3Retest=` 中，短 `h264=` 字段现在会追加 `recv=<n> key=<n> sps=<n> pps=<n> idr=<n> lastNal=<types>`。下一次用户真连后，把它和 Mac `MacHostMedia` / `observe-mac-media` 的 `firstKeyNal`、`firstNal` 对照：Mac 有 `firstKeyNal=7/8/5` 且 Windows 有 `sps/pps/idr` 但 `decoded=0`，优先查 Windows WebCodecs configure/decode/flush/队列；Mac 有但 Windows 没有，才回到传输窗口、重启流或 payload 解析路径。不发密码，不发 input/inject。
- W2-H264 真连复测摘要最新口径：`test-windows-client-browser --boardSummary` 的 `W2W3Retest=` 现在除了 `video=` / `audio=`，还会带短 `h264=` 字段，例如 `status=waiting-keyframe decoded=0 skippedDelta=68 needsKeyframe=yes queue=9 queueMs=900 staleDrops=68 reason=queue-overflow-wait-keyframe`。下一次用户真连后优先看 `surface=` 和 `h264=`：`decoded>0`/canvas 有尺寸说明已出画面；`needsKeyframe=yes` 且 `skippedDelta` 增长说明还在等关键帧；`queueMs`/`staleDrops`/`reason` 高说明 Windows 本机队列或恢复循环仍是重点。不发密码，不发 input/inject。
- W2-H264-KEYFRAME-BLOCKER 最新口径：Windows 控制端已给 H.264 首个 decoded surface 前的本机解码队列增加暖机宽限，避免 1080p/60Hz 初始 WebCodecs 队列短暂超过 8 帧就关 decoder、随后长期等待关键帧。已经画出过一帧后，原 `queue-overflow-wait-keyframe` 低延迟治理仍保留。下一次真实复测继续用 `WinClientRetest=`，重点看 `W2W3Retest=video=` 是否出现 surface/decoded frame，若仍无画面再让 Mac 端同步确认周期 IDR/SPS/PPS 和 Windows 端 WebCodecs 错误。密码只在本机隐藏提示输入，不发通讯板，不发 input/inject。
- W2/W3 真实复测入口最新口径：Windows 开工第一屏 `check-windows-resume-status --checkBoard --boardSummary` 会直接给出 `WinClientRetest=` / `WinClientRetestPs=`。需要真实 60Hz/H.264 复测时，优先复制这条命令；它会优先使用已上板的 `MAC_READY_FOR_REAL_TEST` 或 `MacManualUx target=` 作为 Mac LAN 目标，并在本机隐藏提示输入 Mac 临时密码，要求 H.264，输出一行 `W2W3Retest=video=... audio=...`，但不会把密码放到命令参数或通讯板，也不会开启真实 input/inject。
- W2/W3 真实复测输出最新口径：Windows 端跑 `test-windows-client-browser --boardSummary` 后，摘要会新增 `W2W3Retest=`，直接汇总“现场视频 / 现场声音”。真实 60Hz/H.264 复测时优先把这一行发通讯板；看 `video=` 里的实收 FPS、请求/协商 Hz、平均/最大间隔、卡顿和本机队列，再看 `audio=` 里的队列、接收/播放/丢、补缓冲/稳缓冲/重同步和原因。
- M5 Mac H.264 keyframe 证据最新口径：Mac `MacHostMedia=` / `check-mac-host-readiness --probeMedia --boardSummary` 现在会强校验 H.264 Annex B 至少出现一个带 SPS/PPS/IDR 的关键帧，并在 `media=ok` 后输出 `h264Key=<n> sps=<n> pps=<n> idr=<n> keyParam=<n>`。本轮真机 60Hz/20Mbps/5s 证据为 `h264Key=3 sps=3 pps=3 idr=3 keyParam=3`，说明 Mac 当前发送侧能给 Windows 带参数集的 IDR；如果 Windows 仍“等待关键帧”，下一步优先查 Windows 收到的 NAL 类型、WebCodecs configure/decode、flush 后第一帧和本地队列逻辑，而不是继续猜 Mac 没发 SPS/PPS/IDR。
- M3 Mac readiness 60Hz 基线复跑口径：需要重新给 Windows 端提供 Mac 侧 60Hz/H.264/PCM 基线时，用户在场后 Mac 端可跑 `node scripts/mac/check-mac-host-readiness.mjs --promptPassword --probeMedia --probeMediaResourceSample --probeMediaFps 60 --probeMediaBandwidthKbps 20000 --probeMediaVideoDurationMs 5000 --probeMediaAudioDurationMs 5000 --probeMediaVideoMinFps 50 --boardSummary`。输出会带 `mediaTarget=1280x720@60Hz/20000kbps/5000ms audio=5000ms minVideoFps=50`，方便和 Windows `W2W3Retest=` 对照；密码只在本地提示框输入，不发通讯板，不发 input/inject。
- W3 音频队列最新口径：Mac 侧 60Hz/PCM 基线稳定后，Windows 控制端高水位处理已从全量 flush 改成只修剪尚未播放的未来音源，保留当前正在播放的片段；现场诊断原因是 `queue-overflow-trim-future`。如果用户仍听到断续，下一次复测重点看“现场声音”的接收/播放/丢、平均/最大音频间隔、`重同步`、`补缓冲/稳缓冲` 和这个新原因，判断是 Windows 主线程/解码导致排队，还是音频本身到达抖动。
- W2 视频关键帧最新口径：Windows 控制端 H.264 背压后等待关键帧时，不再因为关键帧等待直接请求 JPEG。60Hz 会至少等 180 个 delta；真正超时后仍发 `preferredVideoCodec=h264` / `preferredVideoEncoding=annexb` 重启 H.264 流，诊断看 `keyframe-wait-h264-recovery`。现场如果仍看到 `background-jpeg` 或 `解码 JPEG 回退`，优先判断为 Mac host 自身降级、浏览器连续解码失败、或用户显式/其它 fallback 路径，不要再把 30Hz/60Hz 关键帧等待本身当作应切 JPEG。
- Agent Link presence endpoint fallback 最新口径：如果 Windows 控制端 Mac 提醒/值守文本看到 `presence endpoint/API/接口 404` 或不可用，会显示“presence 接口未启用”；同段带 `state.userPresence` 为准时，会显示证据“仍以 state.userPresence 为准”。现场看到它时，按“当前联络板服务可能还没启用新版 presence endpoint / 需等服务重启或升级”处理，但用户在场权威仍先看 `/api/state.userPresence`；不要因为 404 请求密码、认证、真实 input/inject 或系统授权。
- MacScriptHelpStatus 最新口径：Mac `test-mac-script-help --boardSummary` 现在会输出稳定 `MacScriptHelpStatus=ok|failed`；Windows 控制端已把 `ok` 显示为“Mac 脚本 help 自检已通过”证据，把 `failed` 显示为“Mac 脚本 help 自检失败”风险。现场看到 failed 时，先让 Mac 端修复/复跑 help 覆盖；不要因此请求密码、认证、真实 input/inject 或系统授权。
- Windows 控制端 UserPresence 最新口径：如果 Mac 提醒/值守文本带 `UserPresence=away`、`UserPresenceAction=no-auth-only` 或 `BLOCKED_BY_USER_AWAY`，页面会直接显示“用户不在 / 只做无授权任务”。现场看到这条时，只允许继续代码、文档、只读检查和无密通讯板同步；不要请求远控密码、系统授权、真实 input/inject、声音输出变更或人工观感确认，直到用户明确在场并同步结构化 presence。
- Windows 重连上限失败最新口径：自动重连 3 次仍未恢复后，页面状态会明确提示“点连接重新尝试 / 复制诊断给两端”，host 诊断显示“自动重连已停止”，导出诊断新增 `重连建议`。现场看到这条时，不要继续等倒计时；先检查 Mac host/局域网，再手动点连接或把诊断发到通讯板。
- Windows 重连按钮倒计时最新口径：断线自动重连等待期间，普通连接区和全屏浮层的“立即重连”按钮都会显示剩余秒数，悬停可看第几次重连、原因和目标地址；按钮仍可手动立即重连。现场如果只想知道多久后自动恢复，不必再只盯状态栏或复制诊断。
- Windows 长状态悬停完整可读最新口径：FPS、声音、连接、输入、剪贴板等长状态仍保持单行省略，避免挤压远控画面；如果用户需要看完整状态，鼠标悬停对应文本即可看到完整 `title`。后续继续新增诊断字段时，优先复用这条 title 同步，不要为了显示全文把状态栏改回多行。

- W2 视频本地队列状态实时可见化最新口径：Windows 控制端页面 FPS 状态会直接显示 `本机队列`、`本地过期丢帧`、`回退恢复`、`恢复暂停` 和 `暂停剩余`。现场如果只看到 `最大间隔/卡顿`，优先查 Mac 采集或网络供流；如果同时看到本机队列、过期丢帧或恢复暂停，优先查 Windows WebCodecs/H.264 本地队列、关键帧等待和 fallback/recovery 循环。

- W2 视频状态实时可见化最新口径：Windows 控制端页面 FPS 状态现在直接显示最近 `最大间隔 <ms> ms`，有明显长间隔时显示 `卡顿 <n>`。现场用户不用复制诊断也能判断画面是否不像 60Hz；若状态行卡顿增长但 `本机队列` 不高，优先查 Mac 采集/网络供流；若本机队列、过期丢帧或恢复暂停也增长，再查 Windows WebCodecs/H.264 本地队列和回退恢复。

- W2 视频断流最新口径：Windows 控制端已经收到过视频帧后，若超过 2.5 秒没有新帧，会在远程画面状态行和复制/导出诊断显示 `视频断流` / `最后收到 <秒>s 前`。现场如果出现这条，先按“画面曾经到达、后来供流/网络/解码链路中断或冻结”处理；优先查 Mac host 是否继续发送 video frame、WebSocket 是否继续到达、H.264 是否卡在关键帧/回退恢复循环，再看 `本机队列`、`本地过期丢帧`、`恢复暂停` 等 W2 字段。
- W2 视频首帧等待最新口径：Windows 控制端连接进入 `streaming` 后，若 3 秒以上没有收到第一帧视频，会在远程画面状态行和复制/导出诊断显示 `等待视频首帧` / `已等待 <秒>s`。现场如果出现这条，优先查 Mac host 是否开始发送 video frame、H.264 首个关键帧是否到达、WebSocket 视频消息是否被协商或发送；如果已经有帧再变卡，则继续看 `实收 FPS`、`卡顿`、`本机队列`、`恢复暂停` 等 W2 字段。
- Mac 手工体验第一屏刷新口径：`MacHeartbeat=`、`MacResumeStatus=` 和 `MacUnattendedStatus=` 现在都会同时给出 `MacManualUxStatus=` 只读检查和 `MacManualUxSendStatus=` 上板刷新命令。若 Agent Link Board 的 `Mac Manual UX` 摘要旧了，优先复制 `MacManualUxSendStatus=` 刷新；该命令只发状态，不发消息、不发 call、不请求密码、不认证、不发送 input_event/inject。真实手工体验仍要用户在场并先说明目标、安全边界和预计时间。
- W3 音频首帧等待最新口径：Windows 控制端在连接后声音开启、但 3 秒以上没有收到第一个音频帧时，会在页面声音状态行和复制/导出诊断显示 `等待音频首帧` / `已等待 <秒>s`。现场如果出现这条，优先查 Mac host 是否真的发送 audio_frame、音频采集模式是否启用、WebSocket 音频消息是否被协商或发送；如果曾收到过音频后才停，则看上一条 `音频断流`。
- W3 音频断流最新口径：Windows 控制端页面声音状态行和复制/导出诊断现在会在声音开启且已收过音频帧后，超过 2.5 秒没收到新音频帧时显示 `音频断流` / `最后收到 <秒>s 前`。现场如果这条出现，优先查 Mac 端系统音频采集是否继续产帧、WebSocket 音频消息是否继续到达、局域网是否瞬断；如果没有断流但 `补缓冲/稳缓冲/重同步` 增长，再查 Windows 本地 WebAudio 播放队列。
- W3 音频缓冲状态实时可见化：Windows 控制端页面声音状态行现在会直接显示 `补缓冲 <n>`、`稳缓冲 <n>` 和 `重同步 <n>`。如果同一行同时有 `最大间隔/音频卡顿`，优先怀疑 Mac 采集或网络供流抖动；如果到达间隔正常但补缓冲/重同步增长，优先查 Windows 本地 WebAudio 队列和播放调度。
- W3 音频状态实时可见化：Windows 控制端页面的“声音：接收中 ...”状态行现在会直接显示最近 `最大间隔 <ms> ms` 和 `音频卡顿 <n>`；用户不用先复制诊断也能判断远端声音是否供流抖动。若状态行显示音频卡顿增长，优先查 Mac 采集/网络供流；如果状态行正常但复制诊断里 `补缓冲` / `重同步` 增长，再查 Windows 本地播放队列。
- W3 音频到达间隔最新口径：Windows 控制端“现场声音”诊断现在会统计最近 2 秒音频帧到达间隔，输出 `平均间隔 <ms> ms`、`最大间隔 <ms> ms`，并把 >=120ms 的长间隔记为 `音频卡顿 <n>` / `最大音频卡顿 <ms> ms`。用户反馈声音断续时，先同时看这四个字段和 `补缓冲` / `稳缓冲` / `重同步`：到达间隔卡顿高，优先查 Mac 采集/网络供流；到达间隔正常但补缓冲或重同步增长，再查 Windows 本地 WebAudio 队列策略。
- M2 真实输入第一屏刷新口径：`MacHeartbeat=`、`MacResumeStatus=` 和 `MacUnattendedStatus=` 现在都会同时给出 `MacInputSafetyStatus=` 只读检查和 `MacInputSafetySendStatus=` 上板刷新命令。若 Agent Link Board 的 `Mac Input Safety` 摘要旧了，优先复制 `MacInputSafetySendStatus=` 刷新；该命令只发布当前门禁状态，不启动 host、不请求密码、不认证、不发送 input_event/inject。看到 `blocked/no-auth-only` 或 `BLOCKED_BY_USER_AWAY` 仍表示只能做无授权任务。
- M1 远端声音第一屏刷新口径：`MacHeartbeat=`、`MacResumeStatus=` 和 `MacUnattendedStatus=` 现在都会同时给出 `MacRemoteAudioStatus=` 只读检查和 `MacRemoteAudioSendStatus=` 上板刷新命令。若 Agent Link Board 的 `Mac Remote Audio` 摘要旧了，优先复制 `MacRemoteAudioSendStatus=` 刷新；该命令只发布当前门禁状态，不会静音 Mac、不切输出设备、不请求密码、不发 input/inject，看到 `blocked-local-output` 仍表示 Mac 本机可能出声。
- M1 远端声音状态门禁最新口径：`MacRemoteAudioStatus=` 现在自身也带 `Consent=explicit-before-change` 和 `RestorePath=required-before-apply`。看到 `local-playback-active`、`candidate-manual-muted` 或任何 remote-only 候选时，都不能当作已启用远端独占声音；必须先向用户说明当前 Mac 本机声音影响、选择唯一处理路线，并确认恢复复查路径后，才能做任何真实音量/输出设备/产品开关变更。
- W2 视频恢复暂停最新口径：Windows 控制端如果短时间内多次从 JPEG 兜底恢复 H.264，会暂停自动恢复并保持 JPEG 保画面；复制诊断会保留 `恢复暂停 <n> 次`，当前仍在暂停窗口内时还会显示 `暂停剩余 <秒>s`。现场看到这条，先理解为控制端主动防抖，不要马上反复重连；下一步查 Mac 关键帧/GOP、采集编码稳定性、网络丢关键帧或 WebCodecs 解码背压。
- 用户在场状态刷新口径：只有用户在当前线程重新明确说“我在场/回来了/可以授权/可以操作”后，且 Agent Link Board `/api/state.userPresence` 仍停在 `away`，新版联络板才可运行 `node scripts/codex-link-client.mjs --server http://192.168.31.68:17888 presence --status present --updatedBy "Mac Codex" --reason "presence refresh"` 同步结构化状态；用户离开时同理用 `--status away`。这只刷新协作门禁，不会请求密码、系统授权、真实 input/inject 或修改声音输出；进入任何需要用户操作的步骤前仍要先响铃并说明目标、安全边界和预计耗时。若当前运行中的联络板还没更新到新版服务端，命令可能返回 404，此时先发普通消息说明，等联络板服务重启后再用结构化 presence。当前权威仍以 `/api/state.userPresence` 为准。
- M1 远端声音上板刷新口径：如果通讯板 `Mac Remote Audio` 仍是旧摘要，Mac 端可运行 `node scripts/mac/check-mac-remote-audio-status.mjs --host 127.0.0.1 --port 43770 --server http://192.168.31.68:17888 --sendStatus --boardSummary` 刷新。`blocked-local-output` 表示 Mac 本机仍会出声，只能继续 no-volume-change 规划；`candidate-manual-muted` 也仍需音频 smoke 和恢复路径，不能当作 remote-only 已启用证据。该入口只读，不改音量、不切输出、不请求密码、不发 input/inject。
- W2 视频恢复循环最新口径：Windows 控制端现在会在 H.264/JPEG fallback 成功恢复后保留 `回退恢复 <n> 次` 和 `最近回退：<原因>`。现场复制诊断若只是短暂 `回退恢复 1 次`，优先理解为自动兜底后已回 H.264；如果次数持续增加，再查 Mac 关键帧/GOP、采集编码稳定性、网络丢关键帧或 WebCodecs 解码背压。
- M2 上板刷新口径：如果通讯板 `Mac Input Safety` 仍显示旧 `UserPresence=present` 或旧 ready 摘要，而 `/api/state.userPresence` 已是 `away`，Mac 端直接跑 `node scripts/mac/check-mac-input-safety-status.mjs --host 127.0.0.1 --port 43770 --checkBoard --server http://192.168.31.68:17888 --sendStatus --boardSummary` 刷新。`away` 时会发送 blocked/no-auth-only 摘要并保持非零退出，这是预期的 fail-closed，不代表脚本坏了；不要因此请求密码、授权或真实 input/inject。
- M2 真实输入用户提示最新口径：Windows 恢复总览现在会消费 `UserNoticeGoal/Action/Boundary/Duration` 并输出 `MacInputSafetyUserNotice=goal=verify-real-mac-input-safe-event-set action=watch-mac-screen-and-be-ready-to-take-over boundary=safe-event-set-only-no-click-delete-shortcuts-return-log duration=2-3-minutes`。现场看到这条时，表示真实 Mac 输入测试前必须先向用户说明目标、让用户看 Mac 屏幕并准备接管、只允许 safe 事件集且不点击/删除/快捷键破坏、预计 2-3 分钟；它仍不是已发送 input/inject 的证据。
- W3/M1 远端独占声音最新口径补充：Windows 控制端已消费 Mac 新版 `Consent=explicit-before-change` / `RestorePath=required-before-apply`，会把恢复路径门禁显示成“恢复路径需先确认”。现场看到这条时，表示任何静音、切输出设备或 remote-only toggle 前都要先说明如何恢复，并在结束后复跑只读状态检查；仍不能让脚本自动改 Mac 音量或输出设备。
- M2 真实输入最新口径：`check-mac-input-safety-status --checkBoard --boardSummary` 和 `plan-mac-safe-inject-rehearsal --checkBoard --boardSummary` 看到 `UserNoticeGoal=verify-real-mac-input-safe-event-set UserNoticeAction=watch-mac-screen-and-be-ready-to-take-over UserNoticeBoundary=safe-event-set-only-no-click-delete-shortcuts-return-log UserNoticeDuration=2-3-minutes` 时，只表示可以向用户发起“看着 Mac 屏幕的 safe 事件集验收说明”。它不是已开启 inject，也不是可自动发 input；真正测试前仍要先当面说明目标、用户动作、安全边界和预计 2-3 分钟，并且只用 `--confirmUserWatching` + `--inputEventSet safe`。
- W2 视频恢复最新口径补充：Windows 控制端触发 `keyframe-wait-timeout-fallback` 后会先请求 MJPEG/JPEG 保画面；如果接下来 2.5 秒冷却期已过且至少收到 3 帧稳定 JPEG，会再用同一 `display_settings` 请求恢复 `preferredVideoCodec=h264` / `preferredVideoEncoding=annexb`。现场看到短暂 JPEG 回退不代表永久降级；若反复回退/恢复循环，下一步查 Mac 端关键帧/GOP、采集编码稳定性或网络丢关键帧。
- W3/M1 远端独占声音最新口径：Windows 控制端现在会把 Mac 上板/提醒里的 `MacRemoteAudioStatus=status=local-playback-active`、`localOutput=audible` 或 `remoteOnly=not-active` 翻成 Mac 值守风险“Mac 本机仍会出声 / 远端独占声音未开启 / 远端独占声音需用户明确同意 / 不会自动改 Mac 音量”。看到这条时说明当前仍可能双路声音；不要把它当作已静音或已切输出设备，也不要让脚本自动改 Mac 音量，后续仍按用户明确同意后的手动静音/虚拟输出/product toggle 路线处理。
- W2 视频最新口径补充：Windows 控制端 H.264 背压后等待关键帧时，关键帧等待本身不再主动请求 MJPEG/JPEG。按当前协商刷新率放宽等待阈值后，超时会继续请求 H.264/annexb 重启视频流，现场诊断应看到 `keyframe-wait-h264-recovery` 和 `解码 recovering`。如果仍出现 `解码 JPEG 回退`，优先查连续解码失败、显式 fallback、Mac host 自身降级到 background-jpeg，或 Mac 端 H.264 关键帧/编码链路异常。
- W3 音频最新口径补充：Windows 控制端现在保留首次低水位 80ms 补缓冲；如果 2 秒内再次低于 70ms，会临时改用 120ms 稳定预缓冲，并在“现场声音”里输出 `稳缓冲 <n>` 与 `原因 queue-underrun-stable-prebuffer`。现场声音若 `补缓冲` 增长但 `稳缓冲` 很少，说明只是偶发供流间歇；若 `稳缓冲` 持续增长，优先查 Mac 采集/网络音频供流抖动或下一步做更完整 jitter buffer。

- W3 音频最新口径补充：Windows 控制端现在会在 WebAudio 队列低于 70ms 并补到 80ms 时输出 `补缓冲 <n>` 和 `原因 queue-underrun-prebuffer`。现场声音若主要是 `补缓冲` 增长，优先查 Mac 采集/网络供流间歇；若主要是 `重同步` 和 `queue-overflow-trim-future`，优先查 Windows 本地播放队列持续溢出或突发堆积。
- W2 视频低延迟最新口径：Windows 控制端现在把 WebCodecs `VideoDecoder.decodeQueueSize` 也纳入本机 H.264 背压治理；如果浏览器内部解码队列超过 8，即使 meta 队列还不长，也会走 `queue-overflow-wait-keyframe` 重同步，清旧队列并等待关键帧。现场诊断若看到 `本地过期丢帧` 和该原因，优先理解为 Windows 本机为了追实时主动丢旧帧，而不是远端采集必然丢帧。
- W2 视频最新口径：Windows 控制端“现场视频”诊断现在会统计相邻帧间隔 >=120ms 的明显卡顿事件，输出 `卡顿 <n>` 和 `最大卡顿 <ms> ms`。用户再说画面不像 60Hz 或偶发卡时，先看同一行的 `实收 FPS`、`平均间隔`、`最大间隔`、`卡顿`、`本机队列`、`解码延迟` 和 `本地过期丢帧`：卡顿次数高但队列不高，优先查采集/网络节奏；队列高或过期丢帧高，再继续查 WebCodecs/H.264 本地队列。
- W3 音频最新口径：Windows 控制端在 WebAudio 队列超过 450ms 并修剪未来 source 后，会把下一帧放到 120ms 重同步短预缓冲；正常 underrun 仍是 80ms 初始预缓冲。用户再反馈声音断续时，复制诊断“现场声音”应能看到 `缓冲 80/70/450/120 ms`、`重同步` 次数和 `queue-overflow-trim-future` 原因；先用这行判断是网络/采集端突发，还是本地播放队列持续溢出。
- Windows 一次性反控授权现在优先用带 userPresence 门禁的入口：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/windows/allow-windows-reverse-control.ps1 -CheckBoard -BoardSummary`，Node 等价为 `node scripts/windows/allow-windows-reverse-control.mjs --checkBoard --boardSummary`。`UserPresence=away` 时会输出 `BLOCKED_BY_USER_AWAY` 并且不会打开临时授权；`UserPresence=present` 时才继续允许 30 秒一次性授权。Mac 端需要反控 Windows 时，仍先说明目标、安全边界和预计耗时，再让 Windows 本机执行这条命令。
- Windows 开工第一屏现在优先读取 Agent Link Board `/api/state.userPresence`：`present` 会在 `check-windows-resume-status --checkBoard --boardSummary` 中显示 `UserPresence=present` 和 `UserPresenceAction=explain-before-auth`，表示可安排授权/输入密码/现场审核任务但必须先说明目标、用户要做什么、安全边界和预计耗时；`away` 会显示 `UserPresenceAction=no-auth-only blocker=BLOCKED_BY_USER_AWAY`，只做无授权任务。不要再从历史“休息/睡觉/USER_SLEEPING”消息推断当前用户状态；结构化 `userPresence` 优先。

- Windows 手工体验第一屏现在也读取 `/api/state.userPresence`：`node scripts/windows/check-windows-manual-ux-status.mjs --boardSummary` 或 PowerShell 入口 `scripts/windows/check-windows-manual-ux-status.ps1 -BoardSummary` 会显示 `UserPresence=present|away` 和 `UserPresenceAction=`。`present` 时可以先说明目标、安全边界和预计耗时，再进入手工体验；`away` 时会显示 `BLOCKED_BY_USER_AWAY`，即使 Mac manual UX 已 ready，也不要请求密码、系统授权、真实 input/inject 或现场音视频确认。

- Mac 真实输入安全路径当前新增只读门禁：开工或准备 M2 前先跑 `node scripts/mac/check-mac-input-safety-status.mjs --host 127.0.0.1 --port 43770 --boardSummary`，或直接看 `MacHeartbeat=` / `MacResumeStatus=` / `MacUnattendedStatus=` 同屏的 `MacInputSafetyStatus=`。当前真机摘要可到 `ready reason=log-mode-permissions-ok inputMode=log permissions=ok`，但同一行仍写明 `realInput=blocked-until-user-watching required=--confirmUserWatching eventSet=safe`；这不是已开启真实输入。只有用户明确确认正在看 Mac 屏幕后，才安排带 `--confirmUserWatching` 的安全启动和 safe 事件集验收。
- Mac 侧心跳/恢复摘要现在会生产稳定 `MacCodexHealth=`：Windows 若要判断 Mac Codex 是否 stale，优先看 `MacCodexHealth=blocked reason=mac-codex-stale`；若只看到 `MacHeartbeatHealth=`，仍按旧兼容逻辑处理。`MacCodexHealth=` 只是无密协作状态，不代表 Mac host/client 离线，也不会自动发送 call、密码、认证或 input/inject。
- 如果 Windows 开工第一屏看到 `MacCodexStaleAction=status=blocked reason=mac-codex-stale next=RefreshAgentLinkBoardOrCallMacCodex`，先让 Mac 端刷新/继续 Mac Codex 或重新上报 `MacHeartbeat` / `MacManualUx`；需要对方配合时再手动复制同屏 `MacCodexStaleCall=` 发起无密 call。这个字段只来自稳定 `MacHeartbeatHealth` 短字段，不代表 Mac host 或 Mac client 离线，也不会自动发送密码、认证或 input/inject。
- 同一开工第一屏如果看到 `MacCodexHealth=blocked|warning|failed|stale ...`，先按 “Mac Codex 自己状态异常” 处理：刷新 Mac 端 Codex、看通讯板最新 call/status，必要时再发无密 call。它不等于 Mac host 断流、不等于 Mac client 页面离线，也不代表需要重新输入远控密码；Windows 端只读显示，不运行 Mac 脚本、不认证、不发 input/inject。
- 不要再回旧第二步/diagnostics 循环：Windows 控 Mac formal E2E 主体已经 PASS。
- 手工体验第一屏口径：`node scripts/mac/check-mac-manual-ux-status.mjs --server http://192.168.31.68:17888 --boardSummary` 若通讯板只有 loopback，会只读本机 Mac host `/discovery` 和本机 LAN IPv4，输出 `Target=<Mac LAN>:43770 TargetSource=mac-host-discovery`；本轮真机 fallback 已验证 `Target=192.168.31.122:43770`。看到 `MacManualUx=status=ready` 且 `Target` 是非 127 的 LAN 地址时，Windows/User 可直接用该目标进入手工体验清单；脚本不认证、不请求密码、不发 user-auth/input/inject。
- 密码输入位置当前口径：Windows 控制页手动连接时，Mac 当前临时密码填页面左侧“连接密码”框；页面会清空演示密码、聚焦该框并显示提示。只有 `--promptPassword` / formal runner 出现“终端隐藏输入”提示时，才把密码输入黑色终端窗口；不要把密码发通讯板。
- Mac 控 Windows 页面当前口径：Windows host 临时密码填 Mac 页面里的“密码”框；该框下方会固定提示“在这里输入 Windows 临时密码；不要发到通讯板；不保存到最近连接或诊断”。复制/导出诊断会带 `密码输入位置`，但不会带连接密码或英文 `password` 字样。
- 如果 Windows 恢复总览或 Windows 控制页看到 `MacClientPasswordLocation=`，按“Mac client 页面密码框填写 Windows 临时密码；formal/browser runner 的终端隐藏输入只用于脚本；不要把密码发通讯板”理解。这只是无密位置提示，不代表已经连接、认证或发送输入。
- Mac 控 Windows 真机 smoke 的安全口径：Windows host 已在线但用户未明确看 Windows 屏幕时，Mac 端优先跑 `node scripts/mac/run-mac-client-formal-smoke.mjs --host <Windows LAN IP> --port 43770 --ensureClient --promptPassword --skipInput --boardSummary`。`--skipInput` 会跳过鼠标/键盘 `input_event` 和 `input_ack`，仍验证连接、视频、音频、剪贴板/诊断等安全链路；密码只在本机隐藏输入，不发通讯板。只有用户明确确认正在看 Windows 屏幕后，才另行做真实输入验收。
- Mac 控 Windows 前若通讯板提示 Windows host offline/no-listener，Windows 侧先跑 `node scripts/windows/start-windows-host.mjs --status --checkBoard --boardSummary --host 127.0.0.1 --port 43770` 看无密摘要。摘要里的 `WindowsHostStartAction=needs-local-password-prompt` 会给出 `Start=` 本机隐藏密码启动命令和 `AfterStart=node scripts/mac/discover-windows-hosts.mjs --checkBoard --boardSummary` 只读重试命令；不要把密码发到通讯板，也不要让 Mac 端默认再发 call。
- 更短的现场启动方式：Windows 用户在仓库根目录双击 `Start-Windows-Host.cmd`，在弹出的本机终端里输入 Windows host 临时密码；启动后再让 Mac 端跑只读 `node scripts/mac/discover-windows-hosts.mjs --checkBoard --boardSummary`。需要看参数说明时运行 `.\Start-Windows-Host.cmd -Help`，不会启动 host；状态摘要里看到 `WindowsHostUserEntry=Start-Windows-Host.cmd` 时，优先按这个双击入口执行。
- 如果 Windows host 在线摘要里看到 `WindowsHostBuildStatus=metadata-stale ... hostRuntimeChanges=0`，只代表运行中 host 的 build metadata 落后，Windows host 运行源码未变，不要把它当成 Mac 控 Windows 阻塞；看到 `WindowsHostBuildStatus=restart-required` 时，再让 Windows 用户本机双击 `Start-Windows-Host.cmd` 重启。
- Mac 端现在会在 `discover-windows-hosts --checkBoard`、`check-mac-client-readiness --checkBoard` 和 `run-mac-client-formal-smoke --discover` 的 JSON/`--boardSummary` 中转述 `WindowsFirewallHealth=`。如果摘要同时有 `WindowsLanRisk=public-profile` 和 `WindowsFirewallHealth=nonblocking reason=public-profile-firewall-disabled`，按“网络 Profile 是 Public，但当前防火墙不是阻塞点”处理；不要仅因为 Public profile 要求用户改网络或防火墙。该转述只读，不认证、不请求密码、不发 input/inject。
- Mac 端 `check-mac-client-formal-status --host <Windows LAN IP> --port 43770 --boardSummary` 现在也会直接显示 `WindowsHostSession=`：新 Windows host 会给出 stage/auth/video/audio/session/closed，旧 Windows host 或未重启时会显示 `diagnostics-unavailable`。这个字段只是定位线索，不代表失败；若 repo/client/host 其它项 ready，可继续按 safe smoke 或用户在场流程推进。
- 如果 Mac browser smoke 或 Mac 控 Windows 页面超时，先让 Windows 侧跑 `node scripts/windows/start-windows-host.mjs --status --host 127.0.0.1 --port 43770 --boardSummary` 看 `WindowsHostSession=`：`stage:streaming videoFrames>0` 代表 Windows host 已看到认证、会话和首帧，优先让 Mac 端查 browser smoke 等待条件/浏览器日志；`auth-failed` 查密码输入位置；`connected/hello` 查客户端是否发认证；`session` 且无首帧再查视频管线。该摘要来自 `/diagnostics`，不含密码、不含原始 auth 消息。
- 同一行如果看到 `WindowsHostDiagnostics=unavailable restart=only-if-session-debug-needed`，先按“旧 Windows host 还没有新诊断端点”理解，不要把它当作远控不可用或必须立刻重启。只有当前任务正需要 `WindowsHostSession=` 追 smoke 超时/会话阶段，才让 Windows 用户本机重启 host；普通 discovery、连接和现有画面/音频验证可以继续。
- 如果 Windows readiness / 通讯板同一行看到 `WindowsFirewallHealth=nonblocking reason=public-profile-firewall-disabled`，按“Public 网络只是环境事实，当前防火墙不是阻塞点”处理；`WindowsLanRisk=public-profile` 可继续提醒现场网络位置，但不要因此要求改防火墙。只有 TCP LAN 探测不通、listener 不是 `0.0.0.0`，或 `WindowsFirewallHealth=warning reason=no-firewall-allow/public-profile` 时，再回到防火墙/网络位置排查。
- 如果 Windows 手工体验第一屏或恢复总览看到 `MacManualUx` 的 `TargetSource=board` / `targetSource=board`，按“目标来自 Mac 手工体验摘要上板”理解；看到 `mac-host-discovery` / `board-discovery` 则分别按 Mac host discovery 或通讯板 discovery 理解。`TargetSource=unknown` 只表示来源未被 Mac 摘要证明，不等于目标不可用；仍继续看实际 discovery、认证、视频/音频/剪贴板证据，不因此回旧 formal/password 流程。
- 最新真实体验 blocker 第一轮已收口：Windows 控制端页面上下抖动来自底部状态栏实时文字换行撑高，已固定为 36px 单行省略；声音丢包第一轮修复为 WebAudio 80ms 普通预缓冲、450ms 高水位修剪未来队列并用 120ms 重同步预缓冲播放最新帧、状态文字限频；视频低延迟第一轮新增 H.264 本机队列治理，解码队列超过 8 帧或最旧帧超过 450ms 时清掉旧队列，delta 帧等待下一关键帧，避免继续播放过期画面。复制诊断现在会输出“现场视频 / 现场声音”两行，包含实收 FPS、请求/协商 Hz、平均/最大帧间隔、卡顿次数、最大卡顿间隔、视频本机队列毫秒、解码延迟、本地过期丢帧/原因、WebAudio 队列毫秒、缓冲阈值、接收/播放/丢弃计数、重同步次数和原因。下一次用户实测时重点确认：连接后画面是否还上下跳、画面是否仍明显卡顿、声音是否仍断续；若仍有问题，先抓这两行诊断，再让 Mac 端同步跑视频/音频基线观察。
- remote-only audio 先按安全方案处理：当前 Mac `system-pcm` 采集不会自动让本机静音；需要“只在 Windows 播放、Mac 本机不出声”时，先跑 `node scripts/mac/plan-mac-remote-audio.mjs --boardSummary` 看 `manual-mute-restore` / `virtual-output-device` / `product-toggle` 三条路线。没有用户明确同意前，不要让脚本自动改系统音量或切换输出设备。
  - 最新 plan 摘要会额外显示 `Consent=explicit-before-change` 和 `RestorePath=required-before-apply`；JSON 会有 `consentChecklist[]` / `restoreChecklist[]`。含义是：执行任何静音、切输出设备或产品 remote-only toggle 前，必须先向用户说明当前 `system-pcm` 不会自动静音 Mac、本次选择哪一条路线、如何恢复，并在恢复后复跑 `check-mac-remote-audio-status`。这些字段仍是只读门禁，不代表已经 remote-only。
  - 先跑只读状态门禁：`node scripts/mac/check-mac-remote-audio-status.mjs --host 127.0.0.1 --port 43770 --boardSummary`。看到 `MacRemoteAudioStatus=status=local-playback-active` 代表 Mac 本机仍会出声，不能宣称 remote-only；看到 `local-output-muted` 也只代表用户手动静音候选，仍需音频 smoke 和恢复路径。该脚本不改系统音量、不切输出设备、不认证、不请求密码、不发 input/inject。
  - 同一只读状态门禁现在也会出现在 `MacHeartbeat=`、`MacResumeStatus=` 和 `MacUnattendedStatus=` 第一屏里；只看 heartbeat/unattended 时也可以直接复制 `MacRemoteAudioStatus=` 检查，不必回完整 resume 才能发现本机仍会出声。
  - Windows 控制端现在会消费同一组 `MacRemoteAudioPlan=` / `Mac remote audio plan:` 文本：Mac 提醒区、Mac 值守快速摘要和复制/导出诊断会把它显示为“Mac 远端独占声音方案已提供 / 当前不会自动静音 Mac 本机 / 远端独占声音需用户明确同意 / 不自动改系统音量”。看到这条时先按“只读方案提示”理解，不要把它当作已经切换远端独占声音。
  - Windows 恢复总览现在也会消费 `MacRemoteAudioPlan=`：开工第一屏、JSON 和 `--boardSummary` 都会显示 `MacRemoteAudioPlan=node scripts/mac/plan-mac-remote-audio.mjs --boardSummary`。看到这条时仍按“只读远端独占声音方案”理解，不要把它当作已经静音 Mac 本机或切换输出设备。
  - Windows 恢复总览现在还会消费 `Mac remote audio plan:` 摘要本体：开工第一屏、JSON 和 `--boardSummary` 会显示 `MacRemoteAudio=status=plan-only capture=system-pcm-does-not-mute-local remoteOnlyOptions=manual-mute-restore/virtual-output-device/product-toggle recommended=product-toggle-with-explicit-consent safety=no-volume-change,no password/input/inject`。看到这条只代表安全方案已被两端共同读懂，仍不能自动静音 Mac、不能自动切换输出设备，也不能跳过用户显式同意；新版 plan 额外的 `Consent=` / `RestorePath=` 是给人工执行前看的门禁提示，旧 Windows 摘要字段保持不变。
  - Windows 控制端现在也会消费 `MacInputSafetyPlan=` / `Mac input safety plan:` 文本：Mac 提醒区、Mac 值守快速摘要和复制/导出诊断会把它显示为“Mac 真实输入安全方案已提供 / 默认输入模式保持安全日志 / 真实输入需用户正在看 Mac 屏幕 / 真实输入需 --confirmUserWatching / 先用 safe 输入事件集 / 不发送输入事件或执行注入”。看到这条时先按“真实输入仍被安全阻止”理解，不要把它当作已经切到 inject。
  - Windows 恢复总览现在也会消费同一条 `MacInputSafetyPlan=` / `Mac input safety plan:`：开工第一屏、JSON 和 `--boardSummary` 都会显示 `MacInputSafetyPlan=node scripts/mac/plan-mac-input-safety.mjs --boardSummary` 以及 `MacInputSafety=status=plan-only default=log realInput=blocked-until-user-watching required=--confirmUserWatching eventSet=safe safety=no-password,no-input-events,no-inject`。看到这条时仍按“只读安全边界”理解，不能当作真实 inject 已开启。
  - Windows 恢复总览现在也会消费 `MacInputSafetyStatus=`：开工第一屏、JSON 和 `--boardSummary` 会显示 `MacInputSafetyStatus=status=<blocked|ready|warning|unknown> ... realInput=blocked-until-user-watching|ready-for-user-watched-inject ... eventSet=safe ...`。看到 `ready-for-user-watched-inject` 也只代表门禁状态可见，真正发送输入仍需要用户正在看屏、显式确认和后续单步验收；不要把它当作自动 inject。
- 尾部 NativeCommandFailed 的处理结论是改用 PowerShell 7 / pwsh 路径。现场复测或用户授权输入密码时，优先复制 resume/status 摘要里的 pwsh ... check-mac-formal-e2e.ps1 ... -PromptPassword 命令。
- Windows 控 Mac 一键入口现在除 LAN `/discovery` 扫描外，还会只读读取 Agent Link Board `/api/state` 里的 Mac 相关状态，把非敏感 Mac LAN host/port 当作 discovery 候选；只有候选真实返回 macOS host discovery 时才会选中并显示 `targetSource=board-discovery`。需要指定通讯板用 `--server` / `-Server`，需要关闭通讯板候选用 `--noBoardTarget` / `-NoBoardTarget`；疑似 `password=...`、token/secret 或 `--password` 的通讯板文本会被忽略，不认证、不请求密码、不发 input/inject。
- 下一步继续手工体验验收：窗口/全屏、画面流畅度、声音、文本和文件剪贴板、input_ack，以及用户明确确认后的真实 inject 安全验收。
- 双端脚本若需要结构化读取 Agent Link Board，优先用 `node scripts/codex-link-client.mjs --server http://192.168.31.68:17888 state --json`；该命令现在输出纯 `/api/state` JSON，适合自动化解析。人工快速查看仍可用默认 `state` 文本输出，`watch --once` 保持事件流文本。
- 巡检或需要上报 DAILY_ITEM 时，优先跑 `node scripts/codex-link-daily-items.mjs --preset wmc-current --boardSummary`；确认一行里有 `DAILY_ITEM W1 PASS`、`W2 PASS`、`W3 PASS`、`M1 PASS`、`M2 PASS`、`C1 PASS`，且每项带旧编号 `alias=` 后，再按需要显式加 `--sendStatus --sendMessage --server http://192.168.31.68:17888` 上板。`--preset night-unattended` 仅作兼容别名，也会输出新 W/M/C 编号。默认只读、不上板、不认证、不请求密码、不发 input/inject。
- 准备进入手工体验或需要告诉 Windows 端 Mac 已待命时，Mac 端可跑 `node scripts/mac/check-mac-manual-ux-status.mjs --server http://192.168.31.68:17888 --sendStatus --sendMessage --boardSummary`；它只读当前 `ManualUxTest` / `USER_AWAKE` 信号并发送无密摘要。若当前是 `USER_AWAKE` 且要求准备真实体验验收，会输出 `MacManualUx=status=call-ready`、`Next=SendManualUxCall` 和 `ManualUxCallCommand=`，用于先在通讯板说明目标、安全边界和预计 5-10 分钟；确认要直接发起该 call 时，可显式运行 `node scripts/mac/check-mac-manual-ux-status.mjs --server http://192.168.31.68:17888 --sendCall --json`。该发送只允许回应匹配的 `USER_AWAKE` 手工体验 call，遇到其他 active call 会拒绝覆盖；默认不自动发 call、不认证、不请求密码、不发 user-auth/input/inject。
- 如果当前 `MacManualUx=status=calling` / `Next=WaitForManualUxConfirmation`，Windows/User 准备好 5-10 分钟真实体验窗口时，在通讯板发送 `MAC_MANUAL_UX_CONFIRMED: Windows/User confirmed the manual UX window.`；Mac 状态脚本只接受 Windows/User 且晚于当前 call 的确认，会转为 `MacManualUx=status=ready` / `Next=ManualUxTest`。旧确认消息和 Mac 自己 call 里的 `Please confirm` 不会误触发。
- 如果 `check-windows-resume-status --checkBoard --boardSummary` 看到 `MacClientManualChecklist=`，说明 Mac client 页面已经把“连接/视频/音频/剪贴板/input_ack/诊断”和复制诊断提示暴露给 Windows 第一屏；现场就按这条清单逐项测，不要把它当成会自动连接或自动发送输入的命令。Windows 只读显示这条提示，不运行 Mac 脚本、不认证、不请求密码、不发 input/inject。
- 如果只想看手工体验第一屏，Windows 现场优先跑 PowerShell 7 入口 `scripts/windows/check-windows-manual-ux-status.ps1 -BoardSummary`；Node 等价命令是 `node scripts/windows/check-windows-manual-ux-status.mjs --boardSummary`。当旧 Mac manual UX call 已超时时，它会输出 `WindowsManualUx=status=reconfirm`、`Next=AskMacReconfirmManualUxCall`、`MacManualUxReconfirm=... --reconfirmCall --json` 和 `MacClientManualChecklist=...`；此时不要直接开始测试或发送旧确认，先让 Mac 端重新确认新的 5-10 分钟手工体验 call。该入口仍只读通讯板，不运行 Mac 脚本、不认证、不请求密码、不发 input/inject。
- Windows/User 准备确认 Mac 手工体验 call 时，先看 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --boardSummary` 的 `MacManualUxAck=`：如果是 `node scripts/codex-link-client.mjs ... MAC_MANUAL_UX_CONFIRMED ... WINDOWS_MANUAL_UX_ACK`，可显式运行 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --sendManualUxAck --json` 或 PowerShell `scripts/windows/check-windows-resume-status.ps1 -CheckBoard -SendManualUxAck -Json` 发无密确认；如果显示 `status=blocked reason=manual-ux-call-timeout`，不要确认旧 call，先让 Mac 端重新发起/确认手工体验 call。
- 如果同一行 `MacManualUx=` 带 `gate=wait-windows-codex-push` 或 `MacManualUxGate=wait-windows-codex-push`，说明 Mac 正在等 Windows 推送/提交窗口结束；不要发送手工体验确认，先完成 Windows commit/push/rebase 并重新跑 resume/status。若同时已 timeout，仍按 `manual-ux-call-timeout` 处理，让 Mac 端重新确认 call。
- 推送预告、文档说明或普通状态备注里不要把“提到确认短标签”当成真实手工体验确认；Mac 状态脚本已防误触，只有确认正文以固定确认短标签开头，或以明确确认动作开头且包含手工体验语义，才会让 `MacManualUx` 进入 ready。
- 如果 `check-mac-manual-ux-status --boardSummary` 已显示 `ManualUxCall=timeout` / `Next=ReconfirmManualUxCall`，同一行还会带 `ManualUxCallAgeMs=` 和 `ManualUxCallOverdueMs=`；未超时时会带 `ManualUxCallRemainingMs=`。Windows resume 会安全规范化为 `callAgeMs=` / `callRemainingMs=` / `callOverdueMs=`，方便两端判断是继续等确认还是重新发起；若 Mac 同行给出 `ManualUxReconfirmCommand=`，Windows resume 现在会单独输出 `MacManualUxReconfirm=node scripts/mac/check-mac-manual-ux-status.mjs --server <board> --reconfirmCall --json`，供 Mac 端在确认 Windows 不处于 push/rebase 临界期后显式重发 call。确认信号只在当前 call 的有效窗口内生效；超时后的 `MAC_MANUAL_UX_CONFIRMED` 不会直接进入 `ManualUxTest`。重新发起前先确认 Windows Codex 不在 `pushing-soon` / push / rebase 临界期；确认后可显式运行 `node scripts/mac/check-mac-manual-ux-status.mjs --server http://192.168.31.68:17888 --reconfirmCall --json` 重新发起新的 5-10 分钟手工体验 call。该入口只会替换 Mac 自己发起且已超时的 manual UX call，未超时、非 Mac call 或 Windows 推送期都会拒绝，不认证、不请求密码、不发 input/inject。
- Mac 控 Windows formal/status 摘要里的反控演练已经改成 Step 1/2/3：Mac 先点“请求反控”并预期 `LAN008/default deny`，Windows 只在 Windows host 本机 loopback 跑 `WindowsOpenOneTimeReverseGrant` 开 30 秒一次性授权，Mac 再点“重试反控”并确认 `临时授权已使用`。看到 `Do not run the grant from Mac` 时要照做：Mac 端不要直接调用 Windows 授权 helper，密码也不要发通讯板。
只放短期任务，长期计划继续放在 `docs/04-task-board.md`。

## 最高优先级

最新现场状态：Windows 控 Mac formal E2E 主体已经 `REAL_TEST_PASS`。固定证据为 `host=192.168.31.122:43770`，`build=8015f22`，`inputMode=log`，H.264/WebCodecs OK，`H264Errors=0`，Plan 2 browser canvas OK，audio PCM 有帧/播放记录，并已出现 `[OK] Formal Mac E2E checks finished`。不要再回到 Mac ready、formal E2E 第二步或 diagnostics 旧循环。

最新分工：Windows 端记录 `REAL_TEST_PASS` 摘要，并单独排查 OK 之后的 PowerShell/Node 尾部错误 `node.exe 无法运行: 索引超出了数组界限 / NativeCommandFailed`；这不推翻 PASS。Mac 端保持 host/client/heartbeat 在线，等待下一轮手工体验测试：画面、声音、剪贴板、文件、小窗、全屏/原画、复制诊断。除非用户明确确认正在看 Mac 屏幕，不做 true input inject。

当前 Windows 首选入口：在仓库根目录双击 `Start-Windows-Control-Mac.cmd`；PowerShell 7 等价入口是 `scripts/windows/start-windows-control-mac.ps1`；Node 等价命令是 `node scripts/windows/start-windows-control-mac.mjs`。入口默认先只读读取 Agent Link Board Mac 候选并探测 LAN `/discovery`，能发现 Mac host 时摘要显示 `targetSource=board-discovery` 或 `targetSource=discovery`；发现失败才回退默认地址。等浏览器打开后，页面会清空演示密码并聚焦左侧“连接密码”框，只在这个框里输入 Mac 当前临时密码并点连接；终端隐藏输入提示只用于 formal/browser runner。需要无密上板时跑 `scripts/windows/start-windows-control-mac.ps1 -DryRun -BoardSummary`。

当前 Mac 控 Windows 首选入口：在 Mac 仓库根目录双击 `Start-Mac-Control-Windows.command`；它只会启动/复用本地 Mac 控制端页面并打开浏览器，之后再由用户在页面里发现 Windows host、输入临时密码并点连接。终端等价命令是 `node scripts/mac/start-mac-client.mjs --allowExisting --open`。

当前 Mac client 状态摘要入口：`node scripts/mac/start-mac-client.mjs --status --boardSummary` 会输出 `MacUsableEntry=... Entry=./Start-Mac-Control-Windows.command`。如果白天开工只看到 `MacClientPage=` 或页面离线，先用这个入口打开本地页面，再继续 Windows discovery / formal checklist。

当前 Windows 最短入口：用户在 Windows 上优先运行 `scripts/windows/start-windows-control-mac.ps1`，或 Node 等价 `node scripts/windows/start-windows-control-mac.mjs`；脚本会先只读读取 Agent Link Board Mac 候选并探测 LAN `/discovery`，自动预填发现到的 Mac host，失败才回退 `192.168.31.122:43770` / WebSocket，用户只需在页面里输入 Mac 当前临时密码后点连接。不要让用户再记旧的 `5197/9337` 诊断端口；本入口固定可用口径为 `clientPort=5200 debugPort=9340`，不认证、不发 input/inject；需要锁死旧地址时加 `-NoDiscover` 或 `--noDiscover`。

最新校正：Mac host 当前实机在线为 `192.168.31.122:43770` / `127.0.0.1:43770`，前台同密/log/60fps，runtime build `8015f22`；通讯板已可达。最新 `MacHeartbeat` / `MacResumeStatus` 已修复旧状态回声，当前值守状态应看作 `MacUnattendedHealth=ok reason=ok blockers=none warnings=none`，不要再把历史 `launch-agent-not-loaded`、`bed2095` 或 `accessibility` warning 当作当前 blocker。

最新 heartbeat watcher 校正：Mac 端后台 `Mac Heartbeat` watcher 已重启并恢复上板，当前 `server=http://192.168.31.68:17888`、`lastRun=1 post=posted`、`lastHeartbeat=status=ok`、`refreshUnattended=true`、`configMismatch=none`。后续若 `MacResumeStatus=` 看到 `post=post-failed` 或 `warnings=agent-link-board`，先看同屏 `server=` 和 `configMismatch=`；若 `configMismatch=server` 或 server 不是当前通讯板，复制 `RefreshRestart=` 重启 watcher，不要误判为 Mac host 离线。

最新 Mac client 安全提示：Mac 控 Windows 页面现在在密码框下显示 `passwordSafetyStatus` 和固定的 `passwordLocationHint`，提示 Windows 临时密码只填 Mac 页面密码框、不要发通讯板、不保存到最近连接或诊断；复制/导出诊断会带 `密码安全` 和 `密码输入位置` 状态，但不输出连接密码或英文 `password` 字样。若页面仍显示“演示密码”，不要把它当正式联调密码。

- 复跑/重启认证路径：当前 `MacUnattendedStatus` / `MacHeartbeat` / `MacResumeStatus` / `MacHostReadiness` / `MacFormalE2E` 会输出 `MacHostAuthPath=prompt-password-required reason=launch-agent-ephemeral-password mode=ephemeral next=MacHostStop->MacMaxFpsSafeStart->MacHostMedia`。Windows 控制端现在也会把这条显示为“Mac host 需要前台输入连接密码 / 当前 Mac host 是一次性密码模式 / Windows 控制页密码框填写同一个临时密码 / 先在 Mac 前台同密重启 60Hz host / 不要把密码发到通讯板”。只有需要复跑正式 Windows 控 Mac 或重新启动 host 时，才按这条路径在用户在场时输入同一个临时密码；密码仍不要发通讯板。

1. Mac 端保持值守并准备手工体验测试。
   - 默认使用 `LAN_DUAL_INPUT_MODE=log` 做安全联调。
   - 当前 Mac host 已在 `192.168.31.122:43770` / `127.0.0.1:43770` 运行当前 build `8015f22`，`inputMode=log`、`maxScreenFps=60`；最新心跳为 `ok`，值守状态为 `MacUnattendedHealth=ok`，电源状态为 `MacPowerHealth=ok`，Mac client 页面也在线。Mac 端不需要再重跑前置动作，也不需要等待 Windows 输入密码才能继续值守。
   - 下一轮用户在场时，优先让 Windows 控制端做手工体验清单：画面清晰度、延迟/帧率、声音接收和播放、文本/文件剪贴板、小窗拖动缩放、普通全屏/真全屏/原画、断线重连、复制诊断是否能直接粘贴给两端。
   - 如果需要复跑自动 formal E2E 或重启 host，再按 `MacHostAuthPath=prompt-password-required` 的前台同密流程处理；密码只在两端本机隐藏输入，不写命令参数，不发通讯板。
   - 真实输入注入只能在用户明确确认正在看 Mac 屏幕后执行；Mac 端通过启动助手切换时必须带 `--confirmUserWatching`，不要把裸 `LAN_DUAL_INPUT_MODE=inject` 当作日常复跑路径。

2. Windows 端继续完善控制体验。
   - 第一阶段 UI 主线已经开始落到 Windows 控 Mac 页面：悬浮控制中心现在有显示器、画质、分辨率、刷新率、码率、缩放、声音、音量、常用 macOS 快捷键、普通全屏、真全屏、窗口、复制诊断、立即重连、退出远控、`Esc` 退出提示、连接/重连状态、视频链路/实收 FPS/帧延迟状态、协商/请求/远端上限刷新率提示、声音接收/播放状态、剪贴板/远端文件状态、输入模式和安全状态；普通窗口诊断条也会在实收 FPS 明显低于协商或请求 Hz 时提示“低于协商/低于请求”，并在 Mac host 上限低于用户选择值时提示“远端上限 30 Hz”。后续真实 Mac 连接时重点验收原画模式是否清楚、真全屏是否能进入/退出、全屏后是否能顺手退出、断线后浮层是否显示倒计时且可立即重连、全屏内复制诊断是否能粘贴快速摘要且按钮有已复制/失败反馈、粘贴出来的诊断报告是否带独立视频状态、输入模式/ack 以及全屏浮层连接/视频/声音/剪贴板/输入/安全状态、视频状态是否能帮助判断“不是 60Hz/卡顿/回退 JPEG/远端最高 30Hz”，声音状态是否能区分没收到音频帧/收到但未播放/音量为 0、剪贴板状态是否能显示远端文件/压缩包接收进度和系统剪贴板写入结果、快捷键入口是否可用，以及 `inputMode=log` 时是否明确显示“只记录、不真正控制”。
   - 小窗/监看模式第一版已落地为页面内“Mac 监看”小窗：复用现有视频画布和诊断状态，默认只监看、不发送键鼠或快捷键，显示远端画面、连接/FPS/延迟、输入模式、安全状态、最近 Mac 提醒/授权等待，并提供拖动、缩放、恢复主窗口、复制诊断和断开。下一步真实 Mac 连接时重点验收小窗尺寸、拖动/缩放手感、Mac 卡住提醒是否能在小窗里读清楚，以及窗口最小化场景是否足够好用；稳定后再把同一状态抽到桌面壳 always-on-top 独立小窗。
   - Windows 侧已经先补了 Mac 可能卡住的独立提醒底座：watcher 会识别 Mac Codex `checking` / `thinking` / `running` 等进行中状态超时、Mac -> Windows currentCall 超时、`MacHeartbeat=stale`、`reason=mac-codex-stale`、`reason=codex-reconnect-signal`、watchdog 心跳过期、Mac host `/discovery` 不可达、502/Bad Gateway/API 网络错误；也已接入精确 `codex-reconnect-stuck` 信号，包括 `正在重新连接 5/5`、`stream disconnected before completion` 和 `error sending request` + `/backend-api/codex/responses`。小窗第一版会直接消费 Mac 提醒 watcher 的 `recentAlerts` / `lastAlert` 和中文风险摘要，不只依赖 Mac Codex 自己上板。Windows 控制端“Mac 提醒”区现在也有五个 Mac heartbeat watcher 复制按钮：`心跳一次`、`前台持续`、`后台启动`、`查状态`、`停止心跳`；它们只复制可在 Mac 端执行的安全命令，不会在 Windows 端认证、发密码或发 input/inject。
   - 正式 Mac E2E 的第二步现在有外层 `step total timeout` 兜底：`check-mac-formal-e2e` 的 runPlan 和执行日志会同时显示 `expected`、`per-wait timeout` 和 `step total timeout`；如果 Windows client browser/H.264 canvas 子进程挂住，外层会杀掉 child 并报 `timed out ... (step total timeout)`。现场看到第二步长时间无进度时，先看进度心跳是否还在变化；超过 step total timeout 仍未结束就是要查 browser/端口/H.264，而不是继续无限等。
   - 重新复查第二步时，当前现场默认调试端口 `9337` 曾被旧 `msedge.exe` 占用，`check-windows-resume-status --checkBoard --boardSummary` 会输出 `WinClientPorts=occupied(9337;stale-diagnostics)` 和备用 `5200/9340` 命令。若看到类似情况，优先用 `--clientPort 5200 --debugPort 9340` 跑 `test-windows-client-browser --diagnosticsOnly --boardSummary` 或 formal preflight；本轮已修复 diagnostics/discovery 全屏诊断里轻量 client 缺 `sendDisplaySettings` 导致的崩溃，备用端口页面诊断已通过。
   - Windows 控制端现在会进一步解析 `MacHeartbeat=` 摘要里的 `checkedAt=`、Mac Codex `updatedAt=` / `ageMs=` 和 `boardUpdatedAt=`；如果 `checkedAt` 超过约 2 分钟，Mac 提醒区、Mac 值守快速摘要和复制/导出诊断会显示“Mac 心跳摘要过旧”，导出报告会带“Mac 心跳新鲜度：心跳检查 / Mac Codex / 联络板”。现场看到这条时，先让 Mac 端重新跑 `MacHeartbeatOnce=` 或查后台 watcher 状态，再判断 Mac Codex 是否真的卡住。
   - 如果最新 `MacHeartbeat=` 是新鲜的 `status=ok`、`blockers=none`、`warnings=none`，Windows 控制端会显示“证据：Mac 心跳正常”或导出“值守证据 Mac 心跳正常”；如果同一行 heartbeat 还带 `Evidence=MacClientPageOnline`，说明 Mac client 页面也在线；如果同一行还带 `MacClientDiagnosticsOk`，说明 heartbeat 同次确认了本地页面标题和 Agent Link Board 可读，可被 Windows resume / 控制端归入同一套稳定“Mac client 诊断已通过”证据。看到 `stale metadata only, hostRuntimeChanges=0` 不要按心跳过期处理；只有真正 stale/blocked/warning、非空 warning/blocker、host 不可达或 Codex 重连信号才继续按风险摘要排查。
   - 当前最新 Mac 值守证据里 `MacPowerHealth=ok reason=ok warnings=none`，系统睡眠和显示器睡眠已设置为 `sleep=0` / `displaysleep=0`；当前前台同密 host 是 build `8015f22` 的 60fps/log host，且 `MacUnattendedHealth=ok`。短期下一步优先按 `REAL_TEST_PASS` 后续分工推进：Windows 记录 PASS 并排查尾部 `NativeCommandFailed`，Mac 保持值守并等待用户在场的手工体验测试；不要再把电源 warning、`launch-agent-not-loaded`、旧 build 或历史 `accessibility` warning 当成本轮 blocker。
   - Windows 控制端页面现在也会消费同一组 `PostPassNext=` / `ManualUxChecklist=`：Mac 提醒区、值守证据和复制/导出诊断会显示“已进入手工体验清单：连接/画面/声音/剪贴板/文件/窗口/全屏/原画/复制诊断”，方便窗口最小化或复制诊断时直接知道下一步是手工体验测试，而不是旧 formal E2E 复跑。
   - 开工或用户准备手工验收前，Windows 侧可先跑 `node scripts/windows/check-windows-manual-ux-status.mjs --boardSummary`：它只读 Agent Link Board，确认是否已进入 `ManualUxTest`，列出连接/画面/声音/剪贴板/文件/窗口/全屏/原画/复制诊断清单，并明确 `Safety=no-password,no-input-inject`；如果只读到 Mac 本机 `127.0.0.1`，会显示 `Target=unknown`，不要把它当 Windows 可连地址。
   - Mac 侧也可同步跑 `node scripts/mac/check-mac-manual-ux-status.mjs --boardSummary`：它只读 Agent Link Board，确认是否已进入 `ManualUxTest`，输出 `MacManualUx=status=ready|waiting|call-ready|calling`、同一手工体验清单、可用 LAN Mac 目标和 `Safety=no-password,no-input-inject`；若当前是 `USER_AWAKE` 且要求准备真实体验验收，会输出 `ManualUxCallCommand=`，先让两端在通讯板确认用户在场目标、安全边界和预计 5-10 分钟，再做任何用户操作。需要直接同步通讯板时显式加 `--sendStatus --sendMessage --server http://192.168.31.68:17888`；确认要直接发 call 时显式加 `--sendCall --json`，且脚本只会回应匹配的 `USER_AWAKE` 手工体验 call，不覆盖其他 active call。默认不上板、不自动发 call、不认证、不请求密码、不发 user-auth/input/inject；`check-mac-resume-status --checkBoard --boardSummary`、`check-mac-heartbeat --checkBoard --boardSummary` 和 `check-mac-unattended-status --boardSummary` 现在都会给出 `MacManualUxStatus=` 复制入口，方便开工第一屏、只看最新心跳或只看独立值守行时不回旧 formal/password 复跑。
   - Windows 开工第一屏现在也会消费 Mac 端心跳/值守里的 `MacManualUxStatus=`：`node scripts/windows/check-windows-resume-status.mjs --checkBoard --boardSummary` 会同步输出 `MacManualUxStatus=node scripts/mac/check-mac-manual-ux-status.mjs --boardSummary`，并拒绝带密码、上板或非只读参数的候选；看到这条时优先进入手工体验状态清单，而不是回旧 formal/password 复跑。
   - Windows 开工第一屏现在还会消费 Mac 端上板的 `MacManualUx=` 摘要本体：同一条 `check-windows-resume-status --checkBoard --boardSummary` 会输出 `MacManualUx=status=ready|waiting|call-ready|calling ... next=SendManualUxCall ... callCommand=present`，但不会透传 `ManualUxCallCommand=` 原文，也不会自动发 call。看到 `status=call-ready` 时，先在通讯板确认用户在场、目标、安全边界和预计 5-10 分钟，再由 Mac 端显式发起手工体验 call；看到 `status=calling` / `next=WaitForManualUxConfirmation` 时，表示 Mac 已发起手工体验 call，等待 Windows/User 确认，不重复发 call；Windows/User 可发送 `MAC_MANUAL_UX_CONFIRMED` 确认当前窗口，Mac 摘要随后会进入 `status=ready` / `next=ManualUxTest`；看到 `status=calling` / `next=ReconfirmManualUxCall` / `manualUxCall=timeout` 时，表示旧窗口已超时，需要 Mac 端显式 `--reconfirmCall` 重新发起；看到 `waiting` 时继续等 Mac/User 信号，不回旧 formal/password 复跑。
   - `check-windows-resume-status --checkBoard --boardSummary` 在当前 `REAL_TEST_PASS` 后续 call 下会输出 `PostPassNext=WindowsRecordPassAndTailError+MacManualUxStandby` 和 `ManualUxChecklist=connection/video/audio/clipboard/file/window/fullscreen/original/copy-diagnostics`，不会再把主 `Next=` 指向 formal E2E `-PromptPassword` 复跑。只有普通 Mac ready / `MAC_READY_FOR_REAL_TEST` 场景才继续按原 `Next=` 进入正式复跑。
   - 同一当前 PASS 后续 call 下，`check-windows-resume-status --sendUserAuthRequest` 也会拒绝发送用户认证请求：JSON 里 `userAuthRequest=NO_USER_AUTH...`，`sentUserAuthRequest.ok=false`，且不会向 Agent Link Board 发 `NEED_USER_AUTH`。如果真的需要复跑正式 E2E，先由两端在通讯板确认新 call，不要沿用 PASS 后续 call 直接请求用户输入密码。
   - 如果后续最新 `MacHeartbeat=` 或 `MacResumeStatus=` 再次带 `MacPowerHealth=warning reason=system-sleep-enabled warnings=system-sleep-enabled,display-sleep-enabled`，说明 Mac 端已从当前 `Mac Unattended` 独立状态安全转述出电源细分风险；这不是 heartbeat/resume 自身失败，而是提醒真实值守前仍需现场处理系统睡眠、显示睡眠或网络唤醒设置。若同段还有 `MacUnattendedFreshness=stale`，先复制同屏 `MacUnattendedSendStatus=` 刷新独立值守证据，再决定是否处理 `pmset` 或 LaunchAgent；freshness 只说明证据新旧，不代表已修复或新增故障。该字段只读 Agent Link Board，不运行 `pmset`，不加载 LaunchAgent，不认证、不发密码/input/inject。
   - 如果后续最新 `MacHeartbeat=` 或 `MacResumeStatus=` 再次带 `MacUnattendedHealth=warning reason=launch-agent-not-loaded blockers=none warnings=launch-agent-not-loaded`，说明 Mac 端已从当前 `Mac Unattended` 独立状态安全转述出 LaunchAgent 值守风险；这不是 heartbeat/resume 自身失败。现场先按同屏 `MacLaunchAgentPlan=` 做只读规划，再由用户确认是否按 `MacHostStop=` -> `MacLaunchAgentLoad=` -> `MacLaunchAgentPrint=` -> `MacUnattendedFormal=` 处理和复查；Windows 端只提示/复制，不运行 Mac 脚本、不执行 `launchctl`、不认证、不发密码/input/inject。
   - 如果最新 `MacHeartbeat=` / `MacResumeStatus=` / `MacUnattendedStatus=` 带 `MacUnattendedHealth=warning reason=accessibility warnings=accessibility`，说明 LaunchAgent 后台 host 已在线但当前进程视角缺 Accessibility/Input Monitoring。`inputMode=log` 安全联调仍可继续；若要真实注入，必须先由用户在 Mac 本机确认权限或改用前台 `MacMaxFpsSafeStart=` 路径，并且仍需 `--confirmUserWatching`。
   - 如果 `MacClientFormalChecklist=` / `check-mac-client-formal-status --boardSummary` 同屏出现 `MacUnattendedFreshness=stale`，先刷新独立 `Mac Unattended` 状态，再继续 Mac 反控 Windows 的正式 checklist / smoke；这个字段只是提示值守或电源证据新旧，不改变 `readyToCall`、blocker 或 call 发送判定，也不代表脚本已经运行 `pmset`、加载 LaunchAgent、认证或发 input/inject。
   - 如果 `MacClientFormalSmoke=` / `run-mac-client-formal-smoke --boardSummary` 同屏出现 `MacUnattendedFreshness=stale`，可直接复制同屏 `MacUnattendedSendStatus=` 刷新独立 `Mac Unattended` 状态，再继续正式 smoke 或判断是否要进入电源/LaunchAgent 人工处理。该 smoke wrapper 只是透传 nested formal checklist 的安全结果，不请求密码、不认证、不发 call/input/inject，也不改变 ready/blocker 判定。
   - 如果 `MacClientDiscoverWindows=` / `discover-windows-hosts --checkBoard --boardSummary` 第一屏出现 `MacUnattendedFreshness=stale`，先刷新独立 `Mac Unattended` 状态，再继续 Mac 反控 Windows 的 formal checklist / smoke；这个字段只读通讯板安全短字段，不改变 Windows host 发现、`requireFound`、ready call 或反控授权命令，也不代表脚本已经运行 `pmset`、加载 LaunchAgent、认证或发 input/inject。
   - 如果 `MacClientDiagnostics=` / `check-mac-client-readiness --checkBoard --boardSummary` 第一屏出现 `MacUnattendedFreshness=stale`，先刷新独立 `Mac Unattended` 状态，再继续 Mac client readiness、Windows host probe 或 formal smoke；这个字段只读通讯板安全短字段，不改变 readiness blockers、call、formal checklist 或反控授权命令，也不代表脚本已经运行 `pmset`、加载 LaunchAgent、认证或发 input/inject。
   - 如果需要处理 `MacPowerHealth=warning`，先让 Mac 端跑 `node scripts/mac/plan-mac-power-settings.mjs --boardSummary`，或直接复制 `MacHeartbeat=` / `MacResumeStatus=` / `MacUnattendedStatus=` / `MacFormalE2E=` / `MacFormalLocalSmoke=` / `MacClientDiscoverWindows=` / `MacClientFormalChecklist=` 摘要里的 `MacPowerPlan=`。它输出 `pmset` 预览、`MacPowerApply=node scripts/mac/apply-mac-power-settings.mjs --apply --confirmUserPresent ...`、`PowerApply=Preview->ManualApply->Verify->MacUnattendedStatus->MacLaunchAgentPlan` 人工顺序、`pmset -g custom` 复查、`MacUnattendedStatus` 复查命令和 `MacLaunchAgentPlan=` 值守加载规划入口；`MacPowerPlan` 本身不执行 `pmset`、不提权、不改系统、不请求密码、不认证、不发 input/inject。真正改电源设置优先运行同屏 `MacPowerApply=`：它会先响铃，再弹 macOS 管理员授权框，成功后复查 `pmset`；若授权超时或用户没看到弹窗，系统设置不会改变，需要重新处理授权弹窗。授权成功后再按 `Verify` / `MacUnattendedStatus` / `MacLaunchAgentPlan` 刷新证据和继续处理 LaunchAgent。
   - 如果开工第一屏跑 `check-windows-resume-status --checkBoard --boardSummary`，并看到 `MacHeartbeatHealth=ok ... blockers=none warnings=none`，说明 Windows resume/status 已经读到 Mac 端稳定健康字段；如果看到 `MacHeartbeatHealth=blocked reason=mac-codex-stale` 或 `warning/failed/stale/unknown`，先按该健康字段的 `reason/blockers/warnings` 处理，而不是只看 `MacHeartbeatFreshness=fresh`。该字段只读通讯板、拒绝敏感候选，不会认证或发送 input/inject。
   - 同一开工第一屏现在也会直接输出 `MacPowerHealth=` 和 `MacUnattendedHealth=`：看到 `system-sleep-enabled,display-sleep-enabled` 就先按 Mac 电源/显示器睡眠风险处理，看到 `launch-agent-not-loaded` 就先处理 Mac LaunchAgent 值守，看到 `accessibility` 就先处理后台 host 权限视角或真实注入前台安全启动路径；这些字段优先来自通讯板当前 status，后面紧跟 `MacPowerPlan=` 或 `--promptPassword` 安全命令也不会污染解析。Windows 端只读展示，不运行 Mac 脚本、不执行 `pmset`、不加载 LaunchAgent、不认证、不发密码/input/inject。
   - 如果 Windows 本机 Mac 提醒 watcher 正在运行，最新 `MacHeartbeatHealth=warning/blocked/failed/stale/unknown` 会直接弹 Windows 本机提醒，适合远控窗口最小化时透传 Mac 端风险；`MacHeartbeatHealth=ok` 不会误弹。提醒正文只保留 `status/reason/heartbeat/blockers/warnings/checkedAt` 等稳定短字段，疑似 token/secret/password 或命令细节会被脱敏，不会回显原始敏感片段。
   - 同一个 Windows 本机 watcher 也会直接消费 `MacPowerHealth=` 和 `MacUnattendedHealth=`：系统睡眠/显示睡眠、LaunchAgent 未加载或值守 blocker 会弹短摘要提醒，但不会把 `MacPowerPlan=`、`MacUnattendedStatus=` 等整条命令或疑似 secret/token 打到弹窗正文里；同一个 `checkedAt` 的电源风险只提醒一次，避免 heartbeat 和 unattended 双重转述刷屏。需要执行修复时，仍让 Mac 端按通讯板或控制端诊断里的安全命令人工复制运行。
   - Windows 控制端页面、Mac 提醒区和复制/导出诊断也会消费 `MacPowerHealth=` / `MacUnattendedHealth=`：看到 `system-sleep-enabled,display-sleep-enabled` 会直接显示“系统睡眠未关闭 / 显示器睡眠未关闭”，看到 `launch-agent-not-loaded` 会显示“自启动未加载”；如果同段电源/睡眠 warning 里带 `MacPowerPlan=`，会额外显示“Mac 电源预案命令已提供”，如果带 `MacPowerApply=`，会额外显示“Mac 电源授权执行命令已提供”；但 `MacPowerHealth=ok`、`MacUnattendedHealth=ok`、单独 `MacPowerPlan=` 或单独 `MacPowerApply=` 不当风险。现场只需复制诊断给两端看，Windows 不会运行 Mac 脚本、不执行 `apply-mac-power-settings`、不执行 `pmset`、不加载 LaunchAgent、不认证、不发密码/input/inject。
   - 如果最新 `MacHeartbeat=` 摘要已经提示 stale/blocked/warning、Mac Codex stale/reconnect、旧 build 或非空 warning/blocker，并同时带 `MacHeartbeatRerun=`，Windows 控制端会显示“Mac 心跳复查命令已提供”。现场可以直接让 Mac 端复制这条 `check-mac-heartbeat --boardSummary` 复查命令刷新证据；如果只是 `MacHeartbeat=status=ok warnings=none blockers=none` 加 `MacHeartbeatOnce/Rerun/Watch/Start/Status/Stop=` 的干净命令清单，Windows 不再把它当成风险；Windows 端只提示/复制，不运行 Mac 脚本、不认证、不发密码/input/inject。
   - 如果 `MacHeartbeat=` 摘要出现 `warnings=mac-host-build-stale` / `restart recommended`，现在还会直接给出 `suggestedAction=restart-mac-host-safely actionCommands=MacHostStop->MacHostSafeStart-or-MacMaxFpsSafeStart->MacResumeStatus`。现场不要误以为脚本已经重启过；这只是只读建议。正式验收前按顺序让 Mac 端先停旧本机 host，再由用户授权安全前台启动，最后跑 `MacResumeStatus` / `MacHostMedia` 确认 build 和媒体基线。
   - 如果 `MacResumeStatus=` 摘要本身也出现 `suggestedAction=restart-mac-host-safely`，按同一条安全顺序处理：`MacHostStop` 停旧本机 host，用户本机授权 `MacHostSafeStart` 或 `MacMaxFpsSafeStart` 前台启动，再跑 `MacResumeStatus` 复查。它仍只是只读建议，不代表脚本已经停/启过 host，也不会请求密码、认证或发送 input/inject。
   - 如果 `MacUnattendedStatus=` / `MacUnattendedFormal=` 摘要本身也出现 `suggestedAction=restart-mac-host-safely`，按同一条安全顺序处理：先 `MacHostStop` 停旧本机 host，再由用户本机授权 `MacHostSafeStart` 或 `MacMaxFpsSafeStart` 前台启动，最后跑 `MacResumeStatus` 复查；正式长测前再按摘要里的 `MacHostMedia` 刷新 H.264/PCM 媒体基线。unattended 脚本只给出只读建议和复查入口，不会自动停/启 host、请求密码、认证或发送 input/inject。
   - 如果 Mac heartbeat/readiness 摘要里出现 `warnings=mac-host-build-stale`、`restart recommended`、`runtimeBuild=` 或 `hostRuntimeChanges=`，并同时给出 `MacHostStop=`，Windows 控制端 Mac 提醒区、值守快速摘要和复制/导出诊断会显示“Mac host 停止旧进程命令已提供”，原始诊断仍保留 `MacHostStop=` 命令。现场流程是先让 Mac 端或人工停止旧 host，再按 `MacHostSafeStart=` 安全前台启动到新 build；Windows 端只提示/复制，不自动停止或启动 Mac host。
   - 如果同一段 Mac heartbeat/readiness/unattended 摘要里还有 `MacUnattendedStatus=` 或 `MacUnattendedFormal=` 安全命令，且已有 warning/blocker、旧 build、重启建议、LaunchAgent/max-fps/fps-limit 等上下文，Windows 控制端会提示“Mac 值守状态命令已提供”“Mac 值守正式检查命令已提供”。现场可先让 Mac 跑普通值守状态或 formal 60Hz 强校验，再判断是否 stop/safe-start；Windows 端只提示/复制，不运行 Mac 命令、不认证、不发密码/input/inject。
   - 如果同一段摘要还带 `MacLaunchAgentLoad=` / `MacLaunchAgentPrint=`，且已有 warning/blocker、旧 build、`fps-limit`、`launch-agent-max-fps` 或 `loaded=false` 等上下文，Windows 控制端会提示“Mac LaunchAgent 加载命令已提供”“Mac LaunchAgent 打印验证命令已提供”。现场处理 60Hz LaunchAgent 切换时，可按 `MacHostStop=` -> `MacLaunchAgentLoad=` -> `MacLaunchAgentPrint=` -> `MacUnattendedFormal=` 的顺序让 Mac 端人工执行和复查；Windows 端只显示和复制，不运行 `launchctl`。
   - 如果同一段摘要还带 `MacHostReadiness=`，且已有 warning/blocker、Mac host 离线/不可达、旧 build、重启建议、`mac-host-max-fps` 或 `fps-limit` 等上下文，Windows 控制端会提示“Mac host 体检命令已提供”。现场需要补 Mac host 详细证据时，可直接让 Mac 端复制该 `check-mac-host-readiness --checkBoard --boardSummary` 命令；Windows 端只提示/复制，不运行 Mac 脚本、不认证、不发密码/input/inject。如果 `MacHostReadiness` 摘要自身也出现 `suggestedAction=restart-mac-host-safely`，按同一条安全顺序处理：停旧 host、用户本机授权安全前台启动、再跑 `MacResumeStatus` 复查。
   - 如果同一段摘要还带 `MacHostMedia=`，且已有 heartbeat/resume/unattended/formal 旧 build、重启建议、H.264/PCM、刷新率或非空 warning/blocker 上下文，Windows 控制端会提示“Mac 媒体基线命令已提供”。正式长测、卡顿/不是 60Hz 或声音问题复查前，可让 Mac 端复制该 `check-mac-host-readiness --probeMedia --probeMediaResourceSample --promptPassword --boardSummary` 命令刷新 H.264/PCM 基线；Windows 端只提示/复制，不运行 Mac 脚本、不认证、不请求或发送密码/input/inject。
   - 需要把当前 Mac 值守健康直接同步给 Windows watcher/resume/status 时，Mac 端可运行 `node scripts/mac/check-mac-unattended-status.mjs --host 127.0.0.1 --port 43770 --sendStatus --boardSummary`。这会以独立设备 `Mac Unattended` 上板 `MacUnattendedHealth=ok|warning|blocked ...`，不会覆盖 `Mac Codex` 的工作状态；默认不加 `--sendStatus` 仍只是本地只读检查，不认证、不请求密码、不发 input/inject。
   - 如果后台 `Mac Heartbeat` 一直在线但 `MacUnattendedFreshness=stale` 周期性出现，可用显式刷新版 watcher：临时刷新一次运行 `node scripts/mac/watch-mac-heartbeat.mjs --once --sendStatus --refreshUnattended --boardSummary`；白天确认要长期保持证据新鲜时，直接复制最新 `MacHeartbeat=` 或 `MacResumeStatus=` 同屏的 `MacHeartbeatRefreshRestart=`，它固定为 `node scripts/mac/start-mac-heartbeat-watcher.mjs --restart --refreshUnattended --boardSummary`，用现有 watcher 管理器重启为刷新模式。默认 watcher 不自动刷新，显式刷新路径只跑只读 `check-mac-unattended-status --sendStatus --boardSummary`，不请求密码、不认证、不发 call/input/inject、不执行 `pmset`、不加载 `launchctl`。
   - 开工第一屏跑 `check-mac-resume-status --checkBoard --boardSummary` 时，现在直接看 `MacHeartbeatRefresh=enabled|disabled|unknown`：`disabled` 表示后台 watcher 在线但不会持续刷新独立 `Mac Unattended` 证据；需要临时刷新就复制同屏 `MacHeartbeatRefreshOnce=`，需要长期保持新鲜就复制 `MacHeartbeatRefreshRestart=`，它等价于 `start-mac-heartbeat-watcher --restart --refreshUnattended --boardSummary`。这些命令仍只读状态和上板心跳，不认证、不请求密码、不发 input/inject、不执行 `pmset`、不加载 `launchctl`；只有人工显式运行 `RefreshRestart` 时才会重启 watcher。
   - 当前 Mac 后台 watcher 已实际处于刷新模式：`start-mac-heartbeat-watcher --status --boardSummary` 显示 `refreshUnattended=true`、`configMismatch=none`，`MacResumeStatus=` 显示 `MacHeartbeatRefresh=enabled` 时，不需要为了 freshness 再重启。只有看到 `configMismatch=refreshUnattended`、`MacHeartbeatRefresh=disabled`，或 `MacUnattendedFreshness=stale` 持续无法通过正常 heartbeat 刷新时，才复制 `MacHeartbeatRefreshRestart=` / `RefreshRestart=`；这仍不执行 `pmset`、不加载 `launchctl`、不认证、不发密码/input/inject。
   - 如果通讯板或 watcher 最新文本已经写明 `MacHostMedia 通过 passed=... media=ok`、`MacFormalLocalSmoke 通过 H.264/PCM/input-log` 或 `MacFormalE2E=status=ok readyToCall=true checklist=passed`，Windows 控制端会把它显示为“证据：Mac 媒体基线已通过 / Mac 本机短验收已通过 / Mac formal E2E 已就绪”，导出诊断里显示为“值守证据”。看到这类证据时先别按风险处理；只有同段另有 warning/blocker、stale、失败或授权问题时，才继续按风险摘要里的下一步排查。
   - 如果同一段摘要提供通用 `Evidence=` / `evidence=` 正向字段，例如 `Evidence=MacClientPageOnline,MacClientDiagnosticsOk,MacHostMediaOk`，Windows 控制端会把已知正向标签显示为值守证据；Mac heartbeat 现在会在自身干净 ok 且 Mac client 页面在线时输出 `Evidence=MacClientPageOnline`，在同次 `--checkBoard` 成功且页面标题匹配时追加 `MacClientDiagnosticsOk`，`MacClientPage=` / `start-mac-client --status --boardSummary` 也会在页面在线时直接输出同一页面标签。看到 `MacHeartbeat=blocked ... evidence=正在重新连接 5/5` 或 Mac client page offline 时仍按风险处理；它不会因为含 `evidence=` 就变成健康证据。
   - Windows 恢复总览也会把通讯板里的干净 `Evidence=` / `evidence=` 和独立稳定短标签汇总成 `MacEvidence=`，Windows 控制端会直接把这个稳定字段显示为值守证据：开工第一屏跑 `check-windows-resume-status --checkBoard --boardSummary` 时，如果看到 `MacEvidence=MacClientDiagnosticsOk,MacHostMediaOk,MacFormalLocalSmokeOk,MacClientPageOnline`，说明这些 Mac 端正向证据已经存在；如果同屏还有 warning/blocker，再按风险字段处理。Mac 端 `check-mac-resume-status --checkBoard --boardSummary` 现在也会把最新干净 heartbeat 证据转述成 `MacEvidence=MacClientPageOnline,MacClientDiagnosticsOk;`，方便两端用同一字段交叉确认。blocked heartbeat 的 `evidence=正在重新连接 5/5` 或失败片段里的 `MacClientDiagnosticsOk failed` 不会进入 `MacEvidence`。
   - 如果同一段摘要是干净结果，例如 `MacClientPage=status=online ... blockers=none warnings=none` 或 `MacClientDiagnostics=status=ok ... blockers=none warnings=none`，Windows 控制端会把它显示为“Mac client 页面在线 / Mac client 诊断已通过”值守证据。看到这类证据时不用再按页面离线或诊断失败处理；只有同段另有非空 warning/blocker、失败、离线或不可达时才继续按风险摘要排查。
   - Mac resume 摘要现在也会给出 `MacHostMedia=node scripts/mac/check-mac-host-readiness.mjs --host <host> --port <port> --checkBoard --probeMedia --probeMediaResourceSample --promptPassword --boardSummary`，JSON 字段为 `commands.macHostMediaCommand`，并保留旧 `commands.mediaReadinessBoardSummary`。现场准备正式 H.264/PCM 长测前，可让 Mac 端复制这条稳定标签刷新媒体基线；它只在人工显式运行时本地提示密码，不把密码发到通讯板，也不认证或发送 input/inject。
   - Mac formal E2E readiness 摘要现在也会给出 `MacHostMedia=node scripts/mac/check-mac-host-readiness.mjs --host <host> --port <port> --checkBoard --probeMedia --probeMediaResourceSample --promptPassword --boardSummary`，JSON 字段为 `commands.macHostMediaCommand`，并保留旧 `commands.mediaReadinessBoardSummary`。现场只看到 `MacFormalE2E=` 时，也可以直接让 Mac 端刷新正式长测前的 H.264/PCM 媒体基线；该摘要本身不运行媒体探针、不弹密码、不认证或发送 input/inject。
   - Mac formal E2E readiness 摘要现在也会转述当前 `Mac Unattended` 的 `MacPowerHealth=` 和 `MacUnattendedFreshness=`，并在同一屏给出 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary` / JSON `commands.macPowerPlanCommand`：如果同一行看到 `MacPowerHealth=warning reason=system-sleep-enabled warnings=system-sleep-enabled,display-sleep-enabled`，说明正式呼叫 Windows 前仍有系统睡眠/显示睡眠值守风险要现场处理；如果看到 `MacUnattendedFreshness=stale`，先刷新独立 `Mac Unattended` 证据再判断，不要把 stale 当作 formal E2E 新 blocker。这不是 formal E2E 自身失败，也不代表脚本已运行 `pmset` 或加载 LaunchAgent。该转述和预案入口只读 Agent Link Board，只输出 dry-run 处理方案，不认证、不请求密码、不发密码/input/inject。
   - Mac formal E2E readiness 摘要现在也会给出 `MacClientBrowserSelfTest=node scripts/mac/test-mac-client-browser-self-test-wrapper.mjs --boardSummary`，JSON 字段为 `commands.macClientBrowserSelfTestCommand`。现场只看到 `MacFormalE2E=` 且真实 Windows host 密码、LAN 或授权链路暂未就绪时，先让 Mac 端跑这条本地 mock browser 自测，排除 Mac client 页面、反控 UI、剪贴板和 input-log 流程退化；formal E2E readiness 摘要本身不请求密码、不认证、不发 call/input/inject。
   - 如果 `MacFormalE2E=` 摘要本身出现 `suggestedAction=restart-mac-host-safely`，按同一条安全顺序处理：`MacHostStop` 停旧本机 host，用户本机授权 `MacHostSafeStart` 或 `MacMaxFpsSafeStart` 前台启动，再跑 `MacResumeStatus` 复查。formal E2E readiness 仍只是只读建议，不会自动停/启 host、请求密码、认证或发送 input/inject。
   - 如果 `MacFormalE2E=` 摘要本身出现 `readyToCall=false`、warning/blocker，并同时给出 `MacClientBrowserSelfTest=`，Windows 控制端 Mac 提醒区、快速摘要和复制/导出诊断会提示“Mac client 本地 browser 自测命令已提供”。无人值守或真实 Windows host 密码/LAN/授权未就绪时，可先让 Mac 复制该本地 mock browser 自测入口排除 Mac client 页面退化；Windows 端只提示/复制，不运行 Mac 脚本。
   - 如果 `MacUnattendedStatus=` / `MacUnattendedFormal=` 或同一段 heartbeat 值守摘要已经有 warning/blocker，并同时给出 `MacClientBrowserSelfTest=`，Windows 控制端也会提示“Mac client 本地 browser 自测命令已提供”。这样窗口最小化或只看值守摘要时，也能先让 Mac 跑本地 mock browser 自测排除 Mac client 页面退化；Windows 端只提示/复制，不运行 Mac 脚本、不认证、不发密码/input/inject。
   - 如果同一段摘要带 `MacScriptHelp=`，并已有 `readyToCall=false`、失败、旧 build、stale 或非空 warning/blocker 上下文，Windows 控制端 Mac 提醒区、快速摘要和复制/导出诊断会提示“Mac 脚本 help 安全自检命令已提供”。现场修改过 Mac `.mjs` 后，可让 Mac 端复制这条统一 help 安全自检；Windows 端只显示和复制，不运行 Mac 脚本、不认证、不发密码/input/inject。
   - Mac host readiness 摘要现在也会给出 `MacResumeStatus=node scripts/mac/check-mac-resume-status.mjs --host <host> --port <port> --checkBoard --boardSummary` 和 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；Windows 侧只看到 `MacHostReadiness=` 时，也可以直接让 Mac 端回到完整恢复总览，或跑统一 help 安全自检，确认最近 Mac 脚本帮助路径不会误启动服务、弹密码、访问联络板、认证或发送 input/inject。
   - Mac LaunchAgent planner 摘要现在也会给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`、`MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary`、`MacResumeStatus=node scripts/mac/check-mac-resume-status.mjs --host 127.0.0.1 --port <port> --checkBoard --boardSummary` 和 `ManualApply=ManualWrite->ManualLoad->MacUnattendedFormal->MacResumeStatus`；Windows 侧只看到 `MacLaunchAgentPlan=` / `ManualWrite=` / `ManualLoad=` 时，也可以先让 Mac 端跑统一 help 安全自检，确认最近 Mac 脚本帮助路径不会误写 plist、加载 `launchctl`、启动 host、弹密码、访问联络板、认证或发送 input/inject，或复制只读电源预案处理系统睡眠/显示睡眠值守风险。准备由用户现场收口 LaunchAgent 时，按 `ManualApply` 的顺序人工执行和复查；该顺序只是标签，不会自动写 plist、加载 `launchctl`、执行 `pmset`、提权、请求密码、认证或发送 input/inject。
   - Windows 控制端现在也会消费 `MacLaunchAgentPlan=`：如果同段已有 `launch-agent-not-loaded`、`launch-agent-max-fps`、`fps-limit`、旧 build 或 warning/blocker，上方 Mac 提醒区和复制诊断会显示“Mac LaunchAgent 预案命令已提供”；如果只是单独 dry-run 命令清单，不会当成风险。Windows 端只提示/复制，不运行 Mac 脚本、不执行 `launchctl`、不认证、不发密码/input/inject。
   - Mac heartbeat 摘要现在也会给出 `MacResumeStatus=node scripts/mac/check-mac-resume-status.mjs --host <host> --port <port> --checkBoard --boardSummary`、`MacHostMedia=node scripts/mac/check-mac-host-readiness.mjs --host <host> --port <port> --checkBoard --probeMedia --probeMediaResourceSample --promptPassword --boardSummary`、`MacUnattendedSendStatus=node scripts/mac/check-mac-unattended-status.mjs --host <host> --port <port> --server <Agent Link Board> --sendStatus --boardSummary`、`MacLaunchAgentPlan=node scripts/mac/install-mac-host-launch-agent.mjs --launchAgentPath <LaunchAgent plist> --port <port> --boardSummary`、`MacMaxFpsSafeStart=node scripts/mac/start-mac-host.mjs --promptPassword --requirePassword --host 0.0.0.0 --port <port> --maxScreenFps 60`、`MacFormalE2E=node scripts/mac/check-mac-formal-e2e-status.mjs --host <host> --port <port> --boardSummary`、`MacClientFormalChecklist=node scripts/mac/check-mac-client-formal-status.mjs --discover --port 43770 --boardSummary`、`MacClientFormalSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --boardSummary`、`MacClientPromptPasswordSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --promptPassword --boardSummary`、`MacClientBrowserSelfTest=node scripts/mac/test-mac-client-browser-self-test-wrapper.mjs --boardSummary` 和 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；Windows 侧只看到最新 `MacHeartbeat=` 时，也可以直接复制这些命令回到完整 Mac 恢复总览、刷新 H.264/PCM 媒体基线、显式刷新独立 `Mac Unattended` 值守状态、查看只读 LaunchAgent 加载规划、获取前台 60Hz 安全启动指引、正式 E2E 只读 readiness、继续 Mac 控 Windows 的只读 formal checklist / 无密 preflight、在用户在场时进入前台密码真测、先让 Mac 端跑本地 mock browser 自测，或让 Mac 端跑统一 help 安全自检并得到一行无密摘要，不需要再翻旧交接文档。`MacLaunchAgentPlan=` 只打印 dry-run 规划，不写 plist、不加载 `launchctl`、不认证、不发密码/input/inject。
   - Mac unattended status 摘要现在也会给出 `MacHostMedia=node scripts/mac/check-mac-host-readiness.mjs --host <host> --port <port> --checkBoard --probeMedia --probeMediaResourceSample --promptPassword --boardSummary`、`MacResumeStatus=node scripts/mac/check-mac-resume-status.mjs --host <host> --port <port> --checkBoard --boardSummary`、`MacClientBrowserSelfTest=node scripts/mac/test-mac-client-browser-self-test-wrapper.mjs --boardSummary` 和 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；Windows 侧只看到 `MacUnattendedStatus=` / `MacUnattendedFormal=` 时，也可以直接让 Mac 端刷新媒体基线、复查完整恢复总览、先跑本地 mock browser 自测排除 Mac client 页面退化，或跑统一 help 安全自检。unattended 摘要本身不会误启动服务、弹密码、访问联络板、认证或发送 input/inject。
   - Mac client readiness 摘要现在也会给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；Windows 侧只看到 `MacClientDiagnostics=` 或 `check-mac-client-readiness --boardSummary` 时，也可以直接让 Mac 端跑统一 help 安全自检，确认最近 Mac 脚本帮助路径不会误启动服务、弹密码、访问联络板、认证或发送 input/inject。
   - Mac client readiness 摘要现在也会给出 `MacClientPromptPasswordSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --promptPassword --boardSummary`；Windows 侧或人工只看到 `MacClientDiagnostics=` 时，用户在场可直接复制这条进入真实 browser smoke。无人值守时不要运行这条，继续用同一摘要里的 `MacClientFormalSmoke=` 无密 preflight 或 `MacClientBrowserSelfTest=` 本地 mock 自测。
   - Mac client readiness 摘要现在也会给出 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary` / JSON `commands.macPowerPlanCommand`；Windows 侧或人工只看到 `MacClientDiagnostics=` 时，也能直接复制只读电源预案入口处理系统睡眠/显示睡眠值守风险。该入口不执行 `pmset`、不提权、不改系统、不请求密码、不认证、不发 input/inject。
   - 如果同一段摘要带 `MacClientPage=` / `MacClientDiagnostics=`，且 Mac client page/readiness 已经出现 offline、unreachable、failed、非空 warning/blocker、Windows host、认证、联络板、视频、build 或 repo 风险，Windows 控制端会提示“Mac client 页面状态命令已提供”“Mac client 诊断命令已提供”。现场先让 Mac 端跑页面状态或 readiness 一行摘要，再按需要粘贴 Mac client 页面“复制诊断”；Windows 端只提示/复制，不运行 Mac 脚本、不认证、不发密码/input/inject。
   - Mac client formal checklist 摘要现在也会给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；Windows 侧只看到 `MacClientFormalChecklist=` 或 `check-mac-client-formal-status --boardSummary` 时，也可以直接让 Mac 端跑统一 help 安全自检，确认最近 Mac 脚本帮助路径不会误启动服务、弹密码、访问联络板、认证或发送 input/inject。
   - Mac client formal checklist 摘要现在也会给出 `MacClientPromptPasswordSmoke=`；已知 Windows host 时是目标明确的 `run-mac-client-formal-smoke --host <Windows IP> --port <port> --ensureClient --promptPassword --boardSummary`，无 host 时是 `--discover --ensureClient --promptPassword --boardSummary`。只看到 `MacClientFormalChecklist=` 时，用户在场可复制这条进入真实 browser smoke；无人值守时继续用 `MacClientFormalSmoke=` 无密 preflight 或 `MacClientBrowserSelfTest=` 本地 mock 自测。
   - Mac client formal checklist 摘要现在也会给出 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary` / JSON `runPlan.commands.macPowerPlanCommand`；只看到 `MacClientFormalChecklist=` 或 `check-mac-client-formal-status --boardSummary` 时，也能直接复制只读电源预案入口处理系统睡眠/显示睡眠值守风险。该入口不执行 `pmset`、不提权、不改系统、不请求密码、不认证、不发 input/inject。
   - Mac client formal smoke 摘要现在也会给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；Windows 侧只看到 `MacClientFormalSmoke=` 或 `run-mac-client-formal-smoke --boardSummary` 时，也可以直接让 Mac 端跑统一 help 安全自检，确认最近 Mac 脚本帮助路径不会误启动服务、弹密码、访问联络板、认证或发送 call/input/inject。
   - Mac client formal smoke preflight 若已经选中 Windows host，会同时给出 `MacClientPromptPasswordSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --host <Windows IP> --port <port> --ensureClient --promptPassword --boardSummary`。准备进入真实认证时优先复制这条 wrapper；不要从 `Next:` 里手工改底层 `test-mac-client-browser --useEnvPassword` 子命令。运行该 wrapper 才会在 Mac 本机响铃并弹前台隐藏密码框，摘要生成阶段仍不认证、不请求密码、不发送 call/input/inject。
   - 如果只看到了 `MacResumeStatus=` 或 `MacHeartbeat=`，现在也可复制其中的 `MacClientPromptPasswordSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --promptPassword --boardSummary` 进入真实 browser smoke；它会先发现 Windows host 并确保本地 Mac client 页面，再响铃和弹前台隐藏密码框。无人值守或用户不在场时不要运行这条命令，继续只跑 `MacClientFormalSmoke=` 无密 preflight / `MacClientBrowserSelfTest=` 本地 mock 自测。
   - Mac 本机 formal local smoke 摘要现在也会给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；Windows 侧只看到 `MacFormalLocalSmoke=` / `RerunFormalLocalSmoke=` 或本机短验收失败摘要时，也可以直接让 Mac 端跑统一 help 安全自检，确认最近 Mac 脚本帮助路径不会误启动服务、弹密码、访问联络板、认证或发送 call/input/inject。
   - Mac 本机 formal local smoke 摘要现在也会给出 `MacClientPromptPasswordSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --promptPassword --boardSummary`；如果只看到 `MacFormalLocalSmoke=` / `RerunFormalLocalSmoke=`，用户在场可复制它继续进入 Mac 控 Windows 真实 browser smoke。无人值守时不要运行这条，继续只用 `MacClientFormalSmoke=` 无密 preflight 或 `MacClientBrowserSelfTest=` 本地 mock 自测。
   - Mac 本机 formal local smoke 摘要现在也会给出 `MacClientBrowserSelfTest=node scripts/mac/test-mac-client-browser-self-test-wrapper.mjs --boardSummary`；如果只看到 `MacFormalLocalSmoke=` / `RerunFormalLocalSmoke=`，无人值守或真实 Windows host 密码/LAN/授权未就绪时，先复制这条本地 mock browser 自测排除 Mac client 页面、反控 UI、剪贴板和 input-log 流程退化。
   - Mac 本机 formal local smoke 摘要现在也会给出 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary` / JSON `commands.macPowerPlanCommand`；如果只看到 `MacFormalLocalSmoke=` / `RerunFormalLocalSmoke=`，也能直接复制只读电源预案入口处理系统睡眠/显示睡眠值守风险。该入口不执行 `pmset`、不提权、不改系统、不请求密码、不认证、不发 input/inject。
   - Mac host readiness 摘要现在也会给出 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary` / JSON `commands.macPowerPlanCommand`；如果只看到 `MacHostReadiness=` 或 `check-mac-host-readiness --boardSummary`，也能直接复制只读电源预案入口处理系统睡眠/显示睡眠值守风险。该入口不执行 `pmset`、不提权、不改系统、不请求密码、不认证、不发 input/inject。
   - Mac Windows discovery 摘要现在也会给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary` 和 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary`；Windows 侧只看到 `MacClientDiscoverWindows=` 或 `discover-windows-hosts --checkBoard --boardSummary` 时，也可以直接让 Mac 端跑统一 help 安全自检，或复制只读电源预案处理系统睡眠/显示睡眠值守风险。该预案不执行 `pmset`、不提权、不改系统、不请求密码、不认证、不发 input/inject。
   - Mac Windows discovery 摘要现在也会给出 `MacClientPromptPasswordSmoke=`；发现到 Windows host 时带目标 host/port，未发现时给出 `--discover --ensureClient --promptPassword --boardSummary` fallback。用户在场时可复制它进入真实 browser smoke；无人值守时继续只跑同一摘要里的 `MacClientFormalSmoke=` 无密 preflight 或 `MacClientBrowserSelfTest=` 本地 mock 自测。Windows 控制端会在发现到 Windows host 的 discovery 摘要里提示“Mac client 前台密码真测命令已提供”，但不会运行 Mac 脚本或传递密码。
   - Windows 侧需要给 Mac 端或通讯板同步一次性反控授权入口时，优先使用 PowerShell 7 摘要命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/windows/allow-windows-reverse-control.ps1 -HostName 127.0.0.1 -Port 43770 -Grant -DurationMs 30000 -BoardSummary`；只读状态查询用同脚本 `-Status -BoardSummary`。恢复总览、Windows host `--status` 和 readiness 摘要会保留兼容的 `ReverseGrantPs=` / `ReverseGrant=`，同时都给出 Mac/Windows 统一稳定标签 `WindowsReverseGrantStatus=`、`WindowsOpenOneTimeReverseGrant=`、`WindowsReverseGrantStatusNodeFallback=`、`WindowsOpenOneTimeReverseGrantNodeFallback=`；恢复总览 `--checkBoard` 还会安全提取 Mac 端同名标签，只接受 Windows 本机回环地址、真实端口、`BoardSummary` 和无敏感参数候选。它们只提示/打开 Windows 本机回环临时授权窗口，不发送密码、不认证远端、不发送输入、不执行 `inject`。
   - Mac 反控 Windows 前若发现 Windows host 离线，优先复制 `start-windows-host --status --boardSummary` 里的 `start safely with ...`；该启动建议会保留当前 `--host` / `--port`，`--status --json` 也有 `safeStartCommand` 和 `ephemeralStartCommand` 可供桌面壳或通讯板自动化读取，避免自定义端口现场被默认 `43770` 覆盖。
   - Mac 真实 browser smoke 如果卡在认证，且 Windows host 是先前随机运行期密码启动的，不要在通讯板询问或发送密码，也不要尝试从日志/进程取回旧密码。直接看 `start-windows-host --status --boardSummary` 或 `check-windows-host-readiness --checkBoard --boardSummary` 里的 `WindowsSecureAuthPath=`：Windows 本机前台用 `node scripts/windows/start-windows-host.mjs --host 0.0.0.0 --port <port> --promptPassword --requirePassword` 重启 host，用户在 Windows 和 Mac 的隐藏密码提示里输入同一个临时密码，再让 Mac 端复跑 browser smoke。该流程只生成安全指引，不把密码放入命令参数/通讯板/日志。
   - Mac 端发现不到 Windows host 或现场怀疑局域网/防火墙挡住时，优先跑 `node scripts/mac/discover-windows-hosts.mjs --checkBoard --boardSummary` 或 `node scripts/mac/check-mac-client-readiness.mjs --checkBoard --boardSummary`；Mac 会从 Agent Link Board 安全消费 Windows readiness 上板的 `WindowsLanRisk=`。`node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --boardSummary` 现在也会把同一条线索透传给底层发现流程，发现失败时直接在摘要里显示脱敏 `WindowsLanRisk=`，不会继续弹密码或认证。也可直接看 Windows 侧 `node scripts/windows/check-windows-host-readiness.mjs --checkBoard --boardSummary`：`no-firewall-allow` 表示未发现 TCP 端口入站放行规则，`public-profile` 表示当前网络是 Public，`lan-probe-blocked` / `tcp-unreachable` 指向局域网探测不可达，`bind-address` 指向监听地址不适合 LAN，`none` 表示这类风险未命中。该摘要只读，不自动改防火墙、不认证、不发密码/input/inject。
   - Windows 端自己开工第一屏也可以直接跑 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --boardSummary` 或 PowerShell 包装入口读取同一条 `WindowsLanRisk=`；如果摘要显示 `no-firewall-allow,public-profile`，先把问题归到 Windows 本机防火墙/网络 Profile 排查，不要误判为 Mac client 第二步或 H.264 画面卡住。resume 只消费短标签，拒绝未知/命令样式/疑似敏感候选，不改系统设置。
   - 看到 `WindowsLanRisk=no-firewall-allow`、`public-profile`、`lan-probe-blocked` 或 Mac 端发现不到 Windows host 时，优先复制 Windows 摘要里的 `WindowsFirewallStatus=` 做只读 JSON 检查；需要预览 Private 入站放行规则时复制 `WindowsFirewallPreview=`。这两个命令默认只读或 dry-run，`WindowsFirewallPreview` 不带 `--addRule`，不要把它改成自动改系统的命令发到通讯板。Windows 控制端 Mac 提醒区、快速摘要和复制/导出诊断已经会在同段风险里识别这两个标签，并提示“只读检查命令已提供 / 放行预览命令已提供”，所以现场可以先看页面诊断再决定是否打开终端复制命令。
   - Windows 恢复总览也会默认给出同一条 `WindowsSecureAuthPath=`，并且 `--checkBoard` 会安全提取联络板上的 `WindowsSecureAuthPath=` / `SecureAuthPath=`；因此开工第一屏可直接跑 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --boardSummary` 或 PowerShell 包装入口确认这条路径。如果当前联络板 call 仍是 Mac 请求 Windows 协调安全认证，摘要会额外给出 `AgentCallNext=mac-confirm-secure-auth-path`，表示 Windows 侧已经提供本地隐藏输入同一临时密码的安全路径，下一步应由 Mac/人工确认路径并按需要清理 currentCall。Windows 控制端 Mac 提醒区和复制诊断现在会在认证/密码/失败/阻塞上下文里显示“Windows 安全认证路径已提供”，但不会自动运行、认证或发送任何密码/input/inject。
   - 同一行如果出现 `AgentCallAck=`，Windows 侧可复制这条 `codex-link-client send` 命令到通讯板回复 Mac；也可显式跑 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --sendAgentCallAck --json`，PowerShell 等价入口是 `scripts/windows/check-windows-resume-status.ps1 -CheckBoard -SendAgentCallAck -Json`。内容只说明 `WindowsSecureAuthPath` 已提供、请 Mac 本机确认安全路径后再清理 call，不包含密码，也不会自动 `clear-call`；若当前不是 secure-auth active call ready，脚本会拒绝发送。不要把 `WindowsSecureAuthPath` 改成带 `--password` 的命令。
   - Windows 开工第一屏现在也会给出并消费 `MacClientDiscoverWindows=`：`node scripts/mac/discover-windows-hosts.mjs --checkBoard --boardSummary`。Mac 控 Windows 真连前，如果 Mac resume/heartbeat/status 已把这条上板，Windows resume 会安全提取并优先展示；否则会输出默认安全入口。候选必须是 discover 脚本且同时带 `--checkBoard` / `--boardSummary`，含密码/token/secret、`--promptPassword`、`--sendCall`、`--forceCall` 或缺联络板读取/摘要参数的版本会被拒绝；这一步只做 Mac 侧只读发现 Windows host 和 `WindowsLanRisk=` 对齐，不认证、不发 call/input/inject。
   - Windows 浮窗提醒和 Windows 控制端复制诊断现在也会识别同一组 `MacClientDiscoverWindows=` / `WindowsLanRisk=` / `WindowsFirewallStatus=` / `WindowsFirewallPreview=`。如果 Mac 端发现 Windows host 失败，同时 Windows 侧摘要出现“Mac client Windows 发现命令已提供”“Windows 防火墙入站放行需检查”“Windows 当前网络是 Public”“Windows 防火墙只读检查命令已提供”“Windows 防火墙放行预览命令已提供”，先排查 Windows host 是否监听 `0.0.0.0:<port>`、当前网络是否为 Private、入站防火墙规则是否放行该端口，再让 Mac 端重跑 discover/formal smoke；不要先把它归因到 H.264、WebCodecs 或 Mac client 页面卡住。
   - Windows 开工第一屏现在也会给出并消费 `MacClientFormalChecklist=`：`node scripts/mac/check-mac-client-formal-status.mjs --discover --port 43770 --boardSummary`。Mac 控 Windows 真连前，如果 Mac resume/heartbeat 已把这条上板，Windows resume 会安全提取并优先展示；否则会输出默认只读发现入口。候选必须是 formal status 脚本且带 `--boardSummary`，含密码/token/secret、`--promptPassword`、`--sendCall`、`--forceCall` 或占位 host/port 的版本会被拒绝；这一步只做无密 checklist，不认证、不发 call/input/inject。
   - Windows 开工第一屏现在也会给出并消费 `MacClientFormalSmoke=`：`node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --boardSummary`。Mac 控 Windows 真连前，如果 Mac resume/heartbeat 已把这条上板，Windows resume 会安全提取并优先展示；否则会输出默认安全入口。它只让 Mac 端启动/复用本地 client 页面并做无密 discovery/formal preflight，不弹密码、不认证、不发 call/input/inject。
   - Windows 控制端本机文件发送重试链路已补旧清单响应/结果/进度保护：确认超时后点击“重新发送”时，如果旧 `transferId` 的 `clipboard_file_response`、`clipboard_file_result` 或 `clipboard_file_progress` 迟到，只写事件日志，不再覆盖当前“正在发送”“等待对端确认”或失败/重试状态；等待当前 transfer 对端结果期间，如果仍收到当前 `transferId` 的 `clipboard_file_progress`，会刷新确认等待保活时间，避免对端仍在接收大文件时误判确认超时；如果当前 transfer 在分块发送中途收到失败 result，也会立即停住、保留文件并切到“重新发送”。真机大文件测试时仍需观察旧 response、旧 result、旧 progress、当前 progress 保活、中途失败和重新发送按钮是否都保持同一套状态优先级。
   - Windows 控制端接收远端文件时，托盘、全屏/监看浮层剪贴板状态和复制/导出诊断现在会显示速度与预计剩余时间；速度优先用最近分块样本做滑动平均，样本不足时回退总平均。接收端会拒绝未在清单中的 `fileIndex`、不连续 `offset`、重复/错位分块和超过声明大小的分块，避免坏包误拼成成功文件；拒绝后会返回 `LAN011` 并提示让 Mac 重新复制。复制/导出诊断还会输出“远端文件建议”：接收超时/中断/坏分块/拒收时提示让 Mac 重新复制并检查连接，系统文件剪贴板写入失败时提示重试写入或打开临时目录取文件。Mac client 发送文件到 Windows 时已同步消费这类失败结果：显示 code/reason、保留文件选择并把按钮切成“重新发送”，重发成功后才清空选择；如果 45 秒内没有收到对端 `clipboard_file_result`，也会显示确认超时、保留文件并允许从头重发，旧 transfer 的迟到结果不会覆盖当前重发等待状态。Windows 本机发送文件时，顶部剪贴板状态和全屏/监看浮层也会显示已发/总量、百分比、速度和预计剩余时间；如果对端直接拒绝文件清单，Windows 端会立即显示对端拒绝原因并切到“重新发送”；未连接、剪贴板同步关闭、未选择文件或文件超过当前大小上限时手动发送文件也会直接提示“请先连接被控端”“已关闭”“未选择文件”或“文件过大”；Windows 桌面版还支持从资源管理器复制普通文件/压缩包后在远控窗口按 `Ctrl+V` 自动读取系统 `FileDropList` 并沿同一发送链路分块发送，未连接或剪贴板同步关闭时会直接提示同样原因，文件夹暂不递归发送；如果系统剪贴板里只有文件夹、原生读取不可用或读取失败，顶部状态和全屏/监看浮层会直接显示中文原因。本机分块发完后会显示等待对端确认，收到对端 `clipboard_file_result` 后会区分对端已写入系统文件剪贴板、已保存到临时目录、暂存在远端托盘或失败原因；手动选文件会等对端接受成功后再清空选择，对端失败、本机发送失败或等待对端确认超过 45 秒没有结果时都会保留文件并切到“重新发送”，超时后已经重新发送时旧 `transferId` 的迟到结果不会覆盖当前等待状态。复制/导出诊断已经把“本机发送文件”独立成一行，能同时保留接收远端文件状态和本机发送等待确认/确认超时/可重发状态；失败或超时时还会输出“本机发送建议”，提示点击“重新发送”，并在重复失败时让对端检查文件剪贴板能力、权限或磁盘空间。若报告里出现“剪贴板能力建议”，先按提示确认剪贴板开关、远端文字剪贴板权限/模式，或远端文件剪贴板能力；出现“文件/压缩包不能直接复制粘贴”时，Windows 控制端现在会在手动发送前本地拦截，不发 `clipboard_file_offer`、分块或完成消息，先使用远端文件托盘/临时目录作为临时路径，再安排对端文件剪贴板能力修复。大文件真测时继续观察两端速度/ETA、对端 result 提示、确认超时提示、资源管理器复制后 `Ctrl+V`、远端/本机两侧复制诊断建议和重发体验是否稳定；如果 ETA 仍明显跳变，再调样本窗口或显示策略。断点续传尚未实现，连接中断后仍需要让对端重新复制。
   - Mac client 复制/导出诊断现在也会输出“文件发送建议”：发送中或等待确认时提示保持连接和等待回执；对端失败、确认超时或可重新发送时提示点击“重新发送”，并让 Windows 端检查连接、文件剪贴板能力、权限或磁盘空间。发送中页面状态还会显示已发/总量、平均速度和预计剩余时间，复制/导出诊断的“文件剪贴板”会同步带出当前状态；真机大文件测试时重点观察 Mac 发送侧速度/ETA、Windows 接收侧速度/ETA 和对端确认结果是否一致。如果 Windows host 明确报告文件剪贴板不可用，Mac client 会本地拦截手动文件/压缩包发送，不发 `clipboard_file_offer`、分块或完成消息；现场粘贴 Mac 诊断到通讯板时，可以先看这一行决定是立即重发、等对端回执，还是让 Windows 端排查文件剪贴板能力或临时改用其他传输方式。
   - Windows host 在线时，`start-windows-host --status --boardSummary` 会同时给出 `MacClientFormalChecklist=node scripts/mac/check-mac-client-formal-status.mjs --host <Windows IP> --port <port> --boardSummary`、readiness 命令和 ready 后的 `--sendCall` 命令；Mac 控制 Windows 真连前，优先复制这个统一标签对应的正式清单入口，确认 ready 后再让 Mac 端发 call。
   - 需要先确认本机 WGC/WinRT/GPU 和浏览器 H.264 解码条件时，不必从 resume 总览里翻命令：`start-windows-host --status --boardSummary` 和 `check-windows-host-readiness --boardSummary` 也会直接给出 `WindowsWgcSupport=` / `WindowsWgcSupportPs=` 以及 `WindowsWebCodecs=` / `WindowsWebCodecsPs=`，先跑专项预检，再按需继续 `WindowsWgcBenchmark=` / `WindowsWgcCompare=`。
   - 修改任意 Windows PowerShell `.ps1` 入口后，先跑 `node scripts/windows/test-windows-powershell-help.mjs --timeoutMs 10000 --boardSummary` 和 `node scripts/windows/test-windows-powershell-help.mjs --shell pwsh --timeoutMs 10000 --boardSummary`。这条统一自检会自动发现带 `Help` switch 的 PowerShell 脚本，并确认 `-Help/-h` 只打印说明，不误启动 host/watcher/Agent Link、不改机器环境、不初始化 WASAPI/采集系统声音、不认证、不发送密码/Token/input/inject；当前覆盖 20 个入口、40 条命令，也包括 `test-windows-host.ps1`、`dev-lab.ps1`、`test-windows-client-browser.ps1`、`check-webcodecs-h264-support.ps1`、`check-windows-wgc-support.ps1`、`benchmark-windows-wgc-settings.ps1` 和 `compare-windows-wgc-h264-sources.ps1` 这类容易误启动本机服务或浏览器/系统探针的入口；`--boardSummary` 可直接输出一行无密结果给 Agent Link Board。
   - 真机联调前优先用 Windows 桌面版“刷新设备”自动扫描同网段 `/discovery`；命令行可用 `node scripts/windows/discover-lan-hosts.mjs --boardSummary --requireMacHost` 快速确认当前 Mac IP，并直接得到 `MacFormalLocalSmoke=` 本机短验收、formal E2E 预检、授权提醒、ready 后自动发送授权提醒和正式验收命令。已知 IP 时用 `--noLocalSubnets --host 192.168.31.122 --port 43770` 可避免扫整段局域网。发现摘要现在会附带 Mac host runtime `buildDiff`：若只是 `stale metadata only, hostRuntimeChanges=0` 可继续无密预检；若显示 `restart recommended`，正式长测前先请 Mac 端重启 host。2026-06-16 真实 Mac host `192.168.31.122:43770` / runtime build `c5e5009` 已通过完整 Windows formal Mac E2E：H.264/WebCodecs、音频、文本/文件剪贴板、input-log 和 Windows 控制端诊断均 OK；formal E2E 未执行 `inject`。之后真实 safe inject 小验收已单独通过，后续复跑仍必须先由用户明确确认正在看 Mac 屏幕，并由 Mac 端用 `--confirmUserWatching` 启动 inject host。
   - 保持诊断状态条准确显示真实视频、模拟回退、Mac host runtime、权限、输入注入和剪贴板状态。
   - 保持顶部“输入事件”状态能直说当前输入模式：`inputMode=log` 必须提示“安全日志，不会真正控制”，真实注入要显示“真实控制 / 已注入”，拒绝要显示错误码；后续改输入 UI 或诊断条时先跑 `node scripts/windows/test-windows-client-browser.mjs --diagnosticsOnly --timeoutMs 45000`。
   - 恢复开工或现场要快速同步 Windows 控制端页面状态时，优先跑 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --boardSummary`，PowerShell 等价入口是 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-resume-status.ps1 -CheckBoard -BoardSummary`；摘要会直接从 Agent Link Board 最近状态/消息里提取安全的 `MacHostSafeStart=`，如果 Mac host 离线或需要重启，可复制其中的 `node scripts/mac/start-mac-host.mjs --promptPassword --requirePassword --host 0.0.0.0 --port <端口>` 让 Mac 端安全前台启动，脚本会拒绝带 `--password`、token、secret 或 `<当前端口>` 这类占位值的候选命令。摘要也会给出 `MacHeartbeat=check-mac-heartbeat --checkBoard --boardSummary`，用于让 Mac 端发布带 `checkedAt=`、`boardUpdatedAt=`、Mac Codex `updatedAt=` 和 `ageMs=` 的独立心跳/watchdog 摘要；若联络板当前已经有 `MacHeartbeat=` 状态，恢复总览还会直接输出 `MacHeartbeatFreshness=fresh|stale checked=<秒> codex=<秒> board=<秒> checkedAt=<时间>`，并取最新 `checkedAt` 的那段，避免旧事件覆盖新状态；看到 `stale` 时先让 Mac 端跑 `MacHeartbeatOnce=` 或 `MacHeartbeatStatus=`。摘要还会默认给出 `MacHeartbeatOnce=` / `MacHeartbeatWatch=` / `MacHeartbeatStart=` / `MacHeartbeatStatus=` / `MacHeartbeatStop=`，分别用于让 Mac 端发一次心跳、直接前台持续 watcher、后台启动 watcher、查看后台状态或停止后台 watcher，若 Mac resume 已经上板则优先使用 Mac 自己给出的同名命令。`MacHeartbeatStatus=` 现在会只读后台 stdout 日志尾部并回显 `lastHeartbeat=status/checkedAt/reason/codexAgeMs` 与 `lastRun/post`，没有日志时显示 `lastHeartbeat=not-seen`。这些后台入口仍以独立设备 `Mac Heartbeat` 上板，不刷新或伪装 `Mac Codex`，不认证、不请求密码、不发送 input/inject。同时会给出并提取 `WindowsReverseGrantStatus=` / `WindowsOpenOneTimeReverseGrant=` 及 Node fallback。如果 Mac 端刚报 `LAN008` 或等待 Windows 临时授权，先复制 `WindowsReverseGrantStatus=` 看当前状态，再复制 `WindowsOpenOneTimeReverseGrant=` 打开约 30 秒一次性授权并让 Mac 重试。摘要里的 `WinClientPorts=` 会先告诉你第二步默认端口 `5197/9337` 是否空闲，如果显示 `occupied(...;stale-diagnostics)`，先用摘要里的 `WinClientDiagnosticsAlt=` / `--clientPort 5200 --debugPort 9340` 备用命令复查，PowerShell 可直接加 `-ClientPort 5200 -DebugPort 9340`，或者手动确认后再关闭旧诊断进程。`WinClientDiagnostics=` 已直接带 `--boardSummary`，可先发一行无密页面诊断，再按需点页面或全屏浮层“复制诊断”粘贴完整快速摘要，报告里会保留 Mac 主机诊断、Mac 值守可远程推断、独立剪贴板状态、独立视频状态、普通声音接收/播放摘要、输入模式/ack，以及当前全屏浮层的连接、视频、声音、剪贴板、输入和安全状态。Mac 值守当前代表 Windows 侧能从连接、发现、重连和提醒 watcher 推断的状态；如果 watcher 状态或诊断文本已经带 `MacUnattendedStatus`、`MacResumeStatus`、`warnings=` / `warnings:`、`blockers=` / `blockers:`，控制端会把短标签翻译成中文风险，包括自启动、电源睡眠、LaunchAgent 刷新率上限、视频链路、运行版本、认证/密码、Windows 被控端、仓库状态、Mac 刷新率上限和 Mac host 刷新率上限；如果同一段 Mac readiness 还带 `MacHostSafeStart=`，快速摘要会提示“Mac host 安全启动命令已提供”，方便离线时直接让 Mac 端复制安全启动命令。更完整的 LaunchAgent、自启动、锁屏/睡眠可达性还要以 Mac readiness/status 上报为准。
   - Mac 端 `check-mac-resume-status --boardSummary` 第一屏也会直接显示 `heartbeatWatcher=running|not-running` 和 `lastHeartbeat=status/checkedAt/reason/codexAgeMs`，可和 Windows 恢复总览里的 `MacHeartbeatFreshness=` 交叉判断 watcher 是否在跑、心跳是否过旧。
   - 如果同一份恢复摘要里出现 `MacMaxFpsSafeStart=`，它代表 Mac 端已经提供可复制的 60Hz 前台安全启动命令；Windows 提取时会要求真实数字端口和 `--maxScreenFps`，拒绝 `<当前端口>` 占位或含密码/token/secret 的候选。现场要解决远端上限 30Hz 时，优先让 Mac 端复制这条前台命令，或按 `MacMaxFpsPlan=` 写入 LaunchAgent；如果 plist 已是 60 但 `loaded=false`，现在可直接按 Mac resume/unattended/formal E2E 摘要里的 `MacHostStop=` -> `MacLaunchAgentLoad=` -> `MacLaunchAgentPrint=` -> `MacUnattendedFormal=` 顺序人工切换并复查。上述摘要只给可复制命令，不自动停止 host、不运行 `launchctl`、不认证、不发密码/input/inject。
   - 需要让 Mac 端补更完整的 host readiness 证据时，直接复制 Windows 恢复摘要里的 `MacHostReadiness=`：`node scripts/mac/check-mac-host-readiness.mjs --host <Mac IP> --port <port> --checkBoard --boardSummary`。它和 `MacUnattended=` / `MacUnattendedFormal=` 分开，默认无密低风险，不跑媒体长探针、不认证、不发 input/inject；摘要还会给出 `MacPowerPlan=` 只读电源预案入口，方便只拿 host readiness 第一屏时处理系统睡眠/显示睡眠值守风险。
   - 正式长验收前需要 Mac 本机先短验 H.264/PCM/input-log 时，直接复制 Windows 恢复摘要、LAN 发现摘要或 formal preflight 摘要里的 `MacFormalLocalSmoke=`：`node scripts/mac/check-mac-formal-local-smoke.mjs --host <Mac IP> --port <port> --promptPassword --boardSummary`。恢复总览也会从联络板最近摘要里安全提取同名命令，含 `--password`、token/secret 或 `<当前端口>` 占位的候选会被拒绝；发现/预检路径只生成命令，不认证、不发送密码、不触发 input/inject。
   - 如果远控窗口最小化或等待 Mac 授权时只想看风险，不必先复制诊断：Windows 桌面版“本机被控 -> Mac 提醒”状态行会直接显示最近提醒和中文风险摘要；看到“视频链路需检查/运行版本需检查/认证密码待确认/Windows 被控端未就绪/仓库状态需检查/Mac host 发现需检查/联络板当前呼叫需协调/联络板状态需检查/Mac host 安全启动命令已提供/Mac 60Hz 安全启动命令已提供/Mac client 正式清单命令已提供/Windows 一次性反控授权命令已提供”等字样时，再复制诊断或让 Mac 端跑对应 readiness/status 摘要。
   - 真实连接时同时观察“实收 FPS”“协商 Hz”“远端上限”和“帧延迟”：帧延迟来自 `video_frame.timestamp` 接收年龄，可帮助区分采集/编码/网络/解码卡顿；普通诊断条和全屏悬浮控制中心展开后都会显示当前视频链路、低于协商/请求刷新率提示、远端 `maxScreenFps` 上限、帧延迟或回退原因，浮层还会显示实收/协商/请求刷新率；现场觉得“没有 60Hz”时先截图或复制诊断看这一行，如果写着“远端上限 30 Hz”，先到 Mac host 启动参数或 LaunchAgent 里确认 `LAN_DUAL_MAX_SCREEN_FPS`；命令行正式预检现在也会在摘要里写 `FpsLimit requested=60Hz remoteMax=30Hz`，并给出 Mac 端 dry-run 的 `MacMaxFpsPlan=install-mac-host-launch-agent --maxScreenFps 60 --boardSummary` 和写入/重启后的 `MacUnattendedFormal=check-mac-unattended-status --requireLaunchAgentMaxFps --requireLaunchAgentLoaded --boardSummary` 强校验命令，先让 Mac 端规划/重启上限，再用强校验确认 blocker 消失后做长测，不要先归因到 Windows 控制端卡住；若显示“时钟偏差”，先校准两端系统时间再判断延迟。
   - 继续验证 Mac 真实 `pcm-f32le-base64` 音频帧播放稳定性，重点看静音、音量变化、长时间运行和延迟；全屏悬浮控制中心展开后会显示声音接收帧数、电平、播放计数、音量和丢帧，复制诊断报告也会写入普通声音接收/播放/音量/电平/丢帧/错误摘要。现场听不到声音时先看它是“等待音频”“已接收，等待播放”“正在播放”还是“播放失败”。
   - Windows 控制端远端文件托盘已补接收中、分块进度、超限拒绝、解析失败、不完整完成、连接中断和 45 秒无新分块/完成消息的超时状态条；后续真实文件/压缩包复制时，除了看事件日志，也要直接看“远端文件”面板状态和全屏悬浮控制中心的“剪贴板”状态是否同步给出接收进度、写入系统剪贴板结果和可执行恢复动作。
   - 黑边输入防护已固化到 Windows 控制端页面级自检；后续改缩放、画布或输入层时保持该回归。
   - 处理真实 Mac 连接中的中文错误提示和重连体验；当前控制端已补自动重连倒计时、“立即重连”按钮，并在全屏悬浮控制中心显示连接/重连倒计时和浮层“立即重连”入口，导出日志也会记录重连原因和下次重连倒计时，报告顶部“快速摘要”会先汇总远端连接和重连状态；下一步重点看真实断网/host 重启后的画面恢复时间和错误提示是否足够直观。
   - 现场跑 `check-mac-formal-e2e --promptPassword` 或底层 `probe-mac-host --durationMs ...` 长测时，先看 runner 打印的 `Starting plan 1/2` / `Starting plan 2/2`：Plan 1 的 H.264 首帧确认后还会继续做长视频观察，再做音频观察，Plan 2 才是 Windows client 浏览器 H.264 canvas 检查。视频/音频观察会先打印目标时长，并默认每 10 秒输出一次进度心跳；如果长测中没有看到心跳，优先检查脚本是否已更新到包含 `--progressIntervalMs` 的版本。正式 E2E 的第二步 `test-windows-client-browser` 现在也会收到同一个心跳参数，等待连接、H.264 canvas 或 FPS 诊断时会输出页面快照；现场需要更密集反馈时加 `--progressIntervalMs 5000`，不需要时传 `0`。如果终端停在隐藏密码输入，脚本现在会先打印“等待隐藏密码输入：请直接在当前终端窗口输入 Mac 端当前临时密码；输入时不会显示字符，按 Enter 继续；这是正常等待，不是卡住；不要输到网页或通讯板。”，随后显示“当前终端输入 Mac 临时密码（输入不显示，回车继续）: ”；这时就在这个黑色终端里输入密码并回车即可，密码不会显示在终端或命令参数里，也不要发到通讯板。
   - 复核第二步时要区分 `expected` 和 `per-wait timeout`：`expected=5m 30s` 这类字段是整段视频/音频观察预计时长，`per-wait timeout=45s` 只是某一帧、音频帧或回执消息的最长等待时间，不代表整步 45 秒就该结束。现在 runPlan 会给 Plan 1 也打印 `Plan 1 hint:`，提醒 `First H.264 frame` 之后仍会继续长观察；自动化可读 `runPlan.steps[].troubleshootingHints[]`，人工看终端时优先看这些 hint。
   - 如果第二步看起来停住，先看同一段输出里的 `Plan 2 hint:`：它会提醒等待的是 Windows client 页面/WebSocket/H.264 canvas/FPS 诊断，并把四类常见误判点摆出来：`progress snapshots` 是否还在刷、`WinClientPorts=occupied(...;stale-diagnostics)` 时改用 `--clientPort 5200 --debugPort 9340`、`WindowsLanRisk=no-firewall-allow,public-profile` 时先查 Windows 防火墙/网络 Profile、`remoteMaxFps` 低于请求 Hz 时先让 Mac 端提升 host/LaunchAgent 上限。现在 `check-windows-resume-status` 在检测到旧诊断端口占用时，`Next=`、`FormalChecklist=` 和正式授权/运行命令会自动优先使用备用端口；如果摘要还有 `WinClientPortsOwners=9337:msedge.exe:<PID>` 这类字段，可直接按端口/进程/PID 判断是旧调试浏览器还是旧本地页面，但不要从通讯板寻找完整 command line。现场仍卡住时优先复制摘要里的新命令，不要回跑旧的 `5197/9337`。自动化脚本可读 `runPlan.steps[].troubleshootingHints[]`，不用解析长日志。
   - 如果把 `check-windows-resume-status --boardSummary` 或页面诊断贴进 Windows 控制端事件日志/复制诊断，控制端现在会把 `WinClientPorts=occupied(...;stale-diagnostics)`、`WinClientDiagnosticsAlt=` 和 `WinClientPortsOwners=` 翻译成中文快速摘要：先按备用端口命令复跑，再按 owner 摘要决定是否手动关闭旧页面/旧调试浏览器；`WinClientPorts=free` 不会误报。

3. 继续 H.264 流式视频链路验收。
   - Mac 真机已通过 `--requireH264` 首帧强校验，Windows 控制端页面级 `--requireH264` 也已验证真实 WebCodecs 解码；下一步继续观察延迟、长时间稳定性和 JPEG 回退体验。
   - Mac host 已增加 H.264 启动 5 秒 watchdog；若启动阶段卡住会回退 `background-jpeg` 并带 `streamFallbackReason`，Windows 控制端诊断条已能显示该原因；后续需要在主 43770 单 host、动态画面和 Windows 控制端真实连接下继续确认正常 H.264 不被误回退。
   - Windows host `ffmpeg-h264` 模式已能尊重 Mac client 的 MJPEG/JPEG fallback 请求；当浏览器拒绝当前 H.264 `codecString` 时，Mac client 会请求 `preferredVideoCodec=mjpeg` / `preferredVideoEncoding=data-url`，Windows host 会把当前会话切到 `windows-ffmpeg-gdigrab-mjpeg`，避免无画面。Windows 端能力体检已确认 FFmpeg 硬编、WGC 和浏览器解码条件；当前 `ffmpeg-h264` 过渡路径也支持 `--h264Encoder h264_nvenc` 并已通过 H.264 输出、Mac client `binary-h264` 和视频传输矩阵 4/4 回归。WGC H.264 桥接已完成 JPEG bridge、raw-bgra binary bridge 和 NV12 binary bridge 三条路径；NV12 bridge 真实 helper + `h264_nvenc` 在 `1280x720` 2.2 秒收到 67 帧、约 30.06 FPS，pipeline=`windows-wgc-helper-nv12-ffmpeg-h264`，并已通过 Mac client 页面级可选矩阵 case `wgc-nv12-h264`：1080P 约 32.6 FPS。`observe-windows-host-video` 和 `benchmark-windows-wgc-settings` 已新增 `freshFps`、`uniqueHelperFps`、`repeatedFramePercent` 和对应阈值；最新静态桌面 60Hz/20M/NV12/`h264_nvenc`/repeat-full 短测会话协商 60Hz，但实收约 22.94 FPS、真实新帧约 3.93 FPS、唯一源帧约 4.59 FPS、重复帧 82.9%，后续判断 60Hz 观感必须同时看这些指标。`benchmark-windows-wgc-settings --motionStimulus` 可打开 WinForms 或 browser 动画窗口做动态桌面对照；当前 WinForms/browser 短测仍只有约 4.9-5.0 uniqueHelperFps，说明下一步应优先查 helper 读回/事件节奏和原生硬编，而不是继续假设只是桌面静态。视频传输矩阵默认单 case 失败后会重试 1 次并换新临时端口，减少浏览器/host 释放慢导致的误报；需要严格复现首次失败时加 `--retries 0`。下一步不要再深调 JSON raw，优先做 helper 原生硬编、helper 内部采集节奏，或让 Mac client 真连观察 WGC NV12 H.264 的画质、延迟和资源。后续 H.264 改动仍先跑 `node scripts/windows/check-windows-video-encoder-support.mjs --requireAnyH264 --requireHardwareH264 --requireWgc --requireWebCodecsH264`，PowerShell 等价入口是 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-video-encoder-support.ps1 -RequireAnyH264 -RequireHardwareH264 -RequireWgc -RequireWebCodecsH264`；再跑 `node scripts/windows/check-webcodecs-h264-support.mjs --requireCodec avc1.42C02A`、`node scripts/windows/test-windows-h264-mode.mjs --h264Encoder h264_nvenc` 和 `node scripts/windows/test-mac-client-video-transports.mjs --h264Encoder h264_nvenc`；涉及 WGC NV12/H.264 时再加 `--case wgc-nv12-h264` 或 `--includeWgcNv12`；若矩阵某项失败，再用 `test-mac-client-browser --screenMode ffmpeg-h264 --requireH264Video`、`--expectBinaryH264Video --h264Encoder h264_nvenc`、`--screenMode ffmpeg-h264 --requireH264Video --disableBinaryVideo`、`--expectH264Fallback`、`--expectWgcNv12H264Video` 或 `--disableWebCodecs` 拆开定位。
   - WGC helper 内部阶段耗时已能通过 `helperTimingMs` 透传并汇总；`scripts/windows/compare-windows-wgc-h264-sources.mjs` 现在可同条件自动对照 raw-bgra 与 NV12 两条 WGC H.264 源格式。最新真实 720p/60Hz/20M/NV12/`h264_nvenc` 短测显示 helper 整帧平均约 127.5ms，`convertEncodeMs` 平均约 126.3ms，`mapMs` 约 0.6ms，`copyResourceMs` 约 0.004ms；`1280x720`、30Hz、10M、repeat-full、`h264_nvenc`、1.2 秒 raw-bgra vs NV12 对照显示 NV12 胜出，FPS 约 `23.03 vs 15.58`，helper frame avg 约 `68.805ms vs 72.086ms`，convert avg 约 `67.741ms vs 70.696ms`。所以下一步性能优化优先减少 CPU readback/转换/缩放：考虑 SIMD/多线程转换、D3D/GPU shader 转 NV12，或更进一步做 helper 原生 D3D11/NVENC 硬编。
   - JPEG 链路继续保留为兜底和权限调试。
   - Windows 控制端继续显示实收 FPS、协商帧率、请求帧率、远端 `maxScreenFps` 上限和帧延迟。

4. 共享协议只做必要变更。
   - 任何协议字段变更都必须同步 Swift、Windows 控制端、Windows 被控端、假 Mac 服务和探针脚本。
   - 协议兼容性不确定时，优先新增字段，不直接删除旧字段。

## Mac Codex 可接任务

- 每次恢复工作、准备向 Windows 端发状态或准备正式密码验收前，先跑 `node scripts/mac/check-mac-resume-status.mjs --checkBoard` 做轻量总览：它只读检查当前 git/联络板/Mac host `/discovery`/权限/LAN 地址/旧 build runtime 源码差异，并给出下一步建议，不启动服务、不认证、不要求密码、不发送输入事件。`--checkBoard` 会结构化读取 Agent Link Board `currentCall`，JSON/普通输出/`--boardSummary` 会显示 `call=active/done/none`；如果看到 `call=active(...)`，先响应或协调该呼叫，再启动新的正式验收。需要直接发通讯板时用 `--boardSummary` 输出一段秘密安全摘要，避免手工拼错 host、权限、buildDiff 或下一步；摘要会明确提醒 inject 启动必须 `--confirmUserWatching`，且不会回显 call command。恢复总览现在还会给出 `MacFormalLocalSmoke=node scripts/mac/check-mac-formal-local-smoke.mjs --host <host> --port <port> --promptPassword --boardSummary`、`MacFormalE2E=node scripts/mac/check-mac-formal-e2e-status.mjs --host <host> --port <port> --boardSummary`、`MacClientPage=node scripts/mac/start-mac-client.mjs --status --boardSummary`、`MacClientDiagnostics=node scripts/mac/check-mac-client-readiness.mjs --probeClientServer --checkBoard --boardSummary`、`MacClientFormalChecklist=node scripts/mac/check-mac-client-formal-status.mjs --discover --port 43770 --boardSummary`、`MacManualUxStatus=node scripts/mac/check-mac-manual-ux-status.mjs --boardSummary`、`CopyDiagnostics=Mac client 事件日志点击“复制诊断”` 和 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`；现场准备正式呼叫 Windows 前，先按 `MacFormalLocalSmoke=` 做本机 H.264/PCM/input-log 短验收并直接得到一行上板摘要，再按 `MacFormalE2E=` 做只读 formal readiness 摘要，确认 ready 后才决定是否另行 `--sendCall`；已经处于 `REAL_TEST_PASS` 后手工体验阶段时，直接按 `MacManualUxStatus=` 确认 `ManualUxTest` 清单，不回旧 formal/password 复跑。需要同步 Mac 控制端页面状态时，再发 `MacClientPage=` 一行确认页面是否在线，随后发无密 readiness 摘要并按需粘贴页面复制诊断。需要走 Mac 控 Windows formal 人工真连清单时，可直接复制恢复总览里的 `MacClientFormalChecklist=` 自动发现 Windows host 并输出 `ManualChecklist=`。如果只是确认本地 Mac client 页面是否在线，也可直接跑 `node scripts/mac/start-mac-client.mjs --status --boardSummary`，摘要同样会给出无占位 `MacClientFormalChecklist=check-mac-client-formal-status --discover --port 43770 --boardSummary` 和 `CopyDiagnostics=`，离线时提示先启动页面、在线后再复制诊断。修改 `scripts/mac/*.mjs` 后也按摘要里的 `MacScriptHelp=` 跑统一 help 安全自检，确认 `--help/-h` 不误启动服务、弹密码提示、访问 Agent Link Board、认证或发送 input/inject，并得到可直接发通讯板的一行摘要。需要脚本消费时加 `--json`；需要在自动化里把离线 host、工作区未清理或运行源码变化变成失败时分别加 `--requireOnline`、`--requireClean`、`--requireNoRuntimeChanges`。如果报告 Mac host 离线，正式端到端验收前用 `node scripts/mac/start-mac-host.mjs --promptPassword --requirePassword` 安全启动；如果只是 build 元数据落后且 `changedHostRuntimeFileCount=0`，通常可先继续 discovery/runtime 协调，部署强验收再重启。
- 只看 `MacClientPage=` / `start-mac-client --status --boardSummary` 时，现在也会看到 `MacClientPromptPasswordSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --promptPassword --boardSummary` 和 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary`。用户在场并准备输入 Windows 连接密码时可复制 prompt-password smoke 进入真实 browser smoke；需要处理系统睡眠/显示睡眠风险时先复制 `MacPowerPlan=` 只读预览，不会执行 `pmset`、不提权、不改系统；如果同屏也需要确认独立值守/电源证据新旧，可显式改跑 `start-mac-client --status --checkBoard --boardSummary` 读取 `MacUnattendedFreshness=`，默认页面状态命令仍不访问 Agent Link Board；无人值守时继续只跑 `MacClientFormalSmoke=` 无密 preflight 或 `MacClientBrowserSelfTest=` 本地 mock 自测。
- 恢复总览里的 `MacHostMedia=` 可直接运行 `node scripts/mac/check-mac-host-readiness.mjs --host <Mac host> --port <port> --checkBoard --probeMedia --probeMediaResourceSample --promptPassword --boardSummary`，用于正式 H.264/PCM 长测前刷新媒体基线。它是 `commands.mediaReadinessBoardSummary` 的稳定别名，方便 Windows 端和人工复制；命令本身会本地提示密码，不把密码放进 argv 或通讯板，不发送 call/input/inject。
- 恢复总览里的 `MacClientDiscoverWindows=` 可直接运行 `node scripts/mac/discover-windows-hosts.mjs --checkBoard --boardSummary`，先从 Mac 侧只读发现 Windows host，同时读取 Agent Link Board 上的 `WindowsLanRisk=`，并拿到 `FormalChecklist=`、`ManualChecklist=connection/video/audio/clipboard/input_ack/diagnostics`、稳定 `MacClientFormalSmoke=node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --boardSummary`、`MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary`、`MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary` 和 ready 后 `--sendCall` 协调命令；该命令不认证、不弹密码、不发送 Agent Link Board call/input/inject，也不会执行 `pmset`。
- 恢复总览里的 `MacClientFormalChecklist=` 可直接运行 `node scripts/mac/check-mac-client-formal-status.mjs --discover --port 43770 --boardSummary`，先只读发现 Windows host，再把 Mac -> Windows formal readiness、`ManualChecklist=connection/video/audio/clipboard/input_ack/diagnostics`、`MacPowerPlan=` 和反控演练提示压成一行；该命令只读，不认证、不弹密码、不发送 Agent Link Board call/input/inject，也不会执行 `pmset`。
- 恢复总览里的 `MacClientFormalSmoke=` 可直接运行 `node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --boardSummary`，先安全启动或复用本地 Mac client 页面，再做无密 Windows discovery/formal preflight；已知 Windows IP 或想避免扫整段局域网时，原始 `--discoverHost`、`--discoverSubnet`、`--discoverNoLocalSubnets` 和 discover timeout 会被摘要里的安全重跑命令保留。该命令不弹密码、不认证、不发送 Agent Link Board call/input/inject。
- `run-mac-client-formal-smoke --json` 或 `--boardSummary` 自己也会输出同一条 `MacClientFormalSmoke=` 安全重跑入口和 `MacScriptHelp=` 安全自检入口；如果现场只拿到 formal smoke 失败/阻塞摘要，不必回翻 heartbeat/resume，直接复制这些标签里的命令先做无密 preflight 或 Mac 脚本 help 副作用检查。它们会拒绝密码、认证、call、input 和 inject 路径。
- 如果真机 Mac client formal smoke 已经选中 Windows host，但在认证前失败或阻塞，最新失败摘要现在也会带 `ReverseGrantCopy=`、`WindowsReverseGrantStatus=`、`WindowsOpenOneTimeReverseGrant=` 和 Node fallback。现场认证恢复后若遇到 `LAN008`，Windows 端直接用这些本机回环命令打开 30 秒一次性授权，Mac 再点“重试反控”；不要把密码或授权 token 发到通讯板，也不要执行 `inject`。
- 同一份失败/阻塞摘要现在也会带 `MacClientBrowserSelfTest=`；如果真实 Windows host 密码、LAN 或反控授权暂时没准备好，先跑这条本地 mock browser 自测，确认 Mac client 页面、反控按钮、剪贴板和 input-log 流程本身没有退化，再回到真实 smoke。
- 恢复总览里的 `MacFormalLocalSmoke=` 可直接运行 `node scripts/mac/check-mac-formal-local-smoke.mjs --host <Mac host> --port <port> --promptPassword --boardSummary`，在正式呼叫 Windows 前先由 Mac 本机短验收 H.264、系统 PCM 和 `inputMode=log` 输入 ack，并只在 stdout 输出一行无密摘要；摘要现在同时给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary` 和 `MacPowerPlan=node scripts/mac/plan-mac-power-settings.mjs --profile all --sleep 0 --displaySleep 0 --networkWake on --boardSummary`，只拿到本机短验收摘要时也能要求 Mac 跑统一 help 安全自检或只读电源预案。它会先响铃并弹隐藏密码框，密码只经环境变量传给子探针，不进入 argv，不发送 Agent Link Board call/input/inject；`MacPowerPlan=` 入口本身不执行 `pmset`、不提权、不改系统。
- 恢复总览里的 `MacFormalE2E=` 可直接运行 `node scripts/mac/check-mac-formal-e2e-status.mjs --host <Mac host> --port <port> --boardSummary`，把 repo、通讯板、Mac host、权限、媒体、剪贴板、display 和 buildDiff readiness 压成一行；摘要会同时给出 `MacScriptHelp=node scripts/mac/test-mac-script-help.mjs --timeoutMs 10000 --boardSummary` 和 `MacClientBrowserSelfTest=node scripts/mac/test-mac-client-browser-self-test-wrapper.mjs --boardSummary`，只拿到 `MacFormalE2E=` 时也能让 Mac 端跑统一 help 安全自检，或在真实 Windows host 密码/LAN/授权未就绪时先做本地 mock browser 自测。如果通讯板 recent lines 或当前 status note 里已有 `MacHostMedia passed=12/12 media=ok`、`MacFormalLocalSmoke H.264/PCM/input-log ... injected=false`，或已有 Windows resume/控制端汇总的 `Evidence=MacHostMediaOk,MacFormalLocalSmokeOk` / `MacEvidence=MacHostMediaOk,MacFormalLocalSmokeOk`，JSON `evidence[]` 会带 `tag=MacHostMediaOk` / `tag=MacFormalLocalSmokeOk`，`--boardSummary` 会显示 `Evidence=MacHostMediaOk,MacFormalLocalSmokeOk`；`callText` 仍保留 `Recent evidence: MacHostMedia ok; MacFormalLocalSmoke ok`。这只是正向证据，不会吞掉同段 warning/blocker。该命令只读，不弹密码、不认证 WebSocket、不发送 Agent Link Board call/input/inject，真正要发正式呼叫时再由人工确认后另行显式 `--sendCall`。
- 准备正式呼叫 Windows 做端到端验收前，优先跑 `node scripts/mac/check-mac-formal-e2e-status.mjs --boardSummary`：它会默认读取联络板，并把 repo、Mac host、LAN 地址、`inputMode=log`、权限、H.264、系统 PCM、剪贴板、显示器和 buildDiff 汇总成正式 checklist；摘要和 JSON 也会给出 `MacFormalLocalSmoke=`，提醒长时间正式验收前先本机短验 H.264/PCM/input-log，并给出 `MacScriptHelp=`，方便另一端要求 Mac 快速确认所有 Mac `.mjs` 帮助路径无副作用。若看到 `fps-limit` 或需要把已写入的 60Hz LaunchAgent 变成当前运行态，摘要和 JSON 还会给出 `MacHostStop=`、`MacLaunchAgentLoad=`、`MacLaunchAgentPrint=`，用于人工按“停当前 host -> 加载 LaunchAgent -> 打印验证 -> MacUnattendedFormal”切换；脚本本身仍不执行这些系统动作。只有没有 blocker 且联络板可读时才会标记 `readyToCall=true`。如果需要机器可读状态，用 `--json` 读取 `checklist`、`counts`、`readyToCall`、`callText`、`commands.macFormalLocalSmokeCommand`、`commands.macScriptHelpCommand`、`commands.macHostStopCommand`、`commands.macLaunchAgentLoadCommand`、`commands.macLaunchAgentPrintCommand` 和 `callPayload`；确认要由脚本代发 Agent Link Board 呼叫时，再显式加 `--sendCall`，它只在 `readyToCall=true` 时发送一条无密 formal E2E call。未 ready 时会拒绝发送并失败，JSON 会保留具体 blockers、下一步和变动 runtime 文件；发 call 和清理旧 call 前会直接读取 `/api/state` 的 `currentCall`，DONE/COMPLETED/CANCELLED/RESOLVED/CLOSED 等完成态不会阻塞，仍在 active 的呼叫默认拒绝覆盖。只有已经在联络板上协调清楚要替换旧呼叫时，才显式加 `--forceCall`。如果历史 formal call 仍挂在板上、但当前 checklist 已不 ready，可显式运行 `node scripts/mac/check-mac-formal-e2e-status.mjs --clearStaleCall --json`：它只会清理 Mac Codex 发起、Windows Codex 配合、目标为“正式端到端验收 Mac host”的过期 call；其他端或其他目标的 call 会保留。开发中本地预览可加 `--allowDirty`，正式发 call 前不要放宽 dirty worktree。该脚本不启动服务、不认证、不要求密码、不发送输入；`inject` 会明确跳过，仍需用户另行明确确认。当前真实 Mac host `192.168.31.122:43770` 已在 runtime build `c5e5009` 完成完整 Windows formal Mac E2E；后续 Mac 侧再发正式呼叫前，应先重新确认 host 是否仍在线和 build 是否当前，而不是复用旧 call。
- 如果 Supervisor 或 Windows 审查机复跑 Mac 侧测试，优先确认 `node scripts/mac/test-mac-password-prompt.mjs` 和 `node scripts/mac/test-mac-host-start-helper.mjs`：前者应使用 fake `osascript`/`swift` 的 Unix shim + `.mjs` + Windows `.cmd` 包装器，在 Windows 上优先直连 fake `.mjs` 执行体以避开 `cmd.exe` 多行 AppleScript 参数转义，不打开真实系统弹窗；后者在非 macOS 上应继续跑 dry-run/status/密码安全检查，并对真实 Swift Mac host 启动段输出清晰 `[SKIP]`。当前 Windows 审查机复跑这两条已通过；若在 Codex 沙盒里看到 `spawnSync node.exe EPERM`，用允许子进程的执行方式复跑确认。
- 如果正式 call Windows 前需要先由 Mac 本机短验收媒体和安全输入链路，可运行 `node scripts/mac/check-mac-formal-local-smoke.mjs --promptPassword --boardSummary`，或用 `LAN_DUAL_PASSWORD` 环境变量提供正式密码。`--promptPassword` 会先播放两声提示音，并输出一行不含密码的前台弹窗提示，再优先打开 macOS 系统隐藏密码弹窗；如果系统弹窗打不开，才尝试原生 AppKit 前台高层级隐藏密码框作为备用。它会串联 H.264、系统 PCM 和 input-log 三个已有探针，密码只通过环境变量传给子探针，不放命令参数；默认拒绝空密码和 `demo-password`，不会启动 host、不会执行 `inject`。需要自动化结果时加 `--json`；需要直接发联络板时用 `--boardSummary`；只有本地假服务测试才加 `--allowDemoPassword`。
- 如果只想快速同步 Mac host 媒体基线而不跑 input-log，可运行 `LAN_DUAL_PASSWORD=... node scripts/mac/observe-mac-media.mjs --json` 或 `node scripts/mac/observe-mac-media.mjs --videoDurationMs 30000 --audioDurationMs 30000 --maxFrameAgeMs 250 --boardSummary`；需要顺手记录本机 Mac host CPU/RSS 时再加 `--resourceSample`。JSON 里优先读 `summary.status=ok|partial|failed`，通讯板给人看的摘要里单路成功单路失败会显示 `Mac media baseline partial`；该脚本只连接已运行 host，不启动服务、不发送输入、不执行 `inject`，默认不播放测试音。长观察默认每 10 秒输出进度心跳，现场需要更密集反馈时加 `--progressIntervalMs 5000`，不需要时传 `0`；`--json` / `--boardSummary` 的 stdout 不会混入心跳。
- 日常启动真实 Mac host 前可先用 `node scripts/mac/start-mac-host.mjs --status --checkBoard --boardSummary` 只读查看当前 `/discovery`、runtime、权限、能力、Windows 可连地址和 Agent Link Board currentCall；它不会启动服务，也不会要求或打印密码。若运行中 build 落后当前 git，`--status` 会只比较 `apps/mac-host` 运行源码并提示旧 build 后是否有 host 行为相关改动，帮助判断是必须重启，还是只是 build 元数据落后；脚本或联络板自动化需要稳定字段时可加 `--json`，读取 `online`、`runtime`、`permissions`、`capabilities`、`lanAddresses`、`buildDiff` 和 `board`。`--checkBoard` 只读 `/api/state.currentCall`，active call 会提示先协调，DONE call 标为 inactive，`--boardSummary` 不回显 call command；如果只写 `--boardSummary --checkBoard` 而忘了 `--status`，启动助手也会自动走只读 status，不会误启动 Mac host。需要启动时优先用 `node scripts/mac/start-mac-host.mjs --promptPassword --requirePassword`；该入口会先播放两声提示音，并输出一行不含密码的前台弹窗提示，再优先打开 macOS 系统隐藏密码弹窗，系统弹窗打不开时才尝试原生 AppKit 前台高层级隐藏密码框，默认 `inputMode=log`、打印 Windows 可连接的局域网地址、等待 `/discovery`，并跑只读 runtime/display 校验。显式 `--promptPassword` 会重新弹窗输入，不会静默复用已有 `LAN_DUAL_PASSWORD`。如果希望启动助手完成 `/discovery` 和 runtime/display 校验后退出、让 host 继续常驻，可显式加 `--background`，日志默认在 `.dev-lab/mac-host/`，也可加 `--logFile <path>`；后台模式仍只有在校验通过后才脱离，且子校验不通过 argv 传密码。需要停止后台 host 时用 `node scripts/mac/start-mac-host.mjs --stop` 或 `--stop --json`，它只停本机 `/discovery` 对应的 macOS host PID，不读取密码，也会拒绝非本机或非 Mac discovery。需要先恢复 `/discovery`、runtime/build 和权限诊断但暂时不能共享正式密码时，可用 `--ephemeralPassword --requirePassword` 生成一次性随机密码且不打印；这只适合无密码 discovery/runtime 检查，认证联调仍需用户输入正式密码或重启到约定密码。直接 `swift run lan-dual-mac-host` 时源码默认也应保持 `log`，`check-mac-host-readiness` 默认会回归该安全默认值；日常真实注入一律走启动助手，必须有人在屏幕前确认安全后再显式加 `--inputMode inject --confirmUserWatching` 或 `--injectInput --confirmUserWatching`。裸 `LAN_DUAL_INPUT_MODE=inject` 只能作为人工监督下的底层调试方式，不作为交接命令。
- Mac host 改动、重启或联调前优先跑 `node scripts/mac/check-mac-host-readiness.mjs --checkBoard --boardSummary` 做低风险体检和通讯板 currentCall 摘要；默认会提示运行中 `/discovery.runtime.buildId` 是否落后当前 git short hash，但不因此失败，并会列出旧 build 后变动的 Mac host runtime 源码文件，方便判断重启必要性。`--checkBoard` 只读 `/api/state.currentCall`，active call 会提示先协调，DONE call 标为 inactive，摘要不回显 command。部署验收可直接用 `--profile deploy`，它会要求 `/discovery` 可达、运行中 host 是当前源码 build、屏幕录制/辅助功能/输入监控权限开启，并串联 H.264、PCM、`log` 输入冒烟和 `--maxVideoFrameAgeMs 250`；如果需要一行汇总 H.264+PCM 媒体状态，可加 `--probeMedia --boardSummary`，摘要会显示 `media=ok|partial|failed`，partial/failed 会保留 passed/failed 计数。正式密码 host 做深度探针时加 `--promptPassword`，readiness 会先响两声并输出不含密码的前台弹窗提示，再优先打开 macOS 系统隐藏密码弹窗，系统弹窗失败时才尝试原生 AppKit 前台高层级隐藏密码框，并用环境变量传给子探针，避免默认 `demo-password` 误判或把密码放入命令参数。更深的本机部署自测可用 `--profile deep` 额外跑启动助手临时端口路径和 Mac host 文件剪贴板安全回归；只想单跑该安全回归时用 `--probeClipboardSecurity`，它只运行本地专项脚本，不启动 host、不写系统剪贴板、不要求密码、不执行输入。需要临时验收旧 build 时加 `--skipCurrentBuildCheck`，或按需单独组合 `--requireControlPermissions`、`--requireInputMonitoring`、`--requireCurrentBuildId`、`--probeVideo`、`--probeAudio`、`--probeMedia`、`--probeInputLog`、`--probeStartHelper`、`--probeClipboardSecurity`。Mac host 最新源码会只读探测真实 Input Monitoring 状态；如果运行中的主机仍显示旧的 `inputMonitoring=off`，先确认它是否已重启到包含该修正的新 build。
- Mac host 文件剪贴板接收相关改动后，先跑 `swift build --package-path apps/mac-host`、`node scripts/mac/test-mac-host-clipboard-file-integrity.mjs` 和 `node scripts/windows/probe-mac-host.mjs --host <Mac IP> --port 43770 --clipboardFile --promptPassword --expectInputMode log` 或 PowerShell 包装的 `-ClipboardFile` 真机探针。当前接收端会拒绝无 offer、未声明 fileIndex、负数/不连续 offset、超大/越界/重复/重叠 chunk 和不完整完成；合法空文件仍会在 offer 阶段创建空文件并可完成。
- 继续验证真实 macOS 系统声音采集，5 分钟只读观察已稳定到 15001 帧/50.0fps/最大间隔 31ms；下一步重点看非静音系统声音、音量变化、Windows 控制端真实听感和延迟。长观察会默认每 10 秒输出帧数、FPS、最大间隔、电平和剩余时间；有人确认可发声时，可运行 `node scripts/mac/observe-mac-audio.mjs --durationMs 4500 --minFrames 160 --maxGapMs 1000 --playTone --requireLevel --minLevel 0.01` 做 Mac 本机有声电平强校验。
- 继续压测 ScreenCaptureKit + VideoToolbox H.264：30 秒动态/活跃窗口曾稳定到 877 帧/约 29.2fps/最大间隔 45ms，空闲桌面 5 分钟实收约 10.6fps、60 秒复测约 10.9fps；下一步重点用动态画面、CPU 占用、端到端延迟和 Windows 控制端同时连接体验来判断真实观感，不要把静态桌面 `--minFps 25` 当硬门槛。做长观察时可给 `scripts/mac/observe-mac-video.mjs` 加 `--expectActiveDisplayId main --requireMonotonicTimestampUs` 或外接屏对应 display id，同时确认帧率、显示器来源和 H.264 媒体时间线；长测会默认每 10 秒输出帧数、FPS、最大间隔、帧年龄和剩余时间；重启到小数秒 timestamp build 后可用 `--maxFrameAgeMs 250` 级别检查本机接收年龄，旧 host 仍需临时放宽到 `1500` 以上。
- 做连续建连体验回归时可运行 `node scripts/mac/stress-mac-host.mjs --iterations 20 --maxProbeMs 8000 --maxFirstFrameMs 3000 --maxH264ConfirmMs 3000 --maxAudioFrameMs 3000 --expectInputMode log`，同时观察首帧、H.264 确认、首个音频帧耗时和 RSS/FD 是否漂移。
- 真实输入注入前先跑 `node scripts/mac/smoke-mac-input-log.mjs`，确认当前 host 仍是 `inputMode=log` 且鼠标/滚轮/键盘/快捷键事件都会返回 `input_ack`；切 `inject` 前需要人工在屏幕前确认安全环境。
- 真实 Mac input inject 小范围验收已在 2026-06-16 通过：Mac host `192.168.31.122:43770` / runtime build `d398d64` / `inputMode=inject`，Windows 本机隐藏输入密码后运行 safe probe，2 个事件均 `injected=true`，transcript：`.dev-lab/mac-inject-safe-20260616-091526.transcript.txt`。后续复跑仍走同一安全事件集：Mac 端先停止当前 log host，确认用户正在看 Mac 屏幕后，再用 `node scripts/mac/start-mac-host.mjs --promptPassword --requirePassword --inputMode inject --confirmUserWatching --background` 启动；Windows 端随后运行 `node scripts/windows/probe-mac-host.mjs --host 192.168.31.122 --port 43770 --promptPassword --requirePassword --inputEvents --inputEventSet safe --expectInputMode inject --expectInputInjected true`。`safe` 只发鼠标移动和 F13，避免点击、滚轮、Delete 或 Ctrl+A；需要更完整输入覆盖时，再明确切到 `--inputEventSet full`。
- 扩展 CGEvent 键盘映射，重点验证中文输入法、Command 组合键和功能键；改映射前后先跑 `node scripts/mac/check-input-keymap.mjs`，它会覆盖常见 code/key、同义项和修饰键 flag fallback。
- Mac host 已有真实显示器枚举、`displayId` 回执和单屏 round-trip 自检；`check-mac-displays` 默认会要求帧级 `activeDisplayId` 诊断，若主机没重启到最新二进制会失败。下一步接外接显示器后跑 `node scripts/mac/check-mac-displays.mjs --switchDisplayId <display-id>`，再让 Windows 控制端用显示器下拉做真实双屏切换验收。
- Mac host 重启或部署后，先看 `/discovery.runtime`、Windows 控制端诊断条，或运行 `node scripts/mac/check-mac-displays.mjs --requireRuntime --expectBuildId <build-id>`，确认 `processId`、`startedAt`、`uptimeSeconds` 和 `buildId` 符合预期；刚重启时可加 `--maxRuntimeUptimeSeconds 120`，避免连到旧进程。新 build 还应确认 `/discovery.lastSeenAt`、`runtime.startedAt` 和 `video_frame.timestamp` 带小数秒，再用 `observe-mac-video --maxFrameAgeMs 250` 做低延迟接收年龄观察。Windows 侧可用 `node scripts/windows/test-windows-client-browser.mjs --diagnosticsOnly --host <Mac IP> --port 43770 --expectDiscoveryRuntimeBuildId <build-id>` 做无密码 UI 验收。
- 继续完善 `apps/mac-client` Mac 控制 Windows 原型：真连前可先用 `node scripts/mac/start-mac-client.mjs --status --boardSummary` 检查本地控制页面；更省心的默认路径是直接跑 `node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --boardSummary`，它会安全启动或复用本地 Mac client 页面；只读确认也可不用 `--ensureClient` 跑 `node scripts/mac/run-mac-client-formal-smoke.mjs --discover --preflightOnly --boardSummary`，脚本会先只读扫描 Windows `/discovery` 并自动选中最佳 Windows host，发现失败不会弹密码框，且 discover 结果会透传 `FormalChecklist=` 与 `ManualChecklist=connection/video/audio/clipboard/input_ack/diagnostics`。需要 Windows 端配合时优先用 `node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --sendCall` 一条命令完成发现、确保本地页面、无密预检和 ready 后通讯板呼叫，它只在 `readyToCall=true` 时发送，未 ready、无 host 或已有 active call 时会拒绝，不认证、不弹密码、不执行 `inject`；完成态 `currentCall` 不会再误挡住新 call，协调清楚要覆盖仍在 active 的旧 call 时再加 `--forceCall`。单独发现也可跑 `node scripts/mac/discover-windows-hosts.mjs --checkBoard --boardSummary`，发现摘要会同时给出 `WindowsLanRisk=`、formal checklist 命令、稳定 `MacClientFormalSmoke=` 安全无密预检命令和 ready 后的 `--sendCall` 协调命令。正式观感/延迟/资源对照前也可用 `node scripts/mac/check-mac-client-formal-status.mjs --discover --boardSummary` 查看完整 `runPlan`；已知 Windows IP 且不想扫整段局域网时加 `--discoverNoLocalSubnets --discoverHost <Windows IP>`，或直接用 `--host <Windows IP> --port 43770`；如果不用 smoke 包装器代发，也可显式改用 `node scripts/mac/check-mac-client-formal-status.mjs --discover --sendCall` 发起无密 Agent Link Board call。ready 后优先在 Mac 侧运行 `node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --promptPassword`，已知 Windows IP 且不想扫整段局域网时加 `--discoverNoLocalSubnets --discoverHost <Windows IP>`。脚本先跑无密 checklist，再用先响两声、输出不含密码提示、优先系统隐藏弹窗且原生 AppKit 高层级备用的密码框或 `LAN_DUAL_PASSWORD` 通过环境变量把正式密码传给页面自检，不把密码放进 argv，不发联络板，不执行 `inject`；本地诊断预览用 `--dryRun --json`，其中 `commands.sendCall` 可直接作为 ready 后的通讯板呼叫命令；如果使用备用通讯板地址，给 smoke 包装器加 `--server <url>` 会传给内部 formal checklist、输出的 sendCall 命令和实际发送 call 的子流程。随后观察真实 Windows WASAPI PCM 播放、视频首帧/持续 FPS/音频首帧和播放计数；Windows 本机临时启动 host 验收可直接加 `--requireAudio`，需要覆盖意外断线恢复时可加 `--expectReconnect`；Mac client 现已显示 Windows host runtime/build，真实 Windows host 从启动助手或桌面壳启动后可直接观察“远端运行”是否为当前 git build；Mac client 已具备 H.264/WebCodecs canvas 接收准备，会优先请求 `h264/annexb`，不支持或解码失败时请求 MJPEG/JPEG 兜底，Windows 端 ffmpeg-h264 推出后应优先用真实 host 验收 canvas 解码、延迟和回退；视频状态和会话诊断会在对端提供 `video_frame.timestamp` 时显示帧到达年龄或时钟偏差，收到 Windows WGC `repeatPreviousFrame` 轻量重复帧时会保持上一帧并显示重复计数，页面级自检可加 `--expectRepeatSignalVideo` 验证；真实视频验收时应同时看首帧耗时、实收 FPS、video frame age 和重复帧计数；音频状态和会话诊断会在对端提供 `audio_frame.timestamp` 时显示帧到达年龄或时钟偏差，真实音频验收时应同时看首帧耗时、播放计数和 audio frame age；体验验收可加 `--maxInitialVideoMs <毫秒>` 建立首帧阈值，搭配 `--expectReconnect` 时可加 `--maxReconnectRestoreMs <毫秒>` 建立断线恢复阈值；视频持续体验可加 `--observeVideoMs <毫秒> --minObservedVideoFrames <帧数> --minObservedVideoFps <FPS>` 建立持续来帧阈值；连接首帧、认证失败、H.264/二进制视频、音频首帧/播放、持续视频观察和重连恢复等待都会默认每 10 秒输出进度心跳，现场需要更密集反馈时加 `--progressIntervalMs 5000`，不需要时传 `0`；音频体验可加 `--maxAudioFrameMs <毫秒>` 建立音频首帧阈值，真实 PCM 播放路径可再加 `--maxAudioPlaybackMs <毫秒>` 建立播放计数阈值；视频参数控件已可请求 1080P/2K/4K、30-240 Hz 和 5-50 Mbps，Windows host 已按码率回传并应用 `jpegQuality`，后续重点看真机 Mac 控制 Windows 的 60 Hz 观感、延迟、画质、音画体验和键盘映射边界。
- Mac -> Windows 真机轻量联调时，`check-mac-client-formal-status --boardSummary` 的 `ManualChecklist=connection/video/audio/clipboard/input_ack/diagnostics` 就是现场人工核对顺序：先确认连接/认证和 Windows runtime，再看视频首帧/FPS/frame age，再看音频 payload/playback/frame age，再测文本和文件剪贴板，再确认 input_ack 仍是安全日志模式，最后复制 Mac client 诊断并确认不含连接密码。

## Windows Codex 可接任务

- 每次恢复 Windows 侧工作、准备向通讯板发状态或准备正式 Mac 验收前，先跑 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-resume-status.ps1 -CheckBoard -BoardSummary`，或 Node 等价命令 `node scripts/windows/check-windows-resume-status.mjs --checkBoard --boardSummary` 做轻量总览：它只读汇总当前 git、通讯板、Mac formal preflight、自动发现目标、Windows 控制端诊断端口状态和下一步安全命令，不启动服务、不认证、不要求密码、不发送输入、不执行 `inject`；`--checkBoard` 会优先结构化读取 Agent Link Board `/api/state`，如果联络板有 active `currentCall`，摘要会直接显示 Mac -> Windows 呼叫标题和方向，DONE call 不会当作待办，便于 Windows 接手时知道是否要先响应 Mac 端。恢复总览的 JSON、普通输出和 `--boardSummary` 也会给出 `WinClientPorts=`、`WinClientDiagnostics=node scripts/windows/test-windows-client-browser.mjs --discover --diagnosticsOnly ...`、`WinClientDiagnosticsAlt=` 和 `CopyDiagnostics=Windows 控制端事件面板点击“复制诊断”，先看“快速摘要”`，用于现场先拿到无密 UI 诊断报告；执行 `WinClientDiagnostics` 时可直接追加 `--boardSummary`，stdout 会只留一行无密页面诊断摘要，适合发回通讯板。如果默认 `5197/9337` 被旧诊断残留占住，PowerShell wrapper 可直接加 `-ClientPort 5200 -DebugPort 9340`，Node 入口则加 `--clientPort 5200 --debugPort 9340`。同时给出 `MacUnattended=node scripts/mac/check-mac-unattended-status.mjs --host <Mac IP> --port 43770 --boardSummary`，用于让 Mac 侧只读同步 LaunchAgent、自启动、权限、电源和锁屏/睡眠限制摘要；给出 `MacUnattendedFormal=node scripts/mac/check-mac-unattended-status.mjs --host <Mac IP> --port 43770 --requireLaunchAgentMaxFps --requireLaunchAgentLoaded --boardSummary`，用于正式 60Hz 验收前把 LaunchAgent max FPS 缺失或低于目标识别为 blocker；给出 `WindowsHostMedia=node scripts/windows/check-windows-host-readiness.mjs --checkBoard --probeMedia --boardSummary` 与 `WindowsHostMediaPs=powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-host-readiness.ps1 -CheckBoard -ProbeMedia -BoardSummary`，用于 Mac 反控 Windows 前快速刷新本机视频/音频基线；给出 `WindowsVideoSupport=node scripts/windows/check-windows-video-encoder-support.mjs --boardSummary` 与 `WindowsVideoSupportPs=powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-video-encoder-support.ps1 -BoardSummary`，用于现场先确认 FFmpeg H.264、硬编、WGC 和 WebCodecs 能力；给出 `WindowsWgcBenchmark=node scripts/windows/benchmark-windows-wgc-settings.mjs --profile 60:20000:balanced --durationMs 1800 --boardSummary` 与 `WindowsWgcBenchmarkPs=powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/benchmark-windows-wgc-settings.ps1 -Profile 60:20000:balanced -DurationMs 1800 -BoardSummary`，用于恢复后直接跑基础刷新率/码率 benchmark；给出 `PowerShellHelp=node scripts/windows/test-windows-powershell-help.mjs --timeoutMs 10000 --boardSummary` 与 `PowerShellHelpPwsh=node scripts/windows/test-windows-powershell-help.mjs --shell pwsh --timeoutMs 10000 --boardSummary`，用于改 `.ps1` 后先发一行 help 覆盖结果；并给出 `ReverseGrant=node scripts/windows/allow-windows-reverse-control.mjs --host 127.0.0.1 --port 43770 --durationMs 30000 --boardSummary`，用于 Windows 本机打开约 30 秒一次性反控授权后让 Mac 重试。JSON 和普通输出会额外给出 `start-mac-alert-watcher.ps1 -Server <Agent Link Board>` 与 `-Status` 命令，并只读显示 watcher 当前 `running/not-running/unknown/unavailable` 状态，用于开工后直接判断 Windows 本机浮窗提醒是否已开；`--boardSummary` 仍保持短摘要，不塞入 watcher 管理命令。准备正式验收前可加 `-CheckClientDiagnostics` / `--checkClientDiagnostics`，让 Windows 控制端页面也做无密发现与 runtime 诊断；如果要直接生成可发联络板的授权提示，用 `-UserAuthRequest` / `--userAuthRequest`，ready 时会给出固定目标的 PowerShell 正式验收命令；确认要由脚本代发通讯板消息时，再显式加 `-SendUserAuthRequest` / `--sendUserAuthRequest`，它只在 preflight ready 时发送，未 ready 会拒绝发送。需要机器读取时用 `-Json` / `--json`；需要把 Mac host 未 ready 或工作区不干净变成失败时分别加 `-RequireMacReady`、`-RequireClean` 或 `--requireMacReady`、`--requireClean`。如果只想定点检查已知 Mac，可加 `-DiscoverNoLocalSubnets -HostName 192.168.31.122 -Port 43770` 或 `--discoverNoLocalSubnets --host 192.168.31.122 --port 43770`。
- 恢复总览判断本机 Mac 提醒 watcher 状态时，会优先调用 `start-mac-alert-watcher.ps1 -Status -Json`，并在 JSON 里保留 `windowsMacAlertWatcher.source`、`payload` 和 `parseError`；脚本或桌面壳读取运行状态时优先用这些结构化字段，不要再解析人类文本。该 JSON 也会读取 watcher stdout 日志里的 `recentAlerts` / `lastAlert`，供 Windows 控制端“Mac 提醒”区和复制诊断显示最近一条提醒短摘要。若旧环境不支持 `-Json`，恢复总览仍会退回文本判断。
   - Mac 端运行 `check-mac-unattended-status --boardSummary`、`check-mac-resume-status --boardSummary`、`check-mac-host-readiness --boardSummary`、`run-mac-client-formal-smoke --preflightOnly --boardSummary`、`check-mac-client-readiness --boardSummary`、`check-mac-client-formal-status --boardSummary` 或 `check-mac-formal-e2e-status --boardSummary` 后，Windows 本机 Mac 提醒 watcher 会识别 `MacUnattendedStatus`、`MacResumeStatus`、Mac host readiness、Mac formal smoke、Mac client/formal/final E2E 摘要里的 `warnings=` / `blockers=`，也兼容人工文本里的 `warnings:` / `blockers:`；LaunchAgent 缺失/未加载、电源或睡眠风险、`warnings=launch-agent-max-fps`、`warnings=fps-limit`、`warnings=h264-fallback,fps-limit`、`warnings=mac-host-discovery,mac-host-max-fps`、`warnings=agent-link-board-currentcall`、`warnings=board`、`warnings=windows-host`、`warnings=video,build,auth` 等非 none 明细会提醒，`warnings=none blockers=none` 不提醒。若同一条 Mac host readiness 摘要带 `MacHostSafeStart=`，Windows 控制端会在风险摘要和复制诊断里提示 Mac host 安全启动命令已提供；若同一条 FPS/LaunchAgent 上限摘要带 `MacMaxFpsSafeStart=`，Windows 控制端会提示 Mac 60Hz 安全启动命令已提供；若同一条 Mac client/formal 摘要带 `MacClientFormalChecklist=`，Windows 控制端会提示 Mac client 正式清单命令已提供；若 formal smoke、认证、失败或 warning/blocker 上下文里带 `MacClientPromptPasswordSmoke=`，Windows 控制端会提示 Mac client 前台密码真测命令已提供，等用户在 Mac 前台时再运行；若 formal smoke/browser self-test 失败或 warning/blocker 上下文里带 `MacClientBrowserSelfTest=`，Windows 控制端会提示 Mac client 本地 browser 自测命令已提供，先让 Mac 跑本地 mock 自测排除页面退化；这些命令单独出现且 findings 为 none 时不误弹。如果窗口最小化但 Mac 值守或正式清单需要处理，Windows 侧应能弹窗/打印提醒。若不想弹窗，可用 `watch-codex-link-mac-alerts.ps1 -Once -NoPopup -AlertExistingEvents` 前台检查同一规则。
- Mac 心跳或值守摘要如果提供 `MacLaunchAgentLoad=` / `MacLaunchAgentPrint=`，且同段有 `loaded=false`、`launchAgentLoaded=false`、`fps-limit`、`launch-agent-max-fps`、旧 build 或重启建议等上下文，Windows 本机 Mac 提醒 watcher 也会弹窗/日志提醒；干净命令清单不提醒。现场处理 60Hz 切换时，最小化窗口也能看到该按 `MacHostStop=` -> `MacLaunchAgentLoad=` -> `MacLaunchAgentPrint=` -> `MacUnattendedFormal=` 继续。
- 如果 Mac 远控窗口最小化、或白天等正式密码/系统授权期间需要 Windows 本机透传提醒，可在 Windows 侧启动 `scripts\windows\start-mac-alert-watcher.ps1 -Server http://192.168.31.68:17888`。它优先使用 PowerShell 7 `pwsh`，只读 Agent Link Board，不发送密码、不认证、不执行输入；Mac 端 `NEED_USER_AUTH`、权限/授权、502/Bad Gateway、`blocked`、长时间无更新，Mac Codex 发给 Windows Codex/Windows host 的 active `currentCall`，Mac client readiness/formal 或 formal E2E status 的 warning/blocker 明细，Mac 本机短验收 `MacFormalLocalSmoke=` / `RerunFormalLocalSmoke=` 的失败或复跑命令，以及反控演练里的 `LAN008`、等待/重试、`WindowsReverseGrantStatus=` / `WindowsOpenOneTimeReverseGrant=`、`ReverseGrant=`、`allow-windows-reverse-control` 或“临时允许反控后重试”都会弹本机提醒；干净 `warnings=none blockers=none` 的反控授权命令清单不误弹。重复启动会复用现有 watcher；需要查看、停止或重启时加 `-Status`、`-Stop` 或 `-Restart`。脚本或桌面壳需要机器读取时可加 `-Json`，`-Status -Json` / `-Stop -Json` 会输出单个 JSON 对象且不会回显 token。改 watcher 规则后先跑 `node scripts/windows/test-mac-alert-watcher.mjs --timeoutMs 20000`。
- Windows 桌面版也可以直接在“本机被控”面板的“Mac 提醒”区查看、开启或停止这个 watcher；日常有人在 Windows 侧等待 Mac 授权/权限/反控重试时，优先用这个入口，命令行入口作为备用。桌面壳自动轮询 watcher 状态会节流到约 15 秒一次，避免频繁启动 PowerShell；需要立即确认状态时点“刷新提醒”。现场导出或复制 Windows 控制端诊断时，也会带出 watcher 开启状态、最近提醒、最近检查时间、轮询间隔、联络板地址，以及本机被控 Windows host 的端口、输入模式、反控策略、体检档位和最近输出脱敏摘要；报告顶部先看“快速摘要”，再看“连接状态”和“本机协作”两段，判断 Mac 反控 Windows 卡点时优先看“本机协作”。需要发给另一端时优先点事件面板里的“复制诊断”。
- 正式 Windows 控制 Mac E2E 前，优先运行 PowerShell 包装入口 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-mac-formal-e2e.ps1 -Discover -PreflightOnly -CheckClientDiagnostics -BoardSummary`，让脚本先自动发现最佳 Mac host，再做无密合并预检并把摘要发到 Agent Link Board；Node 入口等价命令是 `node scripts/windows/check-mac-formal-e2e.mjs --discover --preflightOnly --checkClientDiagnostics --boardSummary`。如果只想先做发现层的一行摘要，可跑 `node scripts/windows/discover-lan-hosts.mjs --noLocalSubnets --host 192.168.31.122 --port 43770 --requireMacHost --boardSummary`，发现结果会同时给出 `FormalChecklist=`、`MacUnattendedFormal=check-mac-unattended-status --requireLaunchAgentMaxFps --requireLaunchAgentLoaded --boardSummary`、授权提醒和正式命令。已知 IP 且想避免扫整段局域网时，用 PowerShell 的 `-Discover -DiscoverNoLocalSubnets -HostName 192.168.31.122 -Port 43770` 或 Node 的 `--discover --discoverNoLocalSubnets --host 192.168.31.122 --port 43770`。2026-06-16 当前 `192.168.31.122:43770` / runtime build `c5e5009` 已跑完整正式验收并通过，记录见 `.dev-lab/formal-e2e-windows-20260616-084744.transcript.txt`：H.264/WebCodecs、音频、剪贴板、input-log 和页面诊断 OK，node exit code 0。后续如需复跑，可按同一流程先预检、再由用户本机隐藏输入正式密码；密码只经环境变量传给子探针，默认不执行 `inject`。如果预检摘要出现 `FpsLimit requested=<请求>Hz remoteMax=<远端>Hz`，说明正式长测会按远端上限运行；摘要里的 `MacMaxFpsPlan=` 是 Mac 端安全 dry-run 命令，不写系统、不包含密码，需 Mac 端确认并重启 host 后再判断 60Hz/更高刷新率体验。如果需要自动化读取预检，用 `--discover --preflightOnly --checkClientDiagnostics --json`，其中 `discoverySelection`、`boardSummary`、`userAuthRequest`、`sentUserAuthRequest`、`clientDiagnostics`、`fpsLimit` 和 `runPlan` 字段可直接供联络板消息或自动化脚本使用；`runPlan` 会列出正式验收两个子步骤、5 分钟视频/30 秒音频等阈值、密码传递方式和 `inject=false` 安全边界。
- 单独做底层探针时，也优先用 `node scripts/windows/probe-mac-host.mjs --discover --promptPassword --requirePassword --requireH264 --expectInputMode log`；PowerShell 包装入口同步支持 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\windows\test-mac-host.ps1 -Discover -PromptPassword -RequirePassword -RequireH264 -ExpectInputMode log`。只想避免扫整段局域网时，加 `--discoverNoLocalSubnets --host 192.168.31.122 --port 43770` 或 PowerShell 的 `-DiscoverNoLocalSubnets -HostName 192.168.31.122 -Port 43770`。音频可加 `--requireAudio --observeAudioMs 30000 --minAudioFrames 1200 --minAudioFps 40 --maxAudioGapMs 1000`，剪贴板可加 `--clipboardText --clipboardFile` 或 `-ClipboardText -ClipboardFile`；discover 路径会在密码提示前先选中 Mac host，发现失败时不会弹密码。
- 真实 Mac input inject 验收时不要复用普通 `--inputEvents` 默认脑补结果，Mac 端启动 inject host 必须带 `--confirmUserWatching`，Windows 端探针必须加 `--expectInputMode inject --expectInputInjected true`；默认 `--inputEventSet safe` 只发鼠标移动和 F13，PowerShell 等价参数是 `-InputEvents -InputEventSet safe -ExpectInputMode inject -ExpectInputInjected true`。只有用户确认可以承受点击/滚轮/快捷键副作用时，才加 `--inputEventSet full` 或 `-InputEventSet full`。
- Windows 控制 Mac 键盘体验或输入状态 UI 改动后，先跑 `node scripts/windows/test-coordinate-mapping.mjs` 和 `node scripts/windows/test-windows-client-browser.mjs --diagnosticsOnly --timeoutMs 45000`：前者覆盖 Win/Alt/Ctrl 到 macOS 修饰键、Ctrl+C/V/X/A/Z/Y 到 Command 快捷键、兼容开关和自定义映射；后者确认页面实际使用同一套共享映射函数，并确认顶部输入状态能区分安全日志、真实控制、已注入和被拒绝。后续真机编辑器/文本框验收前，先用这两条守住语义，再请 Mac 端在安全页面里按 `--confirmUserWatching` 流程切 inject。
- Windows host 文件剪贴板接收完整性整改已完成：接收端现在拒绝无 offer 分片、超大分片、越界 offset、重复/重叠分片、不完整文件、超过总大小或文件数上限的 offer，并按每个文件 expected size 判断完成。Supervisor 或 Windows 审查机复审双端剪贴板整改时，优先运行 `node scripts/windows/test-clipboard-integrity-suite.mjs --boardSummary --timeoutMs 45000`：它会串联 Windows 模块级边界、Windows 服务级 WebSocket 坏包回归和 Mac host 源码完整性专项检查，并输出可贴 Agent Link Board 的无密摘要。后续文件剪贴板改动也可拆开跑 `node scripts/windows/test-windows-clipboard-bridge.mjs`、`node scripts/windows/test-windows-host-clipboard-security.mjs` 和 `node scripts/mac/test-mac-host-clipboard-file-integrity.mjs`；收工或部署前可用 `node scripts/windows/check-windows-host-readiness.mjs --probeClipboardSecurity` 单独纳入一键体检，`--profile deep` 会自动包含这项安全回归；最后跑 `scripts/windows/test-windows-host.ps1` 或 Mac client 页面级文件发送回归。
- Mac host 文件剪贴板接收完整性整改也已完成：Windows 侧正式 `-ClipboardFile` / `--clipboardFile` 探针若失败，先看 Mac 端是否拒绝了无 offer、fileIndex、offset、chunk size、越界、重复/重叠或不完整完成；Mac 端专项回归是 `node scripts/mac/test-mac-host-clipboard-file-integrity.mjs`。
- 优化 Windows 控制端远端文件托盘：桌面版文件剪贴板写入已升级为 1MB 原生分块并支持到 512MB，原生层已补总量保护、7 天旧临时目录清理、临时目录白名单和分块边界单测；系统剪贴板写入失败但文件已落盘时，本地日志和托盘状态会显示临时目录，桌面版可一键打开并可重试写入；清空托盘会清掉内存暂存和提示，但不会删除系统剪贴板仍可能需要的临时目录；后续重点是真实大文件/压缩包复制体验。
- Windows 被控端 FFmpeg gdigrab 过渡层普通启动已可协商 60Hz，当前本机 720p/60Hz 基线为 4 秒 230 帧、57.1 FPS、最大间隔 41 ms、帧年龄最大 1 ms；观察脚本现在也能采样本机 Windows host 资源，`--resourceSampleTree true` 下的 60Hz 对照为 4 秒 198 帧、49.49 FPS、最大间隔 43 ms、帧年龄最大 1 ms、进程树 CPU 平均/峰值 4.5/5.4%、工作集峰值约 309.3 MiB。Windows Graphics Capture 支持预检已通过，且 `LAN_DUAL_WINDOWS_SCREEN_MODE=wgc` 显式入口会输出 `screen.wgc` 与 `wgcFallbackReason`；`LAN_DUAL_WINDOWS_WGC_HELPER` helper 接入点已落地，新增 Rust helper `apps/windows-wgc-helper`，本机 `--probe` 已能初始化 D3D11/WinRT Direct3D/GraphicsCaptureItem/frame pool/session，默认 capture 已能从 `Direct3D11CaptureFrame.Surface` 读回真实帧、按请求宽高等比缩放且不放大，并可输出 JPEG、raw BGRA 或 NV12。普通 JPEG helper 继续使用 `json-lines-v1`，raw-bgra/NV12 H.264 bridge 默认使用内部 `binary-frame-v1`。WGC helper JPEG、raw-bgra、NV12 三条 H.264 bridge 都已能接 `h264_nvenc`，NV12 720p 短测为 67 帧/约 30.06 FPS；新增 `compare-windows-wgc-h264-sources` 可一条命令对照 raw-bgra 与 NV12 的 FPS、重复帧、helperTiming 和资源数据。帧节奏诊断现在会明确区分实收 FPS、真实新帧 FPS、唯一 helper 源帧 FPS 和重复帧比例。WebSocket `binary-jpeg` 和 `binary-h264` 二进制视频帧已落地，Mac client 会按实际 codec 声明 `preferredVideoTransport=binary-jpeg` 或 `binary-h264`，Windows host 支持时 JPEG 或 H.264 Annex B payload 不再以 data URL/base64 文本重发；视频传输矩阵 `test-mac-client-video-transports.mjs` 默认守 `binary-jpeg`、`binary-h264`、JSON/base64 和 fallback 四档，默认单项失败后重试 1 次并换新端口，可选 `--includeWgcNv12` 把真实 WGC NV12 H.264 页面级验收纳入同一矩阵。下一步优先推进 helper 原生硬编、动态画面下的 WGC 源帧节奏和 Mac client 真连观感验收；随后对照延迟、带宽和资源占用。短期继续用 `node scripts/windows/compare-windows-wgc-h264-sources.mjs --profile 60:20000:balanced --durationMs 1800 --boardSummary` 或 `node scripts/windows/observe-windows-host-media.mjs --resourceSampleTree true --boardSummary` 做视频/音频统一报告，避免并发临时 host 干扰采集；媒体脚本成功或失败都会给出脱敏单行摘要，可直接发给 Mac 端对照。
- 跑完整视频传输矩阵时，普通输出现在也默认带 `--progressIntervalMs 10000` 外层进度：会报告当前 case、attempt、临时端口和子进程超时剩余，并把同一参数透传给页面自检；现场需要更密集反馈可加 `--progressIntervalMs 5000`，自动化结果继续用 `--json`，不会混入心跳。需要把矩阵结果发 Agent Link Board 时直接用 `--boardSummary`，摘要会一行说明通过数、失败 case、重试次数和 no input/inject 安全边界。
- WGC H.264 性能排查现在有两级入口：日常收口或 readiness 汇总用 `node scripts/windows/check-windows-host-readiness.mjs --probeWgcH264Sources` 跑一组 30Hz/10Mbps raw-bgra vs NV12 短对照；需要更长窗口、更多 profile、资源采样或动态画面刺激时，先用基础 benchmark：`node scripts/windows/benchmark-windows-wgc-settings.mjs --profile 60:20000:balanced --durationMs 1800 --boardSummary`，现场 PowerShell 等价命令是 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/benchmark-windows-wgc-settings.ps1 -Profile 60:20000:balanced -DurationMs 1800 -BoardSummary`；需要直接对照 raw-bgra/NV12 时，再用 `node scripts/windows/compare-windows-wgc-h264-sources.mjs --profile 60:20000:balanced --durationMs 1800 --boardSummary`，现场 PowerShell 等价命令是 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/compare-windows-wgc-h264-sources.ps1 -Profile 60:20000:balanced -DurationMs 1800 -BoardSummary`。`check-windows-resume-status --checkBoard --boardSummary`、`start-windows-host --status --boardSummary` 和 `check-windows-host-readiness --checkBoard --boardSummary` 都会给出 `WindowsWgcBenchmark=` / `WindowsWgcBenchmarkPs=` 和 `WindowsWgcCompare=` / `WindowsWgcComparePs=`。`--probeWgcH264Sources` 默认不启用，也不随 `--profile deep` 自动触发，避免普通部署体检变慢或因采集环境差异误失败。
- Mac 控 Windows 前若只想从本机被控入口复制排查命令，`start-windows-host --status --json --checkBoard` 也会输出 `windowsWgcCompareCommand` / `windowsWgcComparePowerShellCommand`；对应 `--boardSummary` / `-Status -BoardSummary` 会显示 `WindowsWgcCompare=` / `WindowsWgcComparePs=`，可直接发到通讯板后再决定是否运行较重的 raw-bgra/NV12 对照。
- 跑更长的 WGC benchmark/compare 时，普通输出已经默认带 `--progressIntervalMs 10000` 进度心跳：benchmark 会报告 helper 构建和每个 profile 子观察等待，compare 会报告每个 raw-bgra/NV12 source 子 benchmark 等待；现场需要更密集反馈可加 `--progressIntervalMs 5000`，自动化或通讯板仍优先用 `--json` / `--boardSummary`，这两种输出不会混入心跳。
- Windows 端已新增 `scripts/windows/check-windows-video-encoder-support.mjs` 只读视频编码能力体检，并新增 PowerShell 包装 `scripts/windows/check-windows-video-encoder-support.ps1`；本机强校验确认 FFmpeg `8.1.1` 同时具备 `libx264` 软件编码和 `h264_nvenc` / `h264_qsv` / `h264_amf` / `h264_mf` / `h264_d3d12va` 等硬编入口，WGC 预检通过，Edge WebCodecs H.264 解码支持通过。需要直接发 Agent Link Board 时可跑 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-video-encoder-support.ps1 -BoardSummary`，输出一行无密摘要；需要自动化消费时用 `-Json`。WGC helper JPEG -> FFmpeg/NVENC H.264、raw-bgra binary -> FFmpeg/NVENC H.264、NV12 binary -> FFmpeg/NVENC H.264 三个正确性原型都已经短测通过；下一步 Windows 端视频主线应从“内部 raw 二进制桥接正确性”切到“helper 原生硬编”和真连资源对照，同时保留 `test-mac-client-video-transports.mjs --h264Encoder h264_nvenc` 作为 binary-h264、JSON/base64、fallback、binary-jpeg 四路径回归；涉及真实 WGC NV12 页面接收时加 `--case wgc-nv12-h264` 或 `--includeWgcNv12`。
- Windows 被控端已有可选 `ffmpeg-h264` 流式模式：`LAN_DUAL_WINDOWS_SCREEN_MODE=ffmpeg-h264` 或 `observe-windows-host-video --screenMode ffmpeg-h264 --preferredVideoCodec h264` 会输出 H.264 Annex B 帧，并在 `session_answer`、`display_settings_ack` 和 `video_frame` 带 `codecString` / `h264Encoder`。默认编码器是 `libx264`，也可用 `LAN_DUAL_WINDOWS_H264_ENCODER=h264_nvenc` 或脚本 `--h264Encoder h264_nvenc` 跑 NVENC 过渡路径；本机 NVENC 输出、Mac client `binary-h264` 和视频传输矩阵 4/4 已通过。它是 Mac client H.264 接收准备和编码器选择的对接入口，不是 WGC 真采集替代品；Mac client/WebCodecs 拒绝当前 H.264 配置后，Windows host 已能按页面发出的 `preferredVideoCodec=mjpeg` / `preferredVideoEncoding=data-url` 切回 MJPEG/JPEG。后续 H.264 相关改动先跑 `node scripts/windows/check-webcodecs-h264-support.mjs --requireCodec avc1.42C02A` 确认浏览器支持，再跑 `node scripts/windows/test-windows-h264-mode.mjs --h264Encoder h264_nvenc` 确认本机 H.264 输出健康，然后跑 `node scripts/windows/test-mac-client-video-transports.mjs --h264Encoder h264_nvenc` 确认 Mac client canvas 解码、二进制 H.264 payload、旧 JSON/base64 兼容路径和失败后 JPEG fallback 都未退化。
   - Mac 控制 Windows 真机联调前，优先在 Windows 桌面版左侧“本机被控”面板选择低风险/部署/深度体检、预览防火墙命令，并用隐藏密码启动 Windows host；面板会自动消费 `start-windows-host --status --json --checkBoard`，显示真实 `/discovery` 在线/离线、runtime/build、视频/音频/输入/剪贴板能力、反控策略和 Agent Link Board currentCall；如果 Mac 端已经发起 Windows host 验收 call，UI 会提示“Mac 正在请求 Windows 配合”，DONE call 不当作待办且不回显 call command。如果端口上已有非桌面壳启动的 Windows host，会显示“已在线”，但停止按钮仍只停止桌面壳自己启动的进程。面板默认输入模式是“安全日志”，需要真实反控时再手动切到“真实控制”；反控策略下拉默认“需确认”，会把 `reverseControlMode=deny` 传给启动器并在状态区显示“反控：需确认”，只有可信局域网短测才切到“实验同意”。反控请求接收策略也要同步确认：`start-windows-host --status --boardSummary` 和 `check-windows-host-readiness --checkBoard --boardSummary` 都会显示 `reverse=temporary-grant|pending-request|deny-confirm|accept-lab|disabled`，默认 `deny-confirm` 只回 `LAN008` 不自动切换方向；`temporary-grant` 表示 Windows 本机已临时允许下一次，`pending-request` 表示刚安全拒绝过 Mac 请求并等待 Windows 临时允许后重试，`accept-lab` 只能用于可信局域网短测。命令行备用流程优先用 PowerShell：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-host-readiness.ps1 -CheckBoard -BoardSummary` 做低风险一键体检和通讯板 currentCall 摘要；Node 等价入口是 `node scripts/windows/check-windows-host-readiness.mjs --checkBoard --boardSummary`。它的 runtime 步骤也会消费 `start-windows-host --status --json`，并默认做 WGC 支持预检但不强制失败，真正实装 WGC 采集后可加 `-RequireWgc` / `--requireWgc` 变成强校验。若 Mac 端已经发起 Windows host 验收 call，`-CheckBoard` / `--checkBoard` 会把 active Mac -> Windows call 带进 JSON/普通输出/boardSummary，DONE call 不当作待办。若不确定 Windows host 当前是否已启动，先用 `node scripts/windows/start-windows-host.mjs --status --checkBoard` 或 PowerShell `-Status -CheckBoard` 只读查看 `/discovery`、runtime/build、视频/音频/输入/剪贴板能力、反控策略、旧 build 源码差异和通讯板 currentCall；该入口离线时只给安全启动建议，不会启动、不认证、不要求密码；需要脚本消费时用 `node scripts/windows/start-windows-host.mjs --status --json --checkBoard` 或 PowerShell `-Status -Json -CheckBoard` 输出纯 JSON，读取 `capabilities.reverseControl`、`windowsHostMediaReadinessCommand`、`windowsHostMediaReadinessPowerShellCommand`、`windowsVideoEncoderSupportCommand`、`windowsVideoEncoderSupportPowerShellCommand`、`windowsWgcBenchmarkCommand`、`windowsWgcBenchmarkPowerShellCommand`、`windowsReverseGrantStatusPowerShellCommand` 和 `windowsOpenOneTimeReverseGrantPowerShellCommand`；需要发到通讯板或给 Mac 端下一步时，用 `--status --checkBoard --boardSummary` / `-Status -CheckBoard -BoardSummary`，摘要会同时给出 Mac readiness、formal checklist、ready 后的 `--sendCall` 协调命令、`WindowsHostMedia=` / `WindowsHostMediaPs=` 媒体基线命令、`WindowsVideoSupport=` / `WindowsVideoSupportPs=` 视频能力体检命令、`WindowsWgcBenchmark=` / `WindowsWgcBenchmarkPs=` 基础刷新率/码率 benchmark 命令、`WindowsReverseGrantStatus=` / `WindowsOpenOneTimeReverseGrant=` 临时授权稳定标签、旧 `ReverseGrant=` / `ReverseGrantPs=` 兼容命令和 active Mac -> Windows call 提示。PowerShell 参数忘了可先跑 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/start-windows-host.ps1 -Help` 或 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/check-windows-host-readiness.ps1 -Help`，帮助只打印说明，不启动 host。Windows host 启动后可用 `-Profile deploy` / `--profile deploy` 要求端口可达、运行中 host build 与当前 git 一致，并跑带 1000ms 帧新鲜度和 timestamp 单调性强校验的视频/音频短验收，深度本机部署验收可用 `-Profile deep` / `--profile deep` 额外串联 `test-windows-host.ps1`。正式让 Mac 连入时也可用 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/start-windows-host.ps1 -PromptPassword -RequirePassword` 启动 Windows host，它会列出 Mac 端可填的局域网地址、自动做只读防火墙/端口检查，并通过 `LAN_DUAL_BUILD_ID` 暴露 `/discovery.runtime.buildId`；需要系统声音时再加 `-Wasapi`，需要可信实验自动同意反控时才显式加 `-ReverseControlMode accept`。脚本默认只读，不会自动改系统防火墙；需要预览放行命令时加 `-DryRunFirewallRule`，确认可信局域网且以管理员 PowerShell 运行时才加 `-AddFirewallRule`。需要临时验收旧进程时可加 `-SkipCurrentBuildCheck` / `--skipCurrentBuildCheck` 放宽 warning，正式部署验收不要放宽。
- Windows host 在线后，如果要让 Mac 端发起一次真实反控但又不想长期使用 `accept-lab`，优先在 Windows 桌面“本机被控”面板点击“临时允许反控”：它会从 Windows 本机打开约 30 秒的一次性授权窗口，下一次 `reverse_control_request` 通过后立即消耗，状态区会显示 `反控：临时允许 N 秒`；超时或使用后自动回到默认 `deny-confirm`。如果 Mac 已经先发起过反控请求，Windows 默认拒绝后会在面板显示 `反控：刚收到请求` 和“临时允许后让对方重试”的提示，此时点击临时允许，再让 Mac 重新点一次反控即可。命令行备用入口优先用 PowerShell 7：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/windows/allow-windows-reverse-control.ps1 -BoardSummary`，也可用 Node：`node scripts/windows/allow-windows-reverse-control.mjs --durationMs 30000 --boardSummary`；可在没有桌面壳或需要把授权状态发 Agent Link Board 时打开一次性授权，`-Status -Json` / `--status --json` 可读当前授权和最近请求，`-Revoke` / `--revoke` 可撤销。该授权管理端点只允许回环地址访问，局域网其他设备不能直接打开窗口。
- Mac client 的“反控策略”诊断也会显示 Windows 侧一次性授权和最近请求状态，并在会话诊断下方提供“请求反控/重试反控”按钮：如果看到“Windows 已临时允许一次 / N 秒内重试”，Mac 端应立即点“重试反控”；如果看到“Windows 已收到请求 / 临时允许后重试”或按钮回执提示 `LAN008`，说明 Windows 端已经安全拒绝过一次，先让 Windows 用户点“临时允许反控”，再由 Mac 端点“重试反控”。按钮只发送 `reverse_control_request` 并显示回执，不会发送输入事件；真实切换控制方向前仍按双方现场确认流程执行。
- Mac client 页面现在会在 `LAN008`、最近请求或临时授权窗口状态下直接显示并可一键复制 Windows 本机 PowerShell 推荐授权命令 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/windows/allow-windows-reverse-control.ps1 -HostName 127.0.0.1 -Port <Windows host port> -Grant -DurationMs 30000 -BoardSummary`，同时展示 Node 备用命令 `node scripts/windows/allow-windows-reverse-control.mjs --host 127.0.0.1 --port <Windows host port> --grant --durationMs 30000 --boardSummary`；这些命令仍必须由 Windows 端在 Windows host 本机运行，Mac 端只展示/复制提示，不直接打开授权端点。
- Mac client 事件日志面板也有“复制诊断”入口，复用不含连接密码的导出日志文本写入 Mac 浏览器剪贴板；现场需要把 Mac 控制 Windows 状态发给 Agent Link Board 或 Windows Codex 时优先点这个按钮，不必下载文件。`check-mac-client-readiness --boardSummary` 也会带 `MacClientPage=` 和 `CopyDiagnostics=` 提示，适合先确认本地页面在线，再发一行 readiness、再粘贴完整诊断。
- Mac client formal 工具已经把这条反控演练和安全认证路径写进 `runPlan` 和通讯板摘要：`node scripts/mac/check-mac-client-formal-status.mjs --discover --boardSummary`、`node scripts/mac/check-mac-client-formal-status.mjs --host <Windows IP> --port 43770 --boardSummary` 或 `node scripts/mac/run-mac-client-formal-smoke.mjs --discover --ensureClient --preflightOnly --boardSummary` 会给出 Windows 本机回环 PowerShell 推荐授权命令，并在 JSON 里保留 `windowsOpenOneTimeReverseGrantNodeFallback` / `windowsReverseGrantStatusNodeFallback` 备用命令；同时会输出 `SecureAuthPath=` / `WindowsSecureAuthStart=` / `WindowsSecureAuthStartNodeFallback=`。formal status 现在也可先只读 `--discover` 自动选择 Windows host，并从 Agent Link Board 安全消费 `WindowsSecureAuthPath=` / `SecureAuthPath=`，只接受 Windows 本机 `--host 0.0.0.0 --promptPassword --requirePassword` 隐藏密码重启命令，拒绝明文密码、token/secret、占位端口或错误 host；smoke 会继承 nested formal preflight 的 `commands.windowsSecureAuthPath`。授权命令必须由 Windows 端在 Windows host 本机运行，Mac 不直接访问授权端点；整个演练不发送密码、不发送输入事件、不执行 `inject`。
- Windows 桌面版“本机被控”面板现在可直接勾选“媒体基线”再点体检；这会调用同一条 `--probeMedia` readiness 路径，并在 UI 摘要里显示媒体基线正常/部分通过/失败。命令行仍可用下一条 `--probeMedia --boardSummary` 给 Agent Link Board 发一行状态。
- 需要把 Windows host 视频+音频基线作为一行状态发给 Mac 端时，优先运行 `node scripts/windows/check-windows-host-readiness.mjs --probeMedia --boardSummary`。它会顺序复用媒体聚合脚本，并输出 `media=ok|partial|failed` 与 passed/failed 计数；JSON 里读取 `Windows host media aggregate.details.summary.status`，不用再解析 `Windows media: ...` 文本。
- Windows 被控端输入注入已升级为常驻 C# SendInput helper；可用 `node scripts/windows/measure-windows-input-helper.mjs` 安全干跑量化 cold/warm/p95 延迟；后续真实 `system` 模式仍需有人看屏幕时用 `test-windows-host.ps1 -InputEvents -InputMode system` 验收手感和安全边界。
- 继续验证 Windows 被控端 WASAPI loopback：当前 30 秒本机基线为 1482 帧、49.98 FPS、最大间隔 33 ms、首帧约 395 ms、帧年龄最大 1 ms；短测试音电平强校验旧基线也已通过。观察脚本现在会在本机 host 上输出资源摘要，3.5 秒 WASAPI 短对照为 135 帧、稳态 49.72 FPS、最大间隔 32 ms、主进程工作集峰值约 62.5 MiB；媒体汇总脚本的音频顺序路径 `2026-06-13 13:02` 通过：133 帧、稳态 49.94 FPS、最大间隔 43 ms、首帧约 877 ms、帧年龄最大 13 ms。下一步重点看系统音量变化、60 秒以上长时间运行、Mac client 播放体验和无系统声音时的提示。可用 `node scripts/windows/observe-windows-host-audio.mjs --durationMs 30000 --minFrames 1200 --minFps 40 --maxGapMs 1000 --maxFrameAgeMs 1000 --requireMonotonicTimestamp` 做持续帧观察并强校验音频帧新鲜度；需要确认有声电平时加 `--playTone --requireLevel --minLevel 0.02`。
- 继续优化 Windows 控制端文件托盘和错误提示：接收中/拒绝/失败/超时状态条已补齐，下一步重点看真实大文件或压缩包复制后的 Windows 粘贴可用性、失败后重试路径和用户可理解性。
- Windows host 或假 Mac 认证相关改动后运行 `node scripts/windows/test-auth-retry-policy.mjs`，确认错误密码剩余次数、第三次断开和新连接正确认证未退化。
- Windows 控制端视频、缩放、设备发现或输入相关改动后，先用 `node scripts/windows/test-windows-client-browser.mjs --discover --diagnosticsOnly --expectDiscoveryRuntimeBuildId <build-id>` 做无密码 UI 验收，确认脚本能自动选中最佳 Mac host、刷新设备会自动选中真实在线 WebSocket 目标并显示 runtime；已知 IP 且想避免扫整段局域网时，加 `--discoverNoLocalSubnets --host <Mac IP> --port 43770`。再用真实 Mac host 运行 `node scripts/windows/test-windows-client-browser.mjs --discover --promptPassword --requirePassword --requireH264`，由用户在 Windows 本机隐藏输入正式密码，确认 H.264/WebCodecs 画布解码未退回 JPEG、`H264Errors=0`，并确认黑边输入防护回归通过；等待期间默认每 10 秒会打印连接、远端、诊断、FPS、音频和帧计数快照，方便判断卡在认证、视频、H.264 还是音频播放。同时看页面“帧延迟”和诊断条是否有 `到达 <ms>` 或“时钟偏差”提示，避免再把随机模拟数值当成真实延迟。页面自检 `--discover` 路径改动后先跑 `node scripts/windows/test-windows-client-browser-discover.mjs --timeoutMs 60000`。
- Windows host 相关改动后运行 `scripts/windows/test-windows-host.ps1`，确认真实视频首帧、文本剪贴板和文件剪贴板接收未退化；涉及文件剪贴板接收边界时先跑 `node scripts/windows/test-windows-clipboard-bridge.mjs` 覆盖模块级恶意/损坏分片，再跑 `node scripts/windows/test-windows-host-clipboard-security.mjs` 覆盖真实 WebSocket 服务入口；涉及启动/部署路径时，再跑 `node scripts/windows/check-windows-host-readiness.mjs --requireOpen --requireCurrentBuildId` 或桌面版部署档，确认 `/discovery.runtime.buildId` 不是旧进程；涉及音频时可加 `-AudioMode wasapi -RequireAudio`；涉及 Mac 反控链路时再运行 `node scripts/windows/test-mac-client-browser.mjs`，确认 Mac client 页面可显示 Windows 画面、收到 `input_ack`，并完成 `Command+C` 到 `Ctrl+C` 映射、最近连接保存/回填/清空、文本/本机剪贴板监听/文件剪贴板发送；需要验收真实 PCM 播放时，本机临时 host 可加 `--requireAudio --maxAudioFrameMs <毫秒> --maxAudioPlaybackMs <毫秒>`，已运行 host 可加 `--useExistingHost --enableAudio --expectAudioPayload --expectAudioPlayback --maxAudioFrameMs <毫秒> --maxAudioPlaybackMs <毫秒>`；需要体验阈值时加 `--maxInitialVideoMs` 和 `--observeVideoMs --minObservedVideoFrames --minObservedVideoFps`，需要断线恢复阈值时加 `--expectReconnect --maxReconnectRestoreMs`；认证相关改动可加跑 `node scripts/windows/test-mac-client-browser.mjs --expectAuthFailure --expectedAttemptsRemaining 2 --expectedMaxAttempts 3`。
- Windows host 一键反控请求策略相关改动后，先跑 `node scripts/windows/test-windows-host-reverse-control.mjs`：它覆盖未认证 `LAN002`、默认 `deny` 的 `LAN008` 安全拒绝和最近请求状态、一次性临时授权消耗、缺少 `requestId`、显式 `accept` 实验策略和 `disabled` 能力声明；命令行授权助手相关改动再跑 `node scripts/windows/test-windows-reverse-control-grant-helper.mjs`，确认 `--status/--grant/--revoke`、`--json`、`--boardSummary` 和离线失败摘要都不泄密、不发送输入、不执行 `inject`。当前默认策略仍是安全拒绝，真正产品化同意需要后续接桌面确认入口。
- Windows host 或 Mac client 视频参数相关改动后优先运行 `node scripts/windows/observe-windows-host-media.mjs --resourceSampleTree true --json`，顺序确认普通启动下 60 Hz 视频请求、实际 FPS、最大帧间隔、采集管线、请求码率、`jpegQuality`、帧新鲜度、WASAPI 音频帧、资源摘要和 `summary.status`；需要同步到通讯板时改用 `--boardSummary` 输出一行无密媒体基线，单路成功单路失败会标为 `Windows media: partial`，全部执行链路失败才标为 `Windows media: failed`，便于直接说明 FFmpeg/GDI/WASAPI 卡在哪里。视频或音频其中一路失败时，脚本仍会继续尝试另一条链路并保留成功结果，避免一次抓屏波动把音频基线也丢掉。普通输出模式下视频、音频和聚合观察都会默认每 10 秒打印进度；现场长测可加 `--progressIntervalMs 5000`，自动化 JSON / `--boardSummary` 会保持干净不混入心跳。若只看视频，再运行 `node scripts/windows/observe-windows-host-video.mjs --fps 60 --useDefaultMaxScreenFps --expectSessionFps 60 --durationMs 4000 --minFrames 140 --minFps 35 --requireMonotonicTimestamp --maxFrameAgeMs 1000 --resourceSampleTree true`。可再用 `--bandwidthKbps 5000 --qualityPreset smooth --json` 与 `--bandwidthKbps 40000 --qualityPreset sharp --json` 对照低/高码率；最后运行 `node scripts/windows/test-mac-client-browser.mjs` 确认 Mac client 默认 1080P/60Hz/20Mbps 和 2K/60Hz/40Mbps 更新路径未退化。
- 继续维护本机假 Mac 服务，用于快速回归和失败场景模拟。

## 暂不优先

- 安装包。
- 公网账号系统。
- 大文件断点续传。
- H.264 硬件编码深度调参和长期性能优化。（WGC JPEG 桥接 + `h264_nvenc` 正确性原型已验证；短期下一步是 raw BGRA/NV12 或原生编码，不做深度调参。）
- UDP/mDNS 自动发现完整实现。
